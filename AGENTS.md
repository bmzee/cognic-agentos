# Cognic AgentOS — Operating Model

## Core principle

This repo ships **AgentOS only**: the hardened governance + runtime + protocol kernel that banks deploy once and run forever. Agents, tools, and skills are out of scope here — they ship as separately-versioned plugin packs that install on top of AgentOS.

## What lives where

| Lives in cognic-agentos (this repo) | Lives elsewhere |
|---|---|
| Governance kernel (`core/`) | Layer C agents (`cognic-agent-<name>` repos) |
| Harness (`harness/base_agent.py`) | Per-agent workflows (ship in agent packs) |
| LLM gateway, retrieval orchestrator, persistence, observability | Per-agent eval scorers (ship in agent packs) |
| Channels, RBAC, portal API + workbench | UI (`cognic-portal-ui` separate artefact) |
| Plugin registry + MCP host + A2A endpoint | Tool packs (`cognic-tool-<name>` MCP servers) |
| Sandbox + sub-agent primitives | Skill packs (`cognic-skill-<name>` MCP-composing services) |
| ISO 42001 compliance evidence | Bank-specific overlays (themes, OIDC, custom CBS adapters) |

If you find yourself adding a Layer C agent or persona-specific workflow inside this repo, **stop**. It belongs in its own pack repo.

## Operating modes

### Autonomous low-risk build
Scaffolding, boilerplate, OS-tier tests, mock data inside test paths only, docs, and integration glue.

### Pair-engineering
Critical controls — anything in `core/`, `compliance/`, `protocol/plugin_registry`, `sandbox/`, `subagent/`, or that touches RBAC / cloud-policy / decision-history. Use `core-controls-engineer`.

### Review-and-hardening
Refactors, PR cleanup, negative-path tests, ADRs, evidence docs, RCA notes, release checks.

## Session protocol

1. Identify what you're touching: governance kernel? protocol layer? plugin discovery? portal API? OS subpackage?
2. Read the relevant ADR before editing
3. Keep changes inside declared scope
4. Run tests and document remaining risks
5. Update ADR / evidence if the change requires it

## Stop rules

Stop for human review when touching:
- Anything in `core/` (governance primitives, including `core/approval`, `core/policy`, `core/emergency`, `core/memory`)
- **Hash-chain canonical-form** (`core/canonical.py` — `canonical_bytes`, `hash_record`, `_json_default`, `ZERO_HASH`). Canonical form is the wire-format for evidence-pack export per ADR-006; any change is a wire-protocol change that breaks past evidence verification. Requires human review on **every** edit, not just non-trivial ones, plus an explicit `schema_version` bump in `audit_event` + `decision_history` migrations. (Sprint 2 amendment, 2026-04-28.)
- Plugin trust gate / signature verification (`protocol/plugin_registry.py`, `protocol/trust_gate.py`, `protocol/supply_chain.py`)
- MCP / A2A authorization paths (`protocol/mcp_authz.py`, `protocol/a2a_authz.py`)
- Sandbox or sub-agent enforcement boundaries (including resumable-session checkpoint/wake)
- Cloud-policy enforcement (`llm/gateway.py`)
- ISO 42001 control mapping
- RBAC (`portal/rbac/`)
- Wire-protocol contracts (MCP / A2A schemas, including A2A protobuf source + version-negotiation)
- Evidence-pack format (changes how examiners audit)
- Model registry lifecycle transitions (`models/` + `models/trust.py` + `portal/api/models/lifecycle_routes.py` — register/promote/retire route module added at Sprint 9.5 B4 owns the cosign path-containment helper `_resolve_under_tenant_root` per the resolve-then-validate doctrine, the cosign-OUTSIDE-transaction `_verify_record_signature` with the B4 R2 P1 bundle-digest recompute-before-cosign evidence-integrity gate, the body-aware promote-scope resolution + HumanActor gate when `target_state="serving"`, the state-aware HumanActor gate at `/retire` when current state is `serving`, and the wire-body-collapse cross-tenant invisibility contract (`portal/rbac/model_tenant_isolation.py` — model module ships the stronger wire-collapse than the pack equivalent: cross-tenant + unknown both render as 404 `model_not_found` so a probe cannot distinguish; internal log retains `tenant_id_mismatch` for ops + SIEM correlation))
- Pack data-governance contracts (`packs/evidence/data_governance.py`, runtime DLP enforcement)
- Kill-switch / quota enforcement (`core/emergency/kill_switches.py` — the full 8-class matrix landed Sprint 13.6a; `core/emergency/quotas.py` — the token quota meter landed Sprint 13.6b, on the CC gate)
- Policy-as-code engine (`core/policy/engine.py` + Rego bundles in `policies/_default/`)
- Memory governance enforcement (`core/memory/` per ADR-019)
- UI event-stream contract (`protocol/ui_events.py` per ADR-020 — public event schema, must remain backward-compatible across versions)
- Policy bundle: `policies/_default/sampling.rego` (per ADR-015 + ADR-002 §"MCP sampling" — sampling decision-point bundle invoked by `protocol/mcp_capabilities.py`; refusal-vocab change is a wire-protocol break for sampling-enabled tool packs)
- Policy bundle: `policies/_default/supply_chain.rego` (per ADR-015 + ADR-016 — supply-chain decision-point bundle invoked by `protocol/trust_gate.py` for attestation-grade evaluation; drift breaks the runtime trust-gate's evidence reading)
- Policy bundle: `policies/_default/elicitation.rego` (per ADR-015 + ADR-020 §69-77 — Step 5 of the T8 elicitation gate at `data.cognic.ui.elicitation_submit.allow`; default `allow := false` + URL-always-allows + form-mode-restricted-data-class refusal is the wire-protocol-public Rego contract bank overlays cannot override without coordinated kernel + rego update)
- Policy bundle: `policies/_default/sandbox.rego` (per ADR-015 + ADR-004 + Sprint-8A spec §13 — Wave-1 sandbox admission bundle at `data.cognic.sandbox.admit.allow`; default `allow := false` + 5 rules: (1) allow only if the tier is admissible via `_tier_admissible` (arm 1 = the 2 safe tiers `{read_only, internal_write}`; arm 2 = a high-risk tier on strict Python-attested `input.approval_verified == true` — the Sprint-13.5c1 CONVERT) AND tenant-max satisfied AND credential precondition satisfied AND runtime-image authorised AND egress HTTP/HTTPS-only (a verified high-risk grant does NOT bypass these — every conjunct still applies); (2) refuse the 6 high-risk tiers `{customer_data_read, customer_data_write, payment_action, regulator_communication, cross_tenant, high_risk_custom}` with `sandbox_high_risk_tier_refused_pre_13_5` when `approval_verified` is NOT attested — the value name is KEPT as the engine-absent / unverified reason (the Python seam always threads `approval_verified`, `False` on unwired/auto paths; the seam is production-wired for cold-create at 14A-A2 + wake at 14A-A3c); (3) refuse on `vault_path` set + `credential_adapter_wired=false` (defence-in-depth with Stage-2 §6.1 step 3); (4) refuse on runtime image not in canonical catalog AND not in tenant allow-list (defence-in-depth with Stage-2 §6.1 step 6); (5) refuse on any `egress_allow_list` entry carrying a non-HTTP/HTTPS scheme OR malformed shape — PURE Rego guard with `is_array` + `is_string` type checks so the bundle catches Stage-1 bypass independently per the Sprint-8A T11 R2-R3 defence-in-depth contract. Wire-protocol-public stop-rule precedent identical to `sampling.rego` / `supply_chain.rego` / `elicitation.rego`; bank overlays may tighten; loosening kernel defaults requires coordinated kernel + ADR amendment)
- Policy bundle: `policies/_default/scheduler.rego` (per ADR-015 + ADR-022 + Sprint-10.5b spec §4.8 — Wave-1 scheduler admission bundle exposing TWO wire-protocol-public decision points: `data.cognic.scheduler.admit.allow` (bool) + `data.cognic.scheduler.admit.refusal_reason` (string, 3-value closed-enum). Default `allow := false` + **3 allow arms** (post-Sprint-13.5c2 + Sprint-14A-A4a): arm 1 = safe tiers (`class ∈ {interactive, background}` AND `pack_risk_tier ∈ {read_only, internal_write}` AND tier NOT in the 6-value high-risk set — safe + high-risk sets disjoint by construction); arm 2 (Sprint-13.5c2 CONVERT) = a high-risk tier on strict `input.approval_verified == true`; arm 3 (Sprint-14A-A4a) = a high-risk tier on strict `input.approval_delegated_to == "sandbox_admission"` (the downstream sandbox admission gate owns the human checkpoint; the scheduler mints no grant of its own — **live-exercised at Sprint 14A-A4b: the managed-run executor sets the signal with a pack's real manifest tier**). Refusal-reason if/else chain with **deterministic precedence** per plan §1090: `scheduler_class_unknown` (FIRST — admission cannot evaluate tier semantics until class is in vocabulary) → `scheduler_high_risk_tier_refused_pre_13_5` (mirrors sandbox.rego's 6-tier set; the high-risk arm fires ONLY when NEITHER `input.approval_verified == true` (Sprint-13.5c2 guard) NOR `input.approval_delegated_to == "sandbox_admission"` (Sprint-14A-A4a guard) — the value name is KEPT as the engine-absent / unverified / undelegated reason; no wire rename) → bare-else `scheduler_default_deny` (fall-through; also the `default refusal_reason` for shape-mismatched/missing-input cases the chain cannot evaluate). 3-value closed-enum refusal vocabulary is wire-protocol-public + drift-detector-pinned at `tests/unit/policies/test_scheduler_rego.py::TestSchedulerRegoVocabularyClosed`. Consumed by `core/scheduler/policy.py` via the **10-key** `_build_rego_input` projection (the spec §4.8 8-key set `tenant_id` / `pack_id` / `actor_subject` / `class` / `pack_kind` / `pack_risk_tier` / `current_tenant_concurrent_count` / `requested_estimated_tokens` + `approval_verified` per Sprint-13.5c2 + `approval_delegated_to` per Sprint-14A-A4a, both always-threaded/nullable); the Python policy layer SUPPRESSES the raw bundle `refusal_reason` to `policy_reason=None` on the allow path per plan §1179 (propagating `scheduler_default_deny` on an allow row would be audit/SIEM misleading). Wire-protocol-public stop-rule precedent identical to `sampling.rego` / `supply_chain.rego` / `elicitation.rego` / `sandbox.rego`; bank overlays may tighten; loosening kernel defaults requires coordinated kernel + ADR amendment)

## Critical-controls rule

The following modules are **critical controls**. They get extra scrutiny — 95%+ test coverage, negative-path tests required, no casual refactors:

*Core governance:*
- `core/audit.py`
- `core/canonical.py` (Sprint 2 — single source of truth for canonical form + SHA-256 framing; wire-format for evidence-pack export)
- `core/decision_history.py` + `core/chain_verifier.py`
- `core/guardrails.py`
- `core/escalation.py`
- `core/sla.py`
- `core/auto_degradation.py`
- `core/citation.py`
- `retrieval/citation_verifier.py`

*Runtime authority + emergency (Sprint 13.5 approval; Sprint 13.6a kill switches; Sprint 13.6b quotas):*
- `core/approval/engine.py` (per ADR-014 — runtime tool approval; 4-eyes; risk-tier enforcement)
- `core/policy/engine.py` (per ADR-015 — Rego decision engine for admission, routing, approval, egress, sub-agent spawn, lifecycle)
- `core/emergency/kill_switches.py` (per ADR-018 — fail-closed kill switches with ≤30s P99 propagation. Sprint 13.6a landed the full `KillSwitchEngine` 8-class matrix (`memory_write_freeze` seed + `pack`/`tool`/`model`/`tenant_packs`/`tenant_full`/`cloud_routing`/`feature`) alongside the UNTOUCHED 11.5b seed: generalized `cognic:killswitch:<class>:<scope_key>` scheme, seed-identical fail-closed cache, brake-before-evidence `flip`/`revert` emitting value-free `emergency.kill_switch_flipped`/`_reverted` chain rows (ISO A.6.2.5 + A.9.2; `kill_switch_live_evidence_degraded` on evidence failure with the switch LIVE), `check_gateway` precedence `tenant_full→model[alias-keyed]→cloud_routing`, `SchedulerKillSwitchConformer` (the `KillSwitchInterrogator` real conformer; DI-bound at the composition root `harness/runtime.py` at Sprint 13.7 — wraps the runtime `KillSwitchEngine` into the scheduler's `kill_switch_interrogator` slot) + `MemoryFreezeConformer` (seed OR tenant_full). Gateway F4 gate + memory freeze are production-wired via `build_runtime`; the operator portal surface (`portal/api/emergency/routes.py`, off-gate) mounts from the `create_app(emergency_engine=...)` kwarg (approval-13.5b1 injection-seam posture). `enforcement_status` honesty: `memory_write_freeze`/`model`/`cloud_routing`/`tenant_full` LIVE; `pack`/`tenant_packs`/`tool`/`feature` `armed_no_live_consumer` until their consumers wire.)
- `core/emergency/quotas.py` (per ADR-018 — the `QuotaEngine` token meter, landed Sprint 13.6b, on the CC durable gate. **Decomposed atomic Redis counters, NOT Lua** (the design checkpoint found the bundled adapter exposes only `get`/`set`/`scan_iter`): a consumer-owned `_AsyncRedisQuotaLike` Protocol (`incrby`/`decrby`/`get`/`set`/`getdel`/`expire`; NO shared `db/adapters` change). `would_admit` (the `QuotaInterrogator` seam) does `INCRBY`-first-check-own-cumulative + rollback `DECRBY`; `release_reservation` is `GETDEL`-first (atomic exactly-once → idempotent). "Atomic counter RESERVATION," NOT a single transaction — a bounded crash/partial-increment window is quota-DENYING (never opening) + 48h-TTL-self-heals; fail-immediately-on-Redis-error. Duplicate-`task_id`: same-shape idempotent (no second increment), mismatched-shape fails loud (`QuotaReservationConflict`). Two planes (precision lock 2): scheduler = true per-task reservation (`task_id`/`pack_id`/`estimated_tokens`); gateway = tenant-aggregate actuals-based `check_gateway_admit` (F4 slot, after the kill-switch gate) + post-completion `record_actuals`. Limits = `Settings` defaults + ADR-023 tighten-only ceilings. Hard 100% → `quota_exhausted` (gateway) / `refused_quota_exhausted` (scheduler) + `quota.exhausted` chain row; soft 80% → log-warn only. The gateway quota gate is production-wired via `build_runtime`; the scheduler `QuotaInterrogator` binding landed at the composition root `harness/runtime.py` at Sprint 13.7 (the SAME `QuotaEngine` instance is the scheduler's `quota_interrogator`; conformance test-proven). The real-Redis concurrency proof is env-gated (`COGNIC_RUN_REDIS_INTEGRATION=1`).)

*Plugin trust + supply chain:*
- `protocol/plugin_registry.py` (entry-point discovery)
- `protocol/trust_gate.py` (cosign verification)
- `protocol/supply_chain.py` (per ADR-016 — SLSA + in-toto + SBOM + vuln + license + Sigstore bundle retention)

*Protocol authorization:*
- `protocol/mcp_authz.py` (per ADR-002 amendment — OAuth/PRM token cache + refresh + AS allow-list; also listed under "Protocol — MCP host (Sprint 5)" below)
- `protocol/a2a_authz.py` (per ADR-003 — per-tenant token authorization + Wave 2 mTLS hook + Wave 3 VC hook; also listed under "Protocol — A2A endpoint (Sprint 6)" below)

*Protocol — MCP host (Sprint 5):*
- `protocol/mcp_authz.py` (per ADR-002 amendment — already in critical-controls list pre-Sprint-5; gate enforcement extended in Sprint 5)
- `protocol/mcp_capabilities.py` (per ADR-002 + MCP-CONFORMANCE.md — manifest validation, capability default-deny enforcement, STDIO four-gate + Decision Lock umbrella, sampling four-condition gate via OPA Rego)
- `protocol/mcp_manifest.py` (per Sprint-5 R1 P2 #2 — signed-manifest extractor; deferred-load invariant via `Distribution.locate_file()` without importing pack code per ADR-002 §gate 1)
- `protocol/mcp_transports.py` (per Sprint-5 Decision Lock — Streamable HTTP transport with per-event hook-failure semantics + STDIO refusal-only stub; Sprint-8 launcher is a separate critical-controls module added then)
- `protocol/mcp_host.py` (per ADR-002 — admission-to-invocation orchestrator; ADR-014 transitional risk-tier gate; audit + decision-history correlation via `_emit_call_evidence`; per-tenant `list_tools` cache + bounded pagination + opaque cursor fingerprints + deep-copy descriptors)

**Sprint 13.8 (composition root) — MCP host production construction site**: `MCPHost` is production-constructed in the `create_app` LIFESPAN (`portal/api/app.py`, off-gate), SDK-gated on `is_mcp_available()` (the `mcp` SDK is an optional `adapters` extra; the kernel image lacks it), AFTER `build_runtime` (which stays SDK-free) because the host needs `runtime.audit_store` / `decision_history_store` / `approval_engine`. The off-gate builder `harness/mcp_host.py` owns `build_mcp_host(...)` (assembles `MCPAuthzClient` + `StreamableHTTPTransport` + `MCPHost`, threading `approval_engine=runtime.approval_engine` so the 13.5b2 approval seam is WIRED but DORMANT — no portal route consumes `app.state.mcp_host` yet, mirroring the 13.7 scheduler posture) + the manifest→`MCPServerEntry` mapper (re-extracts each registered pack's manifest, mirrors the admission contract: NotFound/absent-block silent-skip vs Malformed/present-malformed-block warn-skip via a mapper-local tri-state walk; `server_url` http/https SSRF pre-filter; HTTP-family-only via the drift-pinned `_MCP_HTTP_SERVED_TRANSPORTS`; required `scopes`; fail-closed `data_classes`). The trusted-set source is the read-only additive `PluginRegistry.iter_registered_pack_candidates()` (CC stop-rule module modified — registered-only, `package_name` from `record.entry_point_value` per the `_mcp_admit` doctrine at `plugin_registry.py:852`, NO `EntryPoint.load()`; ≥ 95/90 verified in-commit). A dedicated lifespan-owned `httpx.AsyncClient` (predeclared before the inner try) is closed on shutdown + on fail-soft construction failure (`app.state.mcp_host = None` + ERROR log). No CC promotion (count stays 129); the MCP-invocation route + the 14A managed-runtime caller + startup discovery/trust registration are out of 13.8 scope. See ADR-002 "Sprint 13.8 amendment".

*Protocol — A2A endpoint (Sprint 6):*
- `protocol/a2a_authz.py` (per ADR-003 — already in critical-controls list pre-Sprint-6 under "Protocol authorization"; gate enforcement extended in Sprint 6 T15. Per-tenant pinned-token validation with closed-enum 8-value `A2AAuthzReason`; Vault-rotated; anonymous-A2A forbidden Wave-1; Vault-read exception mapping per Sprint-5 T15 R1 P2 #2 doctrine)
- `protocol/a2a_agent_cards.py` (per ADR-003 + A2A-CONFORMANCE.md — three-pass card validator: Pass 1 upstream A2A 1.0 schema + Pass 2 AgentOS bank-grade profile (provider, securitySchemes, securityRequirements, signatures, ≥1 supportedInterfaces entry, **+ T14 Wave-2 auth refusal — any `mtlsSecurityScheme` declared anywhere in `securitySchemes` refused under Wave-1 bearer-token policy with closed-enum reason `agent_card_profile_wave2_auth_required`**) + Pass 3 JWS verification against Sprint-4 `TrustGate` per-tenant trust root. Identity-routing critical: a forged or tampered card routes outbound traffic to attacker-controlled endpoints. 11-value `AgentCardValidationReason` literal. Outbound dispatch path also pins `A2A-Version: 1.0` header on every `_http.get` per T14 Fix #2)
- `protocol/a2a_endpoint.py` (per ADR-003 — inbound receiver + task lifecycle state machine (created → running → succeeded / failed / cancelled) + cross-agent chain linkage via parent-trace + child-trace propagation. Anonymous-refusal gate + Wave-2-refusal gate + caller-URL refusal at the routing gate (URL-shaped `target_agent` → spec wire code `method_not_found` + `policy_reason="unknown_target"`) live here. Single-writer for `TaskState` transitions via `_transition`; pinned by an AST-walk regression in `a2a_cancellation.py`)
- `protocol/a2a_schema.py` (per ADR-003 — pinned A2A 1.0 wire-format types via `a2a-sdk == 1.0.2` re-exported through PEP 562 lazy `__getattr__` so the module imports cleanly without the SDK installed; first attribute access fires `require_a2a()`. Wire-format drift = wire-protocol break; the schema-drift CI gate in `tests/unit/protocol/test_a2a_schema_drift.py` (env-gated on `COGNIC_RUN_A2A_UPSTREAM=1`) catches upstream movement before it reaches us. Pinned digest constants + the upstream URL constants live here)
- `protocol/a2a_version.py` (per ADR-003 + AGENTS.md §"Wire-protocol contracts" — A2A-Version header negotiation; closed-enum 6-case `A2AVersionOutcome` matrix (`accepted` / `absent_rejected` / `legacy_rejected` / `higher_minor_degraded` / `unsupported_rejected` / `malformed_rejected`); rejecting absent-header per spec — the spec interprets absent as A2A 0.3 which AgentOS does not implement. Module is small + pure-functional but the doctrinal surface is wire-protocol-public; promoted from non-critical at Sprint-6 T15 R0 P2 #4 + R2 P2 #4. Source-of-truth for `PINNED_VERSION = "1.0"` shared between inbound negotiator + outbound `a2a_agent_cards._http.get` callers)
- `protocol/a2a_errors.py` (per ADR-003 — owns the spec wire `A2AErrorCode` literal (14 spec-defined codes) + the AgentOS `A2APolicyRefusalReason` literal (11 policy reasons including `unknown_target` / `wave2_feature_refused` / `agent_card_signer_not_allowlisted`) + the `_POLICY_REASON_TO_SPEC_CODE` mapping that drives the error-response builder. Drift in any of these changes what remote A2A callers see; promoted from non-critical at Sprint-6 T15 R3 P2 #2 — earlier draft kept this non-critical because the module is "just enums"; reviewer correctly flagged that the mapping IS wire-protocol contract)
- `protocol/ui_events.py` (per ADR-020 stop rule — Wave-1 typed event taxonomy + emit-hook layer. Public event schema; MUST remain backward-compatible across versions. All 11 Wave-1 Pydantic event-family models seeded in Sprint 6 (agent_run / tool_call / subagent / approval / artifact / interrupt / frontend_action / memory / decision_audit / policy / kill_switch); two-level discriminated union (per-family inner on `type`, top-level on `family`); 3 families wired with emit hooks in Sprint 6 (`tool_call.*`, `artifact.*`, `decision_audit.event_appended`); per-hook deep-copy isolation + tuple-snapshot at dispatch entry to defend against self-registering hooks. Other 8 families have model-only stubs; their emit hooks land in their owning sprints per the ADR-020 phase table)

*Authoring — SDK + CLI (Sprint 7A):*
- `cli/validate.py` (per ADR-008 — orchestrator that coordinates the six per-concern validators + the shape gate against the canonical [pack] / [identity] / [a2a] / [mcp] / [data_governance] / [risk_tier] / [supply_chain] block layout; build-time half of the trust gate; mirrors the runtime `protocol/plugin_registry.py` admission orchestrator. Closed-enum `ValidatorReason` literal at `cli/__init__.py` carries every refusal taxonomy across the six per-concern validators.)
- `cli/validators/identity.py` (per ADR-002 amendment + AGNTCY/OASF Wave-1 strictness — three-tier identity matrix: mandatory fields (agent_id / display_name / provider_organization / provider_url / agent_card_url; agent_card_jws_path agent-only) refuse on missing; optional Wave-1-only-in-Wave-2 fields (oasf_capability_set) warn on missing; Wave-3-reserved fields (verifiable_credentials_path) refuse only if present + path malformed. Wire-protocol-public for cross-org agent discovery.)
- `cli/validators/a2a.py` (per ADR-003 — Wave-2 capability-feature refusal on the [a2a] block. Single Wave-2 refusal mirroring the runtime `read_pack_capabilities` filter; runtime silently filters, validator REFUSES manifest at build time. Dual-path lookup via R23 doctrine. Promoted to critical-controls at Sprint-7A T16 closeout — T8 author's deferred call resolved on the rule "non-trivial allow/deny logic" since refusal paths runtime does not have qualify as policy.)
- `cli/validators/data_governance.py` (per ADR-017 — runtime DLP enforcement depends on this contract being well-formed at build time. Validates closed-enum `data_classes` / `purpose` / `retention_policy` / `egress_allow_list` shapes + `retention_max_window` conditional requirement when `retention_policy != "none"` + cross-validation against `[risk_tier].tier`. Closed-enum vocabularies live at `cli/_governance_vocab.py`.)
- `cli/validators/supply_chain.py` (per ADR-016 — feeds the runtime trust gate. Validates `[supply_chain].attestation_paths` is non-empty + every declared file is reachable + non-empty + path-traversal-safe + AUTHOR-FILL-free. Closed-enum `supply_chain_attestation_path_unresolvable` carries five sub-cases via `payload.failure_mode`.)
- `cli/sign.py` (per ADR-016 — full bundle generator: cosign sign-blob + syft SBOM + grype vuln scan + license audit (pip-licenses) + AgentCard JWS (joserfc, agent packs only) + SLSA provenance template render + in-toto layout template render + 7-attestation persister. Security-critical signing path; the path that produces the cosign signature the runtime trust gate verifies. T14.A landed cosign sign-blob; T14.B landed the orchestrator + the four other supply-chain binaries + JWS + SLSA + in-toto.)
- `cli/verify.py` (per ADR-016 — offline trust gate; mirrors the Sprint-4 runtime trust-gate verification path. **11 numbered steps** (substeps 3b + 5b carry related setup off the main spine): Step 1 trust-root resolution → Step 2 manifest pack-kind → Step 3 attestation-file existence probes (7 files) [substep 3b reads pyproject metadata for the wheel cross-check] → Step 4 wheel discovery → Step 5 cosign verify-blob [substep 5b wheel-anchored integrity check: kind / name / version / entry-points 4-tuple] → Step 6 SBOM digest match → Step 7 SLSA provenance shape + wheel-subject match → Step 8 in-toto layout shape + expected-artifact-set → Step 9 AgentCard JWS (agent packs only) → Step 10 manifest re-validation via the full validate pipeline → **Step 11 load probe (FINAL gate)**. R15 follow-up round 2 P2 #1: load probe was originally step 5c; promoted to step 11 so pack code never executes until every non-executing trust check has passed.)
- `cli/_load_probe.py` (per ADR-016 + R15 follow-up round 2 P2 #2 — isolated-subprocess `EntryPoint.load()` probe. Step 11 of the verify trust pipeline. Probe runs under `sys.executable -I` with minimal PATH+HOME env, asyncio timeout + SIGKILL + reap. **Result-channel hardened five layers deep**: fd inheritance via pass_fds (no path in argv/env); per-invocation 256-bit hex success token via env that the child pops to a local before any imported-module code runs; all probe state in `_run_probe()` locals (NOT __main__ globals); sys.argv stripped after capture; token written by probe-owned code only after `ep.load()` returns; parent enforces token match — mismatch routes to closed-enum `load_probe_success_token_mismatch` (refusal, fail-closed). Stdout/stderr redirected to `os.devnull` file objects (bounded discard, NOT `io.StringIO()` — R15 follow-up round 1 P3 fix prevents print-in-loop OOM in child).)
- `cli/_wheel_integrity.py` (per ADR-016 + R15 follow-up round 1 P2 #1 — wheel identity + dist-info + METADATA + entry-point shape validator. Helper threads the validated `(module_path, object_path)` tuples to verify via 4-tuple return so the load probe operates on exactly the source the integrity helper validated. Eliminates the path-suffix re-discovery anti-pattern that would otherwise let a decoy `aaa/entry_points.txt` redirect the probe. Sprint-7A2 T9 amendment: kind-derivation table extended to map `cognic.hooks` → `"hook"` so hook-pack wheels flow through the same identity + dist-info + entry-point integrity checks as tool / skill / agent packs.)

*Authoring — Hook packs (Sprint 7A2):*
- `packs/hooks/registry.py` (per ADR-008 + ADR-017 — verified-hook admission gate. Keys hook entries by hook ID + phase + pack identity + signed-artefact digest; refuses fail-closed on duplicate-ID across packs, stale digest vs the trust gate's verified bundle, and cross-pack-conflict scenarios. The build-time `HookDeclaration` dataclass enforces the Wave-1 fail-policy invariant (`fail_policy="fail_open"` requires a non-empty `fail_open_exception`) at construction time so a malformed registration cannot reach the dispatcher's MRO-walk carve-out path. `cli/validators/hooks.py` refuses every `fail_open` declaration at the build-time boundary; `fail_open_exception` is wired but unreachable through the manifest pipeline until the matching build-time shape lands in a follow-up sprint per ADR-017 §"DLP hook failure policy".)
- `packs/hooks/dispatcher.py` (per ADR-008 + ADR-017 — runtime decision engine + 5-value closed-enum `HookFailureMode` (`hook_timeout` / `hook_exception` / `hook_malformed_result` / `hook_policy_refused` / `hook_payload_unscannable`). Deterministic phase dispatch with explicit ordering, per-hook timeout clamped against `Settings.hook_max_timeout_s`, payload-budget pre-check (over-budget routes to `hook_payload_unscannable` BEFORE any hook runs), and audit linkage via `policy_input_digest` of the ORIGINAL governed payload (transformations never enter the audit chain). Symmetric exception ordering — pre-instantiation `except HookContractError` matches BEFORE generic `except Exception` so a malicious declaration cannot smuggle a contract violation past the malformed-result gate by naming `HookContractError` (or any subclass name) as `fail_open_exception` and expecting the carve-out's MRO walk to catch it. Pinned by AST self-tests + threat-model-revert verification per `feedback_security_regression_hardening.md`. Operator runbook: `docs/operator-runbooks/hook-pack-failure-policy.md`.)
- `packs/hooks/dlp_integration.py` (per ADR-017 — DLPGuard adapter wrapping `dispatch_for_pack` with `dlp_pre` / `dlp_post` phase semantics. Closed-enum 3-value `DLPRefusalReason` (`dlp_hook_id_unresolved` / `dlp_dispatcher_failed` / `dlp_dispatcher_refused`); ADR-017 line 97 enforcement boundary. T8 commit explicitly tagged `(CRITICAL CONTROLS)`; the refusal-payload-contract-divergence and delegate-first-preserves-precedence doctrine memories were both born from T8 R1 P2 fixes on this module — wrapper's contract for "what payload to return on refusal" diverges from the engine's last-seen-payload contract (DLPGuard returns the ORIGINAL governed payload on every refusal, never a partial-redaction leak); pre-validation passes are forbidden because they break engine precedence (oversized + unknown-id MUST surface as `hook_payload_unscannable` / `dlp_dispatcher_failed`, not `dlp_hook_id_unresolved`).)
- `cli/validators/hooks.py` (per ADR-008 + ADR-017 — `[hooks]`-block manifest validator. Validates the `[hooks]` block declarations (`hook_id` / `phase` / `ordering_class` / `timeout_seconds` / `fail_policy`) + cross-checks declared `hook_id`s against pyproject `[project.entry-points."cognic.hooks"]` keys (in-pack consistency; both directions — every declaration has an entry-point and every entry-point has a declaration). Does NOT cross-resolve `[data_governance].dlp_pre_hooks` / `dlp_post_hooks` against installed hook packs — that contract is split: `cli/validators/data_governance.py` validates the calling pack's reference list as snake_case strings only (shape + identifier syntax + intra-list dedupe; `<field>_invalid_shape` / `<field>_invalid_hook_id` / `<field>_duplicate`); cross-pack hook-id resolution is RUNTIME concern owned by the registry's admission gate + `DLPGuard` (`dlp_hook_id_unresolved` is a runtime closed-enum, never a build-time reason). Wave-1 fail-closed-only — every `fail_policy="fail_open"` declaration refused with closed-enum `hook_fail_policy_invalid` / `fail_open_without_exception`. The orchestrator-owned `hook_pack_kind_constraint_violated` (covers `[a2a]` / `[mcp]` block-presence on hook packs; `payload.failure_mode` ∈ {`a2a_block_forbidden`, `mcp_block_forbidden`}) lives in `cli/validate.py`, NOT here, per the one-validator-owns-each-refusal invariant. Promoted to critical-controls at Sprint-7A2 T12 closeout via Doctrine Decision G's "non-trivial allow/deny logic" rule alongside the runtime registry + dispatcher + DLP integration; `sdk/hook.py` stays off-floor per Doctrine E (public-API stability halt-before-commit).)

*Authoring — Bank pack lifecycle (Sprint 7B.1):*
- `packs/lifecycle.py` (per ADR-012 + ADR-006 — pure-functional pack-lifecycle state machine. Owns the closed-enum **14-value** `LifecycleRefusalReason` at `packs/lifecycle.py:175-190` (`lifecycle_transition_invalid_state_pair` / `…_state_unknown` / `…_kind_unknown` / `…_name_unknown` / `…_terminal_state` / `…_kind_state_combination_forbidden` / `…_double_install` / `…_revoke_already_revoked` / `…_uninstall_not_revoked_or_disabled` / `…_withdraw_post_review` / `…_approve_without_review_claim` / `…_disable_not_installed` / `…_allow_list_not_approved` / `…_manifest_digest_changed_during_submit`) — 13 finalised at T2 from the plan-of-record's provisional ±1 count; +1 at Sprint 7B.2 T9 for the locked manifest-digest precondition refusal that fires from inside `packs/storage._precondition` when the submit-path caller's `expected_manifest_digest` kwarg disagrees with the row-locked `manifest_digest` column (race-condition fix per plan §1179-1181; storage-only-emit — NOT emitted by `validate_transition`). `_VALID_TRANSITIONS` legal-pair table at `:210` (11 transitions / 14 legal pairs: 7 single-from + 3 multi-from × 2 + cancel_draft single-from × 1; Sprint 7B.2 T4 extended from 10/13 by adding `cancel_draft` per ADR-012 §59). `validate_transition(*, from_state, to_state, kind, transition)` pure validator at `:421` — four keyword-only args (positional misuse is a bug-class for state machines; the keyword-only contract eliminates it at the call site). `iso_controls_for(transition)` at `:366` returns the canonical ISO 42001 control tuple from the 3-value `_KNOWN_ISO_CONTROL_CODES` set (`A.5.31` / `A.5.32` / `A.6.2.4` at `:264`); helper is the single write source for the chain row's `iso_controls` tag (storage calls it internally; callers cannot inject or override). `LifecycleTransitionRefused(reason)` at `:332` is a thin wrapper carrying ONLY the closed-enum reason — no transition_name field on the exception. Pure-functional: no I/O, no DB access. **Two-tier consumer split** — `packs/storage.py:129-130` is the SOLE DIRECT importer of `iso_controls_for(...)` + `validate_transition(...)`; Sprint-7B.2 portal pack-API handlers (T4 author / T5 review / T6 operator / T7 inspection) consume the `LifecycleTransitionRefused.reason` closed-enum SURFACED by storage's re-raise (typed-exception catch around `store.transition(...)` call sites in each handler body). T3 storage's `transition()` derives the canonical ISO 42001 control tuple via `iso_controls_for(transition)` at `storage.py:761` BEFORE the `DecisionHistoryStore.append_with_precondition` precondition closure is defined (pure-functional helper call, no I/O). **Three in-precondition refusal sources** (Sprint 7B.2 T9 R37 doctrine — see the `transition()` Raises section docstring): (i) `PackNotFound` when the row lookup is empty; (ii) **storage-only-emit `LifecycleTransitionRefused("lifecycle_transition_manifest_digest_changed_during_submit")` raised at `storage.py:806-808`** when the caller's `expected_manifest_digest` kwarg disagrees with the row-locked `packs.manifest_digest` column AFTER the SELECT projection at `storage.py:778-783` returns AND BEFORE the state-machine validator — race-condition fix per plan §1179-1181; `validate_transition` has no access to the persisted digest column and therefore cannot produce this reason; (iii) `LifecycleTransitionRefused(reason)` raised at `storage.py:817` carrying the pure-functional state-machine refusal from `validate_transition(...)` at `storage.py:810-815`. `engine.begin()` at `core/decision_history.py:482` rolls back the transaction on every in-precondition exception path, and the exception propagates up through `append_with_precondition` and `transition()` to the portal handler — neither storage layer catches it. Doctrine Lock C — the closed-enum vocabulary IS the consumer-API wire-protocol contract for `LifecycleTransitionRefused.reason`; chain audit events themselves use the `pack.lifecycle.<target_state>` namespace built at `storage.py:866` (keyed by target state, not by transition name and not by a `.refused` suffix — refusals roll back BEFORE any chain row is written, so there is no `pack.lifecycle.refused` event).)
- `packs/storage.py` (per ADR-012 + ADR-009 — Postgres-backed `PackRecordStore` at `packs/storage.py:385`; the `DecisionHistoryStore.append_with_precondition` consumer that drives every lifecycle transition through the Sprint-2.5 T2 atomic primitive. `transition()` at `:622` first runs the preflight transition-name guard at `:742-743` — `if transition not in _TRANSITION_TO_TARGET_STATE: raise LifecycleTransitionRefused("lifecycle_transition_name_unknown")` — which fail-loud refuses out-of-vocabulary transitions BEFORE any helper invocation or DB work (mirrors the asymmetric-runtime-guard fix at `packs/lifecycle.py:416-417`). On the green path: resolves `target_state = _TRANSITION_TO_TARGET_STATE[transition]` at `:745`, then derives the canonical ISO 42001 control tuple via `iso_controls_for(transition)` at `:761` BEFORE the closure is defined (pure-functional helper call; no I/O); then opens a chain-head `FOR UPDATE` lock (via `append_with_precondition`'s outer envelope). INSIDE the `_precondition` closure at `:763`: `SELECT ... FOR UPDATE` on the `packs` row at `:778-783` → `validate_transition(from_state=…, to_state=…, kind=…, transition=…)` at `:810-815` under the row-locked view → on success `UPDATE packs SET state, last_actor, updated_at` at `:824-832` under the same lock (three columns; no `version_counter` field — atomicity comes from the chain-head + row lock pair, not a counter) → closure returns the captured `(from_state, kind)` tuple. OUTSIDE the closure: `_build_record` at `:835` mints a `DecisionRecord` with `decision_type=f"pack.lifecycle.{target_state}"` at `:866` and the pre-derived `canonical_iso_controls` value (no fresh `iso_controls_for` call from `_build_record`). **Sprint 7B.2 T6 slice-2 (R24 P2 Path B + B2 user-authorized CC-ADJ) added the optional keyword-only `actor_type: str | None = None` kwarg at `:631`; when non-None it is persisted as a top-level `payload["actor_type"]` key conditionally at `:854-855` so existing call sites + every pre-T6 chain row stay byte-shape compatible (additive-only schema; storage stays a thin string passthrough — the `human | service` closed-enum lives at the rbac boundary).** **Sprint 7B.2 T9 Slice 1 added three further optional keyword-only kwargs to `transition()` at `:632-634`** — `payload_conformance: dict[str, Any] | None` (threads the T9 OWASP conformance suite's chain-payload-serialised dict into the chain row's `payload["conformance"]` key at `:861-864` when non-None, via the same `_build_record` shape pattern as T6's `payload["actor_type"]`; omitted kwargs add NO empty keys — backward-compat with every non-submit caller), `expected_manifest_digest: bytes | None` (when provided, feeds the storage-only-emit digest cross-check at `:806-808` from inside `_precondition` AFTER the `SELECT ... FOR UPDATE` projection at `:778-783` returns AND BEFORE `validate_transition` — race-condition fix per plan §1179-1181 closing the TOCTOU window between handler preload + locked precondition; mismatch raises the new 14th-value `LifecycleTransitionRefused("lifecycle_transition_manifest_digest_changed_during_submit")` per the lifecycle.py entry above), and `evidence_attachments: dict[str, Any] | None` (T5 carry-forward landing at T9 Slice 3 — when non-None it is persisted as a top-level `payload["evidence_attachments"]` key conditionally at `:861-864` via the same byte-shape-compatible additive pattern). Storage performs NO vocabulary or shape validation on `payload_conformance` / `evidence_attachments` — both are thin dict passthroughs; the closed-set shape contracts are owned by the route handlers + their tests, NOT storage. The SELECT projection at `:778-783` was extended from `state, kind` to `state, kind, manifest_digest` at T9 Slice 1 so the locked precondition can read the digest column under the same row lock — a wire-protocol-affecting CC change to the chain row's read path. `append_with_precondition` at `:874` commits chain row + state-cache UPDATE + chain-head UPDATE atomically (single transaction, single commit point). Two-class refusal taxonomy (deliberately split — callers dispatch on class to distinguish state-machine refusals from API-contract refusals): `PackNotFound` at `:272` for missing-pack lookups; `PackRecordRefusalReason` at `:291` is a 4-value Literal (`pack_record_save_draft_initial_state_not_draft` + `pack_record_update_non_draft_state` + `pack_record_update_field_not_allowed` + `pack_record_update_field_invalid_shape`; Sprint 7B.2 T4 bumped from 1 to 4 values when `update_draft()` landed) carried by `PackRecordRefused` at `:299-356` — refusal class for `save_draft` API-contract genesis-state guard AND `update_draft` API-contract refusals (non-draft-state target + field-not-allowed allow-list violation + field-invalid-shape per-field validator). Sprint 7B.2 T4 added `update_draft()` at `:449` — in-place edit of `draft`-state packs via a 4-field allow-list with atomic `UPDATE … WHERE state='draft'` precondition; no chain row emitted (mirrors `save_draft()`'s genesis-state pattern at `:402`). Transition-table refusals raise `LifecycleTransitionRefused(reason)` from inside the precondition closure at `:817` so the engine's transactional rollback fires (no chain row, no state-cache mutation, no orphan INSERT). **Sprint 7B.2 T7 CC-ADJ extension (Slice 1 — pure-read; no Doctrine Lock D touch)**: NEW `list_for_tenant(tenant_id: str, *, limit: int = 50, cursor: uuid.UUID | None = None, state: PackState | None = None) -> list[PackRecord]` at `:933` is the AUTHORITATIVE tenant-scoped read seam for the T7 inspection-list endpoint (the only inspection endpoint without a `{pack_id}` path-param, so `RequireTenantOwnership` cannot enforce row-level filtering — the storage WHERE clause `tenant_id == :tenant_id` IS the tenant boundary). REQUIRED positional-or-keyword `tenant_id` BEFORE the `*` separator (NOT optional like `list_by_status`'s tenant kwarg — making it optional would re-open the cross-tenant leak class); optional `state` keyword-only after `*`. Module-private `_build_list_for_tenant_stmt(...)` at `:1073` is the SOLE query-construction path; the Slice-1 SQL-shape regression at `tests/unit/packs/test_storage_list_for_tenant.py::test_list_for_tenant_compiles_with_indexed_where_clause` imports the SAME builder and asserts the compiled SQL contains `packs.tenant_id = ` (always) + `packs.state = ` (when state non-None), proving the production path uses the `ix_packs_tenant_state` composite-index columns (migration L129). Shared-builder pattern eliminates the vacuous-proof bug class where a test-local duplicate `select` could pass while production drifts. Doctrine Lock D — atomic chain-insert + state-cache UPDATE + chain-head UPDATE under a single transaction; integration tests pin row-lock serialisation on live Postgres + Oracle (skipped without `COGNIC_RUN_POSTGRES_INTEGRATION=1` / `COGNIC_RUN_ORACLE_INTEGRATION=1`).)

*Portal RBAC (Sprint 7B.2):*
- `portal/rbac/scopes.py` (per ADR-012 §40 + AGENTS.md "Wire-protocol contracts" — closed-enum 12-value `PackRBACScope` Literal at `portal/rbac/scopes.py:41-54` (`pack.submit` / `pack.withdraw` / `pack.review.claim` / `pack.review.approve` / `pack.review.reject` / `pack.allow_list` / `pack.install` / `pack.disable` / `pack.revoke` / `pack.uninstall` / `pack.audit.read` / `pack.invocation.read`) IS the wire-protocol contract for every 403 RBAC denial in the portal pack API — bank-overlay binders mint `Actor` instances carrying a `frozenset[PackRBACScope]` and drift between this Literal and the overlay's scope-claim mapping is caught at module load by the BUILD_PLAN §622-625 partition-invariant test. Owns 4 role-group frozensets (`AUTHOR_SCOPES` / `REVIEWER_SCOPES` / `OPERATOR_SCOPES` / `EXAMINER_SCOPES`) whose union equals `PACK_LIFECYCLE_SCOPES` — the closed-set partition that pins which role groups can perform which lifecycle actions. Style note: assigned as plain `= Literal[...]` (without `TypeAlias` annotation) to match the Sprint-7B.1 repo convention at `packs/lifecycle.py:111`.)
- `portal/rbac/actor.py` (per ADR-012 §40 + AGENTS.md "Human-only decisions" — frozen `Actor` Pydantic model + closed-enum 2-value `ActorType = Literal["human", "service"]` at `portal/rbac/actor.py:49`. Identity boundary at the portal admission seam: every authenticated request resolves to exactly one `Actor` carrying `subject` / `tenant_id` / `scopes: frozenset[PackRBACScope]` / `actor_type: ActorType`. The `actor_type` field threads through the lifecycle transition path to the chain row's `payload["actor_type"]` per the R24 Path B + B2 user-authorized CC-ADJ. Production-grade fail-loud default: unconfigured actor providers raise `NotImplementedError` pointing at ADR-012 §40 rather than returning a synthetic actor — silent in-process fallbacks that pretend to work are forbidden by the AGENTS.md production-grade rule.)
- `portal/rbac/enforcement.py` (per ADR-012 §40 — `RequireScope(scope)` FastAPI dependency factory + closed-enum 3-value `RBACDenialReason = Literal["actor_unauthenticated", "scope_not_held", "actor_binder_not_configured"]` at `portal/rbac/enforcement.py:53-57`. Covers three failure modes the dependency chain can produce: `actor_unauthenticated` — no `Actor` resolved from the request; `scope_not_held` — `Actor` resolved but the required scope is not in `actor.scopes` (the common case); `actor_binder_not_configured` — the actor-binder dependency itself is missing (defensive 500). The closed-enum vocabulary IS the wire-protocol contract for the 403 response body's `reason` field; drift breaks bank-overlay error-handling consumers + reviewer evidence panels.)
- `portal/rbac/tenant_isolation.py` (per ADR-012 §40 + Round 1 P2 #3 + T2 R1 P2 #1 — `RequireTenantOwnership(pack_id_param)` FastAPI dependency factory + closed-enum 4-value `TenantIsolationFailure = Literal["tenant_id_mismatch", "pack_not_found", "actor_tenant_id_missing", "pack_store_not_configured"]` at `portal/rbac/tenant_isolation.py:67`. Cross-tenant 404 doctrine: a pack belonging to tenant A is INVISIBLE to tenant B — the response shape MUST match the genuine 404 (NOT 403) so a probe cannot enumerate pack-IDs across tenants. The fourth value `pack_store_not_configured` (T2 R1 P2 #1) defends against a configuration regression that would silently bypass the tenant-isolation seam. `actor_tenant_id_missing` covers the bare-list-endpoint preflight that `inspection_routes.py` adds per plan R20 P2 #2 + R21 P2 #1 (no `{pack_id}` path-param → dependency-chain bypass → explicit preflight guard fires the same closed-enum reason with `pack_id="<list>"` sentinel).)
- `portal/rbac/human_actor.py` (per ADR-012 §40 + Round 1 P3 #8 + AGENTS.md "Human-only decisions" — `RequireHumanActor` FastAPI dependency + closed-enum 1-value `HumanActorDenialReason = Literal["actor_type_must_be_human"]` at `portal/rbac/human_actor.py:48`. The single user-authorized site for the AGENTS.md "Per-tenant allow-list changes" Human-only-decisions doctrine — wired as a sub-dependency on the allow-list endpoint at `portal/api/packs/operator_routes.py` so a service-token actor holding `pack.allow_list` scope is refused at the dep chain BEFORE the handler body runs. R19 P2 #2 mutually-exclusive log contract: refusal emits EXACTLY 1 `portal.rbac.human_actor_required` log + ZERO `portal.packs.allow_list_refused`. The wire-protocol-public surface is the 403 response body's `reason` field — drift breaks the bank-overlay `actor_type` claim mapping.)
- `portal/rbac/role_separation.py` (per ADR-012 §17 + Round 11 P2 #4 — `RequireDifferentActorThanCreator` FastAPI dependency factory + closed-enum 1-value `RoleSeparationFailure = Literal["actor_cannot_review_own_pack"]` at `portal/rbac/role_separation.py:93`. ADR-012 §17 cross-role separation enforcement: the actor who created (drafted/submitted) a pack MUST NOT also review it. Wired on the review-surface endpoints at `portal/api/packs/review_routes.py` (claim / approve / reject) so a reviewer holding both `pack.submit` + `pack.review.*` scopes cannot self-review. Closure-factory pattern parameterises the dependency on the `pack_id_param` name to match the FastAPI path-param convention without sharing instance state across route registrations.)

*Authoring — Portal pack API (Sprint 7B.2):*
- `portal/api/packs/author_routes.py` Sprint 7B.2 T9 Slice 2 extension (per ADR-012 §62 + plan §1062-1252 — `submit_draft` handler at `:545+` now accepts `SubmitDraftRequest(manifest: dict[str, Any])` in the request body; (1) **cheap pre-check** computes `sha256(canonical_bytes(body.manifest))` and refuses 400 + closed-enum `manifest_digest_mismatch` on discrepancy with `record.manifest_digest` — short-circuits BEFORE the conformance suite runs or the storage transaction opens; structured log `portal.packs.submit_refused` emits with `reason` + `actor_subject` + `pack_id` + `from_state` (R40 P2 #2 parity with other submit-refusal paths); pinned by `test_submit_refuses_400_on_manifest_digest_mismatch` with caplog hardening (R40 P2 #2); (2) **auto-run OWASP conformance** via `run_owasp_conformance_for_chain_payload(body.manifest)` OUTSIDE the storage closure; the 4-key serialised dict (`overall_status` / `results` / `summary` / `errored_categories`) lands on the chain row's `payload["conformance"]` via the T9-extended storage `payload_conformance` kwarg per `packs/conformance/runner.py`'s wire shape; (3) **non-gating evidence per BUILD_PLAN §627** — a `red` or `yellow` conformance verdict still produces a successful submit; the chain row carries the verdict for 7B.3 reviewer evidence panels + the 5-gate composition (the gate is 7B.3, not 7B.2); (4) **locked manifest-digest precondition** threads `expected_manifest_digest=record.manifest_digest` into `store.transition(...)` to close the TOCTOU window between handler preload + locked SELECT — race surfaces as a 409 carrying the storage-only-emit `lifecycle_transition_manifest_digest_changed_during_submit` per the lifecycle.py entry above. **R40 P2 #1 4-way refusal union extension**: `AuthorRequestRefusalReason = Literal["manifest_digest_mismatch"]` is the NEW route-owned closed-enum vocabulary for 400-status request-body refusals (distinct from `AuthorRefusalReason` 409s + `TenantIsolationFailure` 404/500s + `RBACDenialReason` 403/500s); pinned by 3 dedicated tests (`test_author_request_refusal_reason_has_exactly_one_value` + `test_manifest_digest_mismatch_constant_traces_to_request_refusal_enum` + `test_author_request_refusal_reason_is_disjoint_from_upstream_enums`) + the build-time drift detector at module foot that verifies the route-owned vocab is disjoint from all 4 upstream enums. Centralised `_MANIFEST_DIGEST_MISMATCH_REASON: Final[Literal["manifest_digest_mismatch"]]` mirror of the `_PACK_NOT_FOUND_REASON` pattern. **Reuses the existing T4 `_mint_request_id(_PACK_SUBMIT_REQUEST_ID_PREFIX)` minter per plan §1177 — NO new T9 prefix introduced**; the bounded request-id invariant (`pack-submit-<uuid4.hex>` = 44 chars ≤ 64 cap) is pinned by both the T4 build-time module-foot assert AND the new T9 runtime `test_submit_request_id_bounded_to_64_chars_after_t9`.)
- `portal/api/packs/review_routes.py` Sprint 7B.2 T9 Slice 3 extension (per ADR-012 §63 + plan §1086-1088 — `reject_pack` handler now threads `evidence_attachments={"rejection_reason": body.reason, "reviewer_comments": body.comments}` into `store.transition(...)`. **Dual-surface emission contract** (defence-in-depth): the categorised pair lands on BOTH (a) the `portal.packs.review.reject` structured log (operations surface, T5 carry-forward — load-bearing for live observability tooling) AND (b) the chain row's `payload["evidence_attachments"]` (examiner surface, NEW at T9 — authoritative source for evidence-pack export per ADR-006); a future change that drops one surface fails the dual-surface regression test. Closed-set `{rejection_reason, reviewer_comments}` keyset shape is **route-owned** — storage stays a thin dict passthrough per the user-locked Slice-3 contract (no vocabulary/shape validation in `packs/storage.py`; the route's tests own the closed-set assertion). T9 introduces NO new request-id prefix — the handler reuses the existing T5-owned `_PACK_REJECT_REQUEST_ID_PREFIX` per plan §1177; pinned by `test_reject_request_id_reuses_t5_prefix_no_new_t9_prefix`. **R18 P2 #2 mutually-exclusive emission contract preserved**: green path emits EXACTLY ONE `portal.packs.review.reject` log + ZERO `portal.packs.reject_refused`; state-machine refusal emits EXACTLY ONE `_refused` log + ZERO accepted; sibling-gate (RBAC/tenant) refusal emits ZERO route-level reject logs (sibling-gate logs handle that axis) — caplog-pinned at T9 Slice 3 via 3 carry-forward regression tests. **Historical-compatibility on the evidence-read endpoint** (`GET /{pack_id}/evidence`): pre-T9 / historical submit chain rows have no `conformance` key — endpoint surfaces `None` gracefully (NOT 500); post-T9 submit rows carry the 4-key conformance dict; both shapes coexist via the same `PackEvidenceResponse` schema.)
- `portal/api/packs/operator_routes.py` (per ADR-012 §68-73 + ADR-008 + AGENTS.md "Human-only decisions" — operator-surface route module. Owns the 5 lifecycle-operator endpoints behind `/api/v1/packs`: POST `/{pack_id}/allow-list` + POST `/{pack_id}/install` + POST `/{pack_id}/disable` + POST `/{pack_id}/revoke` + DELETE `/{pack_id}/install` (uninstall verb shares the install path with method=DELETE per the plan endpoint table; pinned by `test_install_path_carries_both_post_and_delete`). The allow-list endpoint is the **single user-authorized site for the AGENTS.md "Human-only decisions / Per-tenant allow-list changes" doctrine** — wired with `RequireHumanActor()` sub-dependency so a service-token actor holding `pack.allow_list` scope is refused at the dep chain (closed-enum `HumanActorDenialReason("actor_type_must_be_human")`) BEFORE the handler body runs; pinned by `test_allow_list_refuses_service_actor_with_human_actor_log_only` with the R19 P2 #2 mutually-exclusive log contract (EXACTLY 1 `portal.rbac.human_actor_required` log + ZERO `portal.packs.allow_list_refused`). All 5 handlers share a delegate-to-storage 3-arm refusal pattern at the body: `try store.transition(...)` → catch `PackNotFound` → 404 `pack_not_found` + `portal.packs.<verb>_refused` log; catch `LifecycleTransitionRefused` → 409 + closed-enum reason + same log; green path returns the re-loaded `PackResponse`. **R24 Path B + B2 carry-forward**: every handler threads `actor_type=actor.actor_type` into `transition()` so the chain row's `payload["actor_type"]` records the actor type (additive-only schema; storage stays a thin string passthrough — the `human | service` closed-enum lives at the rbac boundary). Allow-list additionally emits the green-path `portal.packs.allow_list` structured log carrying `actor_type` as a dual examiner-traceability surface. Multi-from-state pairs: revoke accepts `installed | disabled → revoked` per `packs/lifecycle.py:224-228`; uninstall accepts `disabled | revoked → uninstalled` per `packs/lifecycle.py:230-234`; both legs pinned explicitly. Per-transition closed-enum reasons surface at the body where the lifecycle table defines them (e.g. `lifecycle_transition_revoke_already_revoked` for re-revoke idempotency at `packs/lifecycle.py:183`; `lifecycle_transition_uninstall_not_revoked_or_disabled` for out-of-vocab uninstall from-state at `packs/lifecycle.py:184`; `lifecycle_transition_disable_not_installed` for out-of-vocab disable from-state at `packs/lifecycle.py:187`); per-transition reasons take precedence over the generic `lifecycle_transition_invalid_state_pair` legal-pair fallback per `validate_transition`'s ordered check chain. R27-hardened race contract: every verb's `PackNotFound` race path emits EXACTLY ONE `portal.packs.<verb>_refused` log carrying reason + actor_subject + pack_id + from_state (caplog asserted; threat-model-revert verified load-bearing at slices 2 + 3 + 4). Standing-offer §30 module-header invariant: `from __future__ import annotations` is INTENTIONALLY OMITTED — PEP 563 string-deferred annotations would break FastAPI's `inspect.signature()` / `typing.get_type_hints()` resolution on `Annotated[..., Depends(<closure-local>)]` annotations (the shared dependency instances are local-scope inside `build_operator_routes`, NOT module globals); pinned by AST self-test + per-verb invocation tests per `feedback_security_regression_hardening.md` (threat-model-revert verified at slices 1 + 2 + 4). Per-verb request-id minter prefixes (`_PACK_{ALLOW_LIST,INSTALL,DISABLE,REVOKE,UNINSTALL}_REQUEST_ID_PREFIX`) at module scope; 13 chars each; module-foot build-time `assert` pins the `len(prefix) + 32 (uuid4().hex) <= 64` invariant against the `decision_history.request_id` String(64) column cap. T6 scope note: this commit adds `operator_routes.py` to the durable critical-controls coverage gate because it owns the AGENTS.md "Human-only decisions" enforcement boundary (`RequireHumanActor` sub-dependency on allow-list) + the R24 actor_type chain-payload provenance surface — both wire-protocol-public surfaces. Whether the sibling pack-router modules already classified CC at commit time (`portal/api/packs/author_routes.py` per T4 + `portal/api/packs/review_routes.py` per T5 + `portal/api/packs/router.py` per T3) should ALSO be added to the durable per-file coverage gate is a separate doctrinal decision — this commit deliberately does NOT change their on-gate status either way; the CC tag carried by their respective landing commits stays as-is until explicitly reviewed.)
- `portal/api/packs/inspection_routes.py` (per ADR-012 §75-79 + BUILD_PLAN §619 + AGENTS.md "Wire-protocol contracts" — inspection-surface route module. Owns the 4 examiner-facing inspection endpoints behind `/api/v1/packs`: GET `/` (list scoped to actor.tenant_id; `pack.audit.read`) + GET `/{pack_id}` (detail incl. lifecycle history; `pack.audit.read` + `RequireTenantOwnership`) + GET `/{pack_id}/audit` (hash-chained audit events; same gates) + GET `/{pack_id}/invocations?from&to` (`pack.invocation.read` + `RequireTenantOwnership`; Sprint 7B.2 returns `[]` per plan §1000 deeper-analytics-deferred scope). **NOT-CC at the task level** — pure-read endpoints; no `store.transition()` calls; no chain-row writes; no Human-only-decisions enforcement boundary (operator_routes.py owns that surface). **Halt: YES — CC-ADJ propagation** because Slice 1 extended the critical-controls module `packs/storage.py` with `list_for_tenant` (the authoritative tenant-isolation boundary for `GET /api/v1/packs`); the CC risk is covered by `packs/storage.py` being on the durable coverage gate (no separate gate entry for inspection_routes.py per R32 doctrine — only `operator_routes.py` was gate-promoted because it owns the Human-only-decisions enforcement boundary + the R24 actor_type chain-payload provenance surface; the T7 inspection surface carries neither). **Plan R20 P2 #2 + R21 P2 #1 — list-endpoint route-level `actor_tenant_id_missing` preflight guard**: the bare `GET /api/v1/packs` bypasses `RequireTenantOwnership` (no `{pack_id}` path-param) which means it also bypasses the existing 500 `actor_tenant_id_missing` emission at `portal/rbac/tenant_isolation.py:144-152`. The handler runs an EXPLICIT preflight that calls the existing `_emit_isolation_log` helper with closed-enum `reason="actor_tenant_id_missing"` + `pack_id="<list>"` sentinel (`Actor.tenant_id: str` per `actor.py:71`; the only reachable falsy case under live Pydantic validation is `""`; the sentinel keeps the helper's `pack_id: str` signature type-safe under mypy — no Optional kwarg). Pinned by `test_list_returns_500_when_actor_tenant_id_missing` + `test_list_emits_actor_tenant_id_missing_log_with_pack_id_sentinel`. The other 3 endpoints inherit the existing `RequireTenantOwnership(pack_id_param="pack_id")` semantics for cross-tenant 404 `tenant_id_mismatch` + unknown-UUID 404 `pack_not_found` (mirrors T5/T6 path-param convention). **Shared closure-local dependency**: one `RequireTenantOwnership(pack_id_param="pack_id")` instance reused across the three `{pack_id}` endpoints — FastAPI's per-request sub-dependency cache then dedupes the `store.load` round-trip to ONE call per request (mirrors the T5 `_require_tenant_ownership` pattern at `review_routes.py:133`). **Standing-offer §30 module-header invariant**: `from __future__ import annotations` is INTENTIONALLY OMITTED so FastAPI's `inspect.signature()` / `typing.get_type_hints()` can resolve `Annotated[..., Depends(<closure-local>)]` against the closure-local dependency instances (the three `RequireScope(...)` + the shared `RequireTenantOwnership(...)` are LOCAL variables inside `build_inspection_routes`, NOT module globals); a regression that adds the future-import would make FastAPI silently fall back to treating handler parameters as query params. **Route registration order**: specific `{pack_id}/audit` + `{pack_id}/invocations` sub-paths registered BEFORE the broad `{pack_id}` detail endpoint — defensive ordering documented inline; FastAPI's path-segment-exact matcher does not require this for correctness today, but a future catch-all extension would otherwise silently mis-route. **Route-shape split (R33 P2 doctrine decision)**: the bare list endpoint is registered DIRECTLY on the parent pack-router via `register_inspection_list(parent, *, store)` — path `""` + parent prefix `/api/v1/packs` yields the EXACT slashless compiled path `/api/v1/packs` (plan §997 + ADR-012 §75 wire-protocol contract). The three `{pack_id}` sub-handlers live on a sub-router returned by `build_inspection_routes(*, store)` and included via `router.include_router(...)`. The split is required because FastAPI's `include_router` rejects an empty-prefix include of a sub-router with an empty-path route (`FastAPIError: Prefix and path cannot be both empty`). An earlier draft used `@router.get("/")` on the sub-router producing `/api/v1/packs/` (trailing slash) + relying on FastAPI's `redirect_slashes=True` 307 fallback; R33 P2 rejected that — the slashless form IS the contract, not a 307-redirect target. Pinned by `test_review_queue_and_inspection_list_both_reachable` (plan §1010 T5/T7 collision split) + a negative regression asserting `/api/v1/packs/` (trailing-slash) does NOT appear in compiled paths. **DTO additions**: `PackDetailResponse(pack: PackResponse, history: list[PackLifecycleEventResponse])` + `PackLifecycleEventResponse` (frozen + extra=forbid + from_attributes; projects `DecisionRecord` onto the wire, intentionally omits `sequence` since the live `DecisionRecord` at `core/decision_history.py:240-249` does not carry it — surfacing would be a CC-ADJ extension to the canonical dataclass, deferred). Inspection storage path: `store.load_lifecycle_history(pack_id)` already filters by `payload['pack_id'] == str(pack_id)` at the storage layer (`packs/storage.py:981-1033`), so cross-pack rows cannot reach the handler — pinned by `test_audit_filters_to_pack_id`.)

*Pack conformance evidence (Sprint 7B.2):*
- `packs/conformance/checks.py` (per ADR-012 §119 + BUILD_PLAN §628 + ADR-006 evidence-pack export — wire-protocol-public shared types. Owns the closed-enum 10-value `OWASPCheckCategory` Literal at `:11-22` (`tool_misuse` / `goal_hijacking` / `identity_abuse` / `prompt_injected_skills` / `dependency_poisoning` / `secret_exfiltration` / `unsafe_filesystem` / `unsafe_network` / `supply_chain_integrity` / `skills_top_10`) — IS the wire-protocol contract for reviewer evidence: T9 attaches per-category results to the chain payload's `payload.conformance` extension and 7B.3 reviewers consume the same set; drift breaks evidence-pack export readers. Owns the 3-value `ConformanceCheckStatus = Literal["pass", "fail", "not_applicable"]` (per-check enum — **never `yellow`**) AND the 3-value `ConformanceOverallStatus = Literal["green", "red", "yellow"]` (composite-report enum). Owns the frozen `ConformanceCheckResult(category, status, findings: list[str])` + the frozen `ConformanceReport(overall_status, results, summary, errored_categories: tuple[OWASPCheckCategory, ...] = ())` — 4-field order is wire-protocol-public per ADR-006; drift breaks examiners reading positional / keyword-by-name.)
- `packs/conformance/owasp_agentic.py` (per ADR-012 §119 + BUILD_PLAN §628 — 10 deterministic manifest-shape check bodies + the `_APPLICABILITY` 10x4 per-pack-kind matrix + the `_CHECK_REGISTRY` ordered tuple (1:1 with `OWASPCheckCategory` Literal order) + the `run_owasp_conformance` dispatcher. **Manifest-shape only** — no filesystem reads, no network calls, no dependency downloads, no digest recomputation; the CLI validators at `cli/validators/identity.py` / `supply_chain.py` / `data_governance.py` own the file-system-touching checks at build/admission time and the conformance runtime duplicates only the small manifest-shape subset inline (no `conformance → cli` runtime dependency). Each check returns one of `pass` / `fail` / `not_applicable` and fail findings carry stable `manifest.<path>: <reason>` field-path prefixes. **`_APPLICABILITY` is examiner-readable from the static table** — T9 chain-payload consumers + 7B.3 reviewers can predict which checks will be N/A for a given pack kind without running the suite: all 4 kinds for `tool_misuse` / `identity_abuse` / `dependency_poisoning` / `secret_exfiltration` / `unsafe_network` / `supply_chain_integrity`; `agent` + `skill` only for `goal_hijacking`; `skill` only for `prompt_injected_skills` + `skills_top_10`; `tool` + `skill` + `agent` for `unsafe_filesystem` (hook N/A per ADR-004 — hooks have no FS surface). Runner consults `_APPLICABILITY` BEFORE invoking a body — on a known kind not in the applicability set it synthesises a `not_applicable` result with a `manifest.pack.kind:` field-path prefix WITHOUT calling the body (pinned by `test_runner_does_not_invoke_skills_top_10_body_on_tool_pack` via registry monkeypatch). Bodies are wrapped in `try / except Exception` so a checker raising synthesises a `not_applicable` result with the user-locked exact format `"manifest: <category> checker raised <ExcType>: <message>"` AND appends the category to `ConformanceReport.errored_categories`; the runner preserves `_CHECK_REGISTRY` order in both `results` key-iteration AND `errored_categories` tuple. **Overall-status precedence: yellow > red > green** — yellow takes precedence over red because a checker exception means the suite is incomplete and the red/green verdict is not trustworthy. Cross-set drift guard at `test_owasp_applicability.py::TestCategorySetCohesion` pins `OWASPCheckCategory` / `_CHECK_REGISTRY` / `_APPLICABILITY` all carry the same 10-element category set in registry-iteration order — drift in any one of the three is the most-likely future regression class. **Sprint 7B.2 R45 CC-ADJ — risk-tier vocabulary alignment**: T8 originally seeded `_VALID_RISK_TIERS = frozenset({"low", "medium", "high"})` which diverged from ADR-014's canonical 8-value `RiskTier` Literal at `cli/_governance_vocab.py:117` (`read_only` / `internal_write` / `customer_data_read` / `customer_data_write` / `payment_action` / `regulator_communication` / `cross_tenant` / `high_risk_custom`). The divergence was tolerable doctrine drift pre-T11 but became a functional bug once T11 wired the matrix as a post-validate tail-call in `agentos test-harness` (every validator-clean pack tripped `check_tool_misuse` on the disjoint vocabulary). R45 inlined the canonical 8-value set at `packs/conformance/owasp_agentic.py:115-140`; the production module stays self-contained because the architectural arrow runs `cli → packs` (reversing it would create a circular dependency through `cli/validators/risk_tier.py` which already consumes the matrix's wire types). Drift detector at `tests/unit/packs/conformance/test_owasp_risk_tier_vocab_drift.py` enforces lockstep at test time via three regressions: (1) `test_owasp_valid_risk_tiers_matches_canonical_adr014_set` imports both `_VALID_RISK_TIERS` AND `RiskTier` + asserts `frozenset(typing.get_args(RiskTier)) == _VALID_RISK_TIERS`; (2) `test_canonical_set_has_exactly_eight_values` count guard pinned independently for crisp drift-diagnosis; (3) `test_production_conformance_module_does_not_import_cli` AST-scans `owasp_agentic.py` source to assert NO `cognic_agentos.cli` imports — pins the architectural-arrow contract as a production-code regression.)
- `packs/conformance/runner.py` (per ADR-012 §119 + ADR-006 evidence-pack export — chain-payload serialization adapter, Sprint 7B.2 T9 Slice 2. Single public seam `run_owasp_conformance_for_chain_payload(manifest: dict[str, Any]) -> dict[str, Any]` is the WIRE-SHAPE boundary between the T8 `run_owasp_conformance` check matrix and the T9 chain row's `payload["conformance"]` key. Delegates to the T8 dispatcher for the check matrix, then converts the returned `ConformanceReport` via `dataclasses.asdict` AND explicitly converts `errored_categories` from tuple to list. The tuple→list conversion is **load-bearing**: Python's `dataclasses.asdict` PRESERVES tuple-typed fields, but the Sprint-2 canonical-form at `core/canonical.canonical_bytes` REJECTS tuples in chain payloads to prevent the silent list/tuple ambiguity bug class; without the explicit conversion every T9 submit chain insert would fail with a TypeError on the canonical-form gate. The 4-key top-level wire shape — `overall_status` / `results` / `summary` / `errored_categories` — is wire-protocol-public per ADR-006; T9 chain payload consumers + 7B.3 reviewer evidence panels read this exact dict shape. Promoted to durable critical-controls gate at Sprint-7B.2 T9 Slice 4 (gate 46 → 47) because it owns the chain-payload serialization contract — NOT pure re-export glue. Pinned by `tests/unit/packs/conformance/test_runner.py`'s 9-test wire-shape suite.)

*Conformance CLI surfaces (Sprint 7B.2, off-floor):* The following two CLI modules surface the OWASP matrix to pack authors + CI tooling but stay OFF the durable critical-controls coverage floor — they are authoring/dev-only public commands per the Sprint-7A T13 R4 P3 #5 doctrine ("public command, NOT test-only path, off-floor because every gate it surfaces is enforced upstream by the CC matrix at `packs/conformance/owasp_agentic.py`"). Both delegate to the on-floor matrix for the actual decision logic; the CLI modules carry only manifest-parse + dispatch + render + exit-code translation glue.

- `cli/conformance.py` (Sprint 7B.2 T10 — NOT-CC per plan §1255-1273. Thin `agentos conformance <pack_path> [--json]` wrapper. Pure-function seam `run_conformance(pack_path) -> ConformanceReport | ConformanceInvocationFailure` is side-effect-free + never raises (R44 P2 #1 extended the `except` to catch `OSError` subclasses too so `PermissionError` / race-induced `FileNotFoundError` / etc collapse into the existing `conformance_manifest_unparseable` invocation failure). Closed-enum 3-value `ConformanceInvocationError = Literal["conformance_pack_path_not_found", "conformance_manifest_not_found", "conformance_manifest_unparseable"]` distinct from the verdict vocabulary (`ConformanceOverallStatus`); drift detector pins the value count. Exit codes: `0` green, `1` red OR yellow (yellow's incompleteness signal surfaces with the same non-zero exit as red because the verdict is not trustworthy when a checker raised), `2` invocation error.)
- `cli/test_harness.py` Sprint 7B.2 T11 extension (CC-ADJ to off-floor module per plan §1276 + Sprint-7A T13 R4 P3 #5 — the wrapped runner already sits off the critical-controls floor because every gate it surfaces is enforced upstream by `cli/validate.run_validators`. T11 adds an OWASP conformance tail-call AFTER the existing per-kind dispatch loop completes green: new `ConformanceSummary(green: bool, overall_status: Literal["green", "red", "yellow"], findings: list[str])` frozen dataclass + new `HarnessReport.conformance: ConformanceSummary | None = None` field (backward-compatible additive). `_build_conformance_summary(pack_path)` projects the matrix verdict into the summary; the projection iterates BOTH red-path `status == "fail"` results AND `report.errored_categories` (R45 P2 #1 fix — pre-fix a yellow verdict produced an empty findings list + zero stderr warnings, losing the incompleteness signal). **NON-GATING evidence per BUILD_PLAN §627** (mirrors T9's submit-flow design): a non-green verdict does NOT flip `HarnessReport.overall_status` to `"fail"`; it surfaces as data in JSON + text summary + `::warning::` stderr annotations. The Sprint-7B.3 5-gate composer owns the gating decision. **Deferred-full-harness boundary per plan §1280 + §1286**: AST-scan regressions in `tests/unit/cli/test_test_harness_owasp_integration.py::TestSprint7B2T11DeferredFullHarnessBoundary` pin that `cli/test_harness.py` does NOT import `cognic_agentos.core.audit` / `core.decision_history` / `core.guardrails` / `sandbox` / `subagent` — the modules the ADR-012 §114-122 fixture-AgentOS instance harness would require, deferred post-7B. Six AST scans (5 negative + 1 positive on `packs.conformance`) force any future expansion to be a deliberate doctrine-track decision.)

*Authoring — Reviewer evidence + 5-gate composer (Sprint 7B.3):*

- `packs/evidence/data_governance.py` (per ADR-017 — pure-functional data-governance evidence-panel projector. `project_data_governance_panel(*, manifest, record_kind, tenant_policy=None)` at `packs/evidence/data_governance.py:300` reads a pack's persisted `[data_governance]` manifest block + optionally a deploying tenant's `TenantDataGovernancePolicy` (TypedDict at `:132`) and returns the frozen `DataGovernancePanelData` (at `:172-173`). Owns the wire-protocol-public closed-enum 7-value `DataGovernanceDiffFlag` Literal at `:81-88` (`data_class_not_in_tenant_allowlist` / `purpose_not_declared` / `retention_exceeds_tenant_max` / `egress_endpoint_not_in_tenant_allowlist` / `dlp_pre_hook_missing` / `dlp_post_hook_missing` / `none`) — the `tenant_policy_diff` tuple vocabulary; `()` means "no tenant policy wired", `("none",)` means "policy wired + no drift". No I/O — the route handler in `evidence_routes.py` fetches the manifest via `store.load_lifecycle_history` + `find_latest_submit_row` + `payload["manifest"]`. On the durable coverage gate from T3.)
- `packs/evidence/risk_tier.py` (per ADR-014 §24-37 — pure-functional risk-tier evidence-panel projector. `project_risk_tier_panel(*, manifest, record_kind)` at `packs/evidence/risk_tier.py:222` projects the `[risk_tier]` block onto the frozen `RiskTierPanelData` (at `:181-182`). Owns the wire-protocol-public closed-enum 7-value `ApprovalFlowKind` Literal at `:70-78` (`auto_run` / `audit_emphasis` / `single_approval` / `four_eyes` / `four_eyes_categorised` / `operator_legal_signoff` / `pack_declared`) + the 1:1 `_RISK_TIER_TO_APPROVAL_FLOW` mapping table at `:132` keyed by the ADR-014 canonical 8-value `RiskTier`. Drift in either the Literal or the mapping is wire-protocol regression. On the durable coverage gate from T4.)
- `packs/evidence/supply_chain.py` (per ADR-016 §23-33 + §70-72 — pure-functional supply-chain evidence-panel projector. `project_supply_chain_panel(*, manifest, record_kind, submit_created_at)` at `packs/evidence/supply_chain.py:294` projects the `[supply_chain]` block onto the frozen `SupplyChainPanelData` (at `:178-179`), deriving the 7-year sigstore-bundle retention floor from `submit_created_at`. Owns the wire-protocol-public closed-enum 7-value `AttestationKind` Literal at `:111-119` (`cosign` / `slsa` / `sbom` / `vuln_scan_baseline` / `license_audit` / `sigstore_bundle` / `in_toto`) — also the keyset for the 5-gate composer's Gate 1 evidence lookup. `_CANONICAL_ATTESTATION_BASENAMES` at `:160` maps each canonical attestation filename to the panel field that surfaces its declared path. On the durable coverage gate from T5.)
- `packs/evidence/conformance_matrix.py` (per ADR-002 + ADR-003 + AGNTCY/OASF Wave-2 posture — pure-functional protocol-conformance evidence-panel projector. `project_conformance_matrix_panel(*, manifest, record_kind, conformance_payload, matrix=None)` at `packs/evidence/conformance_matrix.py:741` compares a pack's manifest-declared MCP / A2A / OASF features against the static-shipped `_CONFORMANCE_MATRIX` (loaded once at import from `conformance_matrix.json` at `:251`) AND defensively reconstructs the persisted `payload["conformance"]` OWASP verdict into the panel-local `OwaspVerdictData` (frozen, at `:397`; reconstruction at `_project_owasp_verdict` `:689`). Owns the wire-protocol-public closed-enum 6-value `MatrixComparisonFlag` Literal at `:148-155` (`mcp_capability_restricted` / `mcp_capability_unknown` / `a2a_feature_forbidden` / `a2a_wave2_feature_declared` / `a2a_feature_unknown` / `oasf_capability_wave2_declared`) + the R9 kind-applicability sets `_MCP_APPLICABLE_KINDS` / `_OASF_APPLICABLE_KINDS` at `:316-318`. The static JSON is generated build-time by the off-floor `tools/generate_conformance_matrix_json.py`; runtime never parses Markdown. On the durable coverage gate from T6.)
- `packs/approval_gates.py` (per ADR-012 §41 + §107-110 — the substantive enforcement boundary for the `under_review → approved` lifecycle transition. The pure-functional `compose_approval_gates(*, signature_input, evaluation_input, adversarial_input, owasp_input, pack_kind)` at `packs/approval_gates.py:412` decides whether a pack clears the 5 orthogonal ADR-012 §41 gates (`signature` / `evaluation` / `adversarial` / `owasp_conformance` / `reviewer_acknowledgement` — the `ApprovalGateName` Literal at `:112-118`, canonical order mirrored by `_GATE_ORDER` at `:237`). Owns 10 wire-protocol-public closed-enum Literals: `ApprovalGateOutcome` (`green` / `red` / `evidence_not_attached`, at `:123`); the binary `SignatureGateOutcome` (`green` / `red` only — makes the illegal `evidence_not_attached` signature state unrepresentable per ADR-012 §110); the 5 per-gate red-reason vocabularies (`SignatureRedReason` 13-value at `:159`, `EvaluationRedReason` 2-value at `:184`, `AdversarialRedReason` 3-value at `:191`, `OwaspRedReason` 3-value at `:199` — incl. `owasp_yellow_blocks_approval` per R10 LOCK Flag #2, `ReviewerAckRedReason` 1-value at `:215`); the consolidated 22-value `ApprovalGateRedReason` union (the 412 `ApproveRefusalResponse` refusal vocabulary); and the T8 override path's `OverrideRefusalReason` 4-value Literal at `:510` (`composition_already_all_green` / `override_scope_not_held` / `override_reason_missing` / `non_overridable_red_gate`). `_NON_OVERRIDABLE_GATES = frozenset({"signature"})` at `:249` is the ADR-012 §110 + R10 LOCK Flag #4 policy constant. The T8 override seam — `evaluate_override_decision(*, composition, override_scope_held, override_reason)` at `:551` returning the frozen `OverrideDecision` (at `:536-537`) + the canonical-safe `composition_snapshot(composition)` serialiser at `:602` for the `pack.approval_override` chain-event payload. Pure-functional (no I/O); the route handler at `review_routes.py` (T9) owns the wiring + pre-computes gates 1-4. On the durable coverage gate from T7.)
- `portal/api/packs/evidence_routes.py` (per ADR-012 §75-79 + ADR-017/014/016/002/003 — route module for the 4 GET reviewer evidence-panel endpoints. `build_evidence_routes(*, store: PackRecordStore) -> APIRouter` at `portal/api/packs/evidence_routes.py:164` mirrors the `build_review_routes` / `build_operator_routes` / `build_inspection_routes` factory pattern; the 4 handlers (`data_governance_panel` `:195`, `risk_tier_panel` `:305`, `supply_chain_panel` `:425`, `conformance_panel` `:556`) each fetch the persisted manifest via `find_latest_submit_row`, delegate the projection to the matching `packs/evidence/*` projector, and emit one `pack.evidence_read.<panel>` audit chain row per successful 200 read via the on-gate `packs/storage.py:append_evidence_read_event` seam (T10 — emitted ONLY after BOTH the projector AND the response-DTO `model_validate` succeed; 4xx/5xx paths emit zero audit events). Owns the route-owned closed-enum 3-value `EvidencePanelRefusalReason` Literal at `:135` (`pack_not_yet_submitted` / `manifest_evidence_not_persisted` / `pack_kind_mismatch`) for the 409 refusal contract. **STAYS OFF the durable coverage gate** (plan Round-19 user decision, 2026-05-15) — same R32-doctrine carve-out as `inspection_routes.py`: the module owns no Human-only-decisions enforcement boundary and no actor_type chain-payload provenance surface; the T10 audit-emission CC risk is covered upstream by `packs/storage.py` being on the gate. `from __future__ import annotations` is INTENTIONALLY OMITTED per the standing FastAPI-`inspect.signature()` invariant.)
- `packs/_lifecycle_helpers.py` (per ADR-012 + plan R1 P2 #1 — module-private pure-functional helper. `find_latest_submit_row(history)` at `packs/_lifecycle_helpers.py:49` walks a pack's `load_lifecycle_history` output newest-first and returns the most recent `pack.lifecycle.submitted` chain row (or `None`). It is the AUTHORITATIVE manifest-evidence-source seam for the T3-T6 panel projectors + the T9 approve handler — the persisted manifest lives at `payload["manifest"]` on that row. `_SUBMIT_DECISION_TYPE` at `:34` mirrors `packs/storage.py:866`'s submit-transition f-string output; a drift detector pins the equality. NOT on the durable gate — module-private implementation detail, no closed-enum vocabulary; the CC risk is covered by the on-gate projectors + `storage.py`.)
- Cross-cutting 7B.3 extensions to already-documented modules: `portal/rbac/scopes.py` gained the 13th `PackRBACScope` value `pack.override.approval_gate` (the T8 override RBAC scope; already on the durable gate from 7B.2 T12). `packs/storage.py` (on the durable gate) gained the `append_override_event` seam (T8 — `pack.approval_override` chain event, ISO `A.6.2.4`), the `append_evidence_read_event` seam (T10 — `pack.evidence_read.<panel>` chain event, ISO `A.5.31`), and 4 optional keyword-only `transition()` kwargs (`reviewer_acknowledgement` / `payload_manifest` / `override_event_id` / `signed_artefact_root`) threading 7B.3 evidence onto the approve chain row.

*Authoring — UI event-stream (Sprint 7B.4):*

- `protocol/ui_events.py` Sprint 7B.4 T10 extension (per ADR-020 — already on the durable gate from Sprint 6 as part of the UI event-stream contract stop rule. T10 added the frozen `_DHReplaySnapshot` dataclass at `protocol/ui_events.py:990` — replay-side snapshot shape carrying exactly the 9 fields the per-type projectors read from a chain row (`sequence` / `decision_type` / `tenant_id` / `trace_id` / `request_id` / `payload` / `new_hash` / `chain_id` / `created_at`); module-private, NOT in `__all__`. Constructed by `_replay_from_decision_history` in `stream_routes.py` from raw `_decision_history` SQLAlchemy rows; consumed by `_project_typed_decision_history` at `:963` + `_build_decision_audit_for_dh_snapshot` via `cast(AppendedDecisionSnapshot, snapshot)` so the live + replay paths stay byte-identical. Drift between the dataclass fields + the projector access surface is pinned by 4 regressions at `tests/unit/protocol/test_ui_events_dh_replay_snapshot.py::TestDHReplaySnapshotShapeMatchesAppendedDecisionSnapshot` + 26 parametrized runtime calls at `TestDHReplaySnapshotDrivesEveryDispatchedProjector` (AST-walks the actual projector source + drives every entry in `_DECISION_HISTORY_TYPED_PROJECTORS` at `:953` plus every `rbac.<suffix>` route via `_RBAC_DENIAL_TYPE_VALUES` at `:221`). The existing Sprint-6 surface — `UIEventBroker` at `:1158`, `AppendResult` at `:414`, `ChainCursor` at `:333`, `_chain_derived_event_id` at `:347`, `_decode_chain_cursor` at `:378`, `CursorMalformed` at `:317`, `CursorChainUnsupported` at `:323`, `TenantConnectionCapExceeded` at `:1117`, `RBACDenialType` 9-value Literal at `:189`, `PolicyRBACDenied` at `:690`, `_PENDING_TYPED_EVENT_ID` ContextVar at `:1112`, `_WAVE_1_FAMILIES` 11-value Final at `:120`, `_SSE_WAVE_1_STREAMED_FAMILIES` 9-value Final at `:157` — is unchanged.)

- `portal/api/ui/elicitation_gate.py` (per ADR-020 §69-77 — substantive policy boundary for the submit_elicitation surface. Pure-async `evaluate_elicitation_submission(*, request, actor, adapter, rego_engine) -> GateOutcome` at `portal/api/ui/elicitation_gate.py:152` implements the 5-step refusal contract (Step 1 adapter wired → Step 2 ctx lookup → Step 3 mode parity BOTH directions → Step 4 form-mode + restricted-data-class intersection → Step 5 Rego eval at `data.cognic.ui.elicitation_submit.allow` with fail-closed mapping for OPA-unavailable / bundle-error / decision-false). Owns the 10-value `ActionRejectionReason` Literal at `:109` (6 gate-emitted + 4 handler-emitted reasons) AND the 3-value `_RESTRICTED_DATA_CLASSES` frozenset at `:88` (`customer_pii` / `payment_action` / `regulator_communication`). The Literal is lockstep with `portal/api/ui/dto.py`'s parallel `ActionRejectionReason` at `dto.py:95` via the test-only drift detector at `tests/unit/portal/api/ui/test_dto_action.py::TestActionRejectionReasonCrossModuleEquality` per the user-locked `feedback_drift_detector_test_only_no_runtime_import` doctrine — NO runtime cross-module import. The restricted-data-class set is a deliberate inline mirror of `protocol/mcp_capabilities._RESTRICTED_DATA_CLASSES`; three-way lockstep (gate ↔ Rego bundle's `restricted_classes` set ↔ mcp_capabilities canonical) pinned by `tests/unit/portal/api/ui/test_elicitation_gate.py::TestRestrictedClassesThreeWayLockstep` at every CI run. `GateOutcome` frozen dataclass at `:137` carries `(allowed, reason, ctx)` so the T11 action handler can thread `originating_decision_record_id` onto the rejection chain row's payload for examiner traceability. HTTP mapping (HTTPException raising) is in `action_routes.py` at the call site, NOT here — the gate stays pure-functional. On the durable coverage gate from T13.)

- `portal/api/ui/action_routes.py` (per ADR-020 §22 — wire-protocol-public POST `/api/v1/ui/actions`. `RequireUIAction(broker: UIEventBroker)` at `portal/api/ui/action_routes.py:110` is the FastAPI dep closure-factory that parses body via `Annotated[ActionRequest, Body(...)]` (Pydantic v2 discriminated union over 6 `action_class` values) + binds actor via `Depends(_bind_actor)` + maps `body.action_class` → `ui.action.<class>` required scope + on scope miss delegates to `_emit_denial_or_500` (dual-surface log + broker `emit_rbac_denial` + fail-closed 500 `rbac_denial_emit_failed` on emit failure) + raises 403 with `{"reason": "scope_not_held", "required_scope": <scope>}` + returns `UIActionContext(body, actor, request_id)`. **P1 #2 SCOPE LOCK**: `RequireUIAction` lives HERE, NOT in `dto.py` (the dep is FastAPI + broker + RBAC coupled; placing it in the type-only DTO module would break the NOT-CC classification on `dto.py`). `build_action_routes(*, broker, elicitation_adapter, rego_engine)` at `:165` is the closure factory that returns the POST `/actions` `APIRouter`; mounted under `/api/v1/ui` by T12's `build_ui_routes`. Pipeline: dep → `broker.append_frontend_action_submitted` (ALWAYS — audit completeness; pinned BEFORE dispatch so the resolution row's `submitted_event_id` cursor references the row's deterministic event_id) → per-class dispatch. `_STUB_REASONS` at `:90` is the 5-class deferred-stub map (approve/deny → `action_backend_deferred_to_sprint_13_5`; cancel_run/interrupt → `action_backend_deferred_no_run_primitive`; resume → `action_backend_deferred_sandbox_unwired`) — wire-protocol-public. `submit_elicitation` path routes through `_dispatch_submit_elicitation` → `evaluate_elicitation_submission` (T8 gate); green-path calls `adapter.handle_submission` and emits `frontend_action.accepted`; gate refusal OR backend exception emits `frontend_action.rejected` with the closed-enum reason (gate refusals carry the gate's reason; backend exceptions translate to `elicitation_backend_failed` so examiners can tell "backend rejected" apart from "gate refused" in audit logs without re-running the gate). `_payload_digest` at `:99` computes `sha256(canonical_bytes(body.model_dump(mode="json")))` — sensitive fields enter the hash but NEVER appear plaintext in the chain row. `_build_response` at `:355` is typed with `outcome: ActionOutcome` Literal (NOT `str` + type-ignore — per R3 P2 fix). **T7 forward watchpoint honored**: `ElicitationAdapter` is `@runtime_checkable` but isinstance only checks method-presence (not signature/asyncness); the action handler does NOT isinstance-check; authoritative shape check is the `await adapter.handle_submission(...)` call site (TypeError/AttributeError translate to `elicitation_backend_failed`). On the durable coverage gate from T13.)

- `portal/api/ui/stream_routes.py` (per ADR-020 §60-63 — reconnect-safe SSE transport. 3 SSE GET endpoints registered by `build_stream_routes(*, broker, settings, decision_history_store)` at `portal/api/ui/stream_routes.py:131` (closure-capture pattern — `create_app` populates `app.state.decision_history_store` but NOT `app.state.settings`; closure-capture avoids a CC-ADJ to `portal/api/app.py`): `GET /runs/{run_id}/events` gated by `ui.run_stream`; `GET /tenants/{tenant_id}/events` gated by `ui.tenant_stream` (cross-tenant returns 404 `pack_not_found` per the cross-tenant-invisible doctrine); `GET /events/since/{event_id}` gated by `ui.tenant_stream` (replays from path-param cursor). Owns the 4-value `CursorRefusalReason` Literal at `:103` (`cursor_malformed` / `cursor_chain_unsupported` / `cursor_not_found` / `cursor_projection_drift_detected`); `cursor_tenant_mismatch` deliberately NOT in the enum per the cross-tenant-invisible doctrine (cross-tenant cursors emit `pack_not_found` so a probe cannot enumerate tenant boundaries by response shape — the omission is intentional + documented inline at `:91-101`). Last-Event-ID header WINS over URL cursor; malformed header fails closed with 422 `cursor_malformed` (no silent fall-back). Cursor validation in `_validate_cursor_tenant` runs PRE-STREAM (NOT in the body generator) so the 500 actually reaches the client — type_hash drift detection lives here (raising HTTPException after `http.response.start` would leave the client with a broken stream). `_sse_generator` at `:407` is the replay-then-live coroutine generator; closure-captured `broker` + `settings` + `decision_history_store`; broker/generator-owned heartbeat via `: keepalive` comments at `settings.ui_event_stream_heartbeat_interval_s` (sse-starlette internal ping set to `_SSE_STARLETTE_PING_SENTINEL_S = 86400` at `:128` so the broker stays authoritative). `_replay_from_decision_history` at `:470` reads the exported `_decision_history` SQLAlchemy Table directly via `store._engine`; constructs `_DHReplaySnapshot` per row + casts to `AppendedDecisionSnapshot` for the projector calls. Boundary-row dedup, type_hash drift detection, send_timeout half-open client cleanup, per-tenant `tenant_connection_cap_exceeded` 429 — all pinned by T10 + R2 tests. On the durable coverage gate from T13.)

- Cross-cutting 7B.4 extensions to already-documented modules: `portal/rbac/scopes.py` gained 8 UI scopes (2 stream `ui.run_stream` / `ui.tenant_stream` + 6 action `ui.action.{approve,deny,cancel_run,interrupt,resume,submit_elicitation}`) — `UIRBACScope` Literal at `portal/rbac/scopes.py:90`. `portal/rbac/enforcement.py` already on the gate gained the `_emit_denial_or_500` dual-surface helper at `:74` + the async `_bind_actor` wrapper at `:163` (T6 — fail-closed 500 `rbac_denial_emit_failed` on broker exception). `portal/api/app.py` (CC-ADJ at T6 + T12) gained 3 optional `create_app` kwargs at T12 (`broker` injection seam + `elicitation_adapter` + `rego_engine`) + auto-mounts the composed UI router via `build_ui_routes` + registers `.well-known/cognic-ui-events.json` at root when the broker is wired. 4 off-gate modules ship the surface scaffolding: `portal/api/ui/dto.py` (T9 — Pydantic v2 type-only DTOs + closed-enum Literals), `portal/api/ui/router.py` (T12 — composition factory), `portal/api/ui/well_known_routes.py` (T12 — schema publication; snapshot-pinned at `tests/unit/portal/api/ui/well_known_schema_snapshot.json`), `protocol/elicitation_adapter.py` (T7-foundation — narrow `@runtime_checkable` Protocol + `ElicitationContext` / `ElicitationResult` / `ElicitationBackendError`).

*Sandbox primitive (Sprint 8A):*

The entire `sandbox/` tree remains a stop-rule isolation boundary (see `*Isolation boundaries:*` below). The 7 modules listed here are the substantive admission + lifecycle + egress + warm-pool enforcement surfaces promoted to the durable per-file coverage gate at Sprint 8A T12 per spec §17. All ride the standard 95% line / 90% branch floor.

- `sandbox/protocol.py` (per ADR-004 + Sprint-8A spec §5 — `SandboxBackend` + `SandboxSession` Protocols + `PackAdmissionContext` + `SandboxPolicy` + `SandboxBackendHealth` + the wire-protocol-public closed-enum vocabularies. Owns `SandboxRefusalReason` (15-value Stage-1 + Stage-2 refusal taxonomy per spec §4.1), `SandboxPolicyViolationReason` (6-value at T10c R1 P1.2 — wire-public per ADR-006 amendment; chain row's `sandbox.policy.violated.payload.reason`), and `SandboxLifecycleEvent` (8-value top-level audit-event family discriminator per ADR-006 A.6.2.5). Backend-conformance contract: every backend (DockerSibling Wave-1, KubernetesPod Sprint 8B, gVisor/Firecracker Wave-2) MUST structurally conform; drift in any Protocol method or closed-enum is wire-protocol-public regression that breaks every downstream backend + caller. Pure-Protocol module — no I/O, no decision logic; the substantive enforcement lives in admission.py + policy.py + the backends.)
- `sandbox/policy.py` (per ADR-004 + Sprint-8A spec §6 — `SandboxPolicy` frozen dataclass (`@dataclass(frozen=True)` at `policy.py:133`, NOT a Pydantic model) + the pure synchronous `validate_policy_shape()` (Stage-1 admission glue). Owns `_validate_egress_host` (RFC 1123 hostname guard + HTTP/HTTPS-only scheme guard) feeding `sandbox_policy_egress_host_invalid` (`policy.py:277,282`) + `sandbox_policy_egress_protocol_not_http` (`policy.py:267`) refusals — both values live on the wire-public `SandboxRefusalReason` Literal at `protocol.py:45-46`. Stage-1 is the cheap shape gate that runs BEFORE async Stage-2 admission; a bug here lets malformed policies reach the catalog + cosign + Rego layers. The Rego bundle's `_egress_http_only` rule is PURE-Rego defence-in-depth per the T11 R2/R3 contract — if Stage-1 is ever bypassed (refactor, direct OPA eval, fresh admission path), the bundle still refuses.)
- `sandbox/admission.py` (per ADR-004 + Sprint-8A spec §6.1 — async `admit_policy()`; Stage-2 admission pipeline (catalog membership + cosign verification + SBOM verification + Rego bundle eval at `data.cognic.sandbox.admit.allow` + credential-adapter precondition + high-risk-tier gate). The substantive trust-gate-equivalent decision point shared across all backends. Declares `CatalogProtocol` + `CredentialAdapter` Protocols + `KernelDefaultCredentialAdapter` sentinel inline per the `feedback_consumer_owned_protocol_for_unlanded_dep` resolution (T5 commit `4967ce8` — `sandbox/credentials.py` is a thin re-export shim; `sandbox/catalog.py` structurally conforms to `CatalogProtocol`). `KernelDefaultCredentialAdapter` raises `NotImplementedError` pointing at the landed Sprint-10 `VaultCredentialAdapter` (`sandbox/credentials.py`, on the gate as of Sprint 10 Z1 / 2026-05-24) per the production-grade rule. Step 6 (T11 R1 fix) captures `runtime_image_in_canonical_set` + `runtime_image_in_tenant_allow_list` bools as locals BEFORE Step 9 threads them into the Rego input dict; pinned by 2 regression tests at `tests/unit/sandbox/test_admission_pipeline.py::test_admit_policy_threads_runtime_image_bools_into_rego_input` + `…test_admit_policy_threads_tenant_allow_list_bool_when_only_path_is_overlay` so a future refactor can't disconnect the Stage-2 catalog check from the Rego rule-4 decision point.)
- `sandbox/catalog.py` (per ADR-004 + ADR-016 amendment + Sprint-8A spec §9 — `CanonicalImageCatalog` + cosign subprocess verification + real syft SBOM verification + per-tenant allow-list. Spec round-1 P1 promotion (NOT plan-default); the reviewer rationale at spec line 923 was "a bug here lets untrusted images run; not 'thin wiring'". Mirrors the `protocol/trust_gate.py` cosign subprocess invariants (no shell, list-form argv, version-pinned, asyncio timeout, output ignored for parsing); shares the doctrine the `feedback_canonical_artifact_not_oss_substitute` memory locks — the cognic/sandbox-runtime-* canonical images are real shippable artifacts, NOT placeholder substitutes for OSS images. Test fixtures behind `COGNIC_USE_LOCAL_FIXTURE_*=1` env flags; production profile refuses missing canonical artifacts fail-loud.)
- `sandbox/proxy.py` (per ADR-004 + Sprint-8A spec §10 — egress proxy config rendering + allow-list enforcement + per-request audit-log shaping. Spec round-1 P1 promotion (NOT plan-default); the reviewer rationale at spec line 924 was "the single egress enforcement point; a bug here lets forbidden outbound traffic through; not 'thin wiring'". Owns the wire-protocol-public `ProxyAccessRecord` 6-field frozen dataclass at `protocol.py:140-188` (`host` / `method` / `timestamp` / `policy_id` / `outcome` / `refusal_reason`) materialised via `proxy_log_to_chain_payload` at `proxy.py:238` into `payload["proxy_log"]` on `sandbox.lifecycle.exec_completed` (`audit.py:53` — `{"exit_code": int, "proxy_log": list[dict]}`) AND on `sandbox.policy.violated` (per `audit.py:56` — the T10c R2 ADR-006 amendment landed proxy_log on both lifecycle events for examiner traceability). Per `feedback_evidence_boundary_runtime_validation` — T7 R1-R5 closed 5 evidence-boundary gaps: tz-aware audit timestamps (`tzinfo is not None` + `utcoffset() is not None` at `proxy.py:320`); joint invariants across closed-enum + payload key set (`outcome='allowed'` requires no `refusal_reason`; `outcome='refused'` requires a valid `refusal_reason` per `proxy.py:342-357`); unknown-Literal value refusals; `# type: ignore` smuggling guard; empty-actor_id at audit-row boundary. Dual-container topology: proxy runs in a sidecar container on a sandbox-private internal bridge per the T10a doctrine — the runtime container has `--network none` so the proxy is the only egress path.)
- `sandbox/warm_pool.py` (per ADR-004 + Sprint-8A spec §11 — bounded warm-pool + drain semantics + audit emission + `use_warm_pool=False` replenishment contract. The latency-target enforcement surface (≤500ms P95 sandbox session create per spec §16 exit criterion vs 800-2000ms cold-only). Pool-key derivation at `_derive_pool_key` (`warm_pool.py:164`) keys by `tenant_id + policy + 5 PackAdmissionContext admission fields` — `tenant_id` is part of the key per the T9 R1 P1 reviewer fix (`warm_pool.py:174`) which closed the cross-tenant pool-reuse bug class (admission contexts from different tenants that happen to look identical MUST NOT share a warm session); pinned by `test_checkout_under_different_tenant_id_returns_none` (`tests/unit/sandbox/test_warm_pool.py:641`). Without the tenant_id component, two distinct (policy, pack_context) triples differing only in tenant could silently leak permissions across tenant boundaries. Per `feedback_evidence_boundary_runtime_validation` — T9 R1-R3 closed 3 evidence-boundary gaps: capacity-before-eviction (eviction sweep runs BEFORE capacity decision so a sweep-then-admit race can't admit past cap); wall-time-vs-idle-time semantic clarity at the audit row (idle time is the load-bearing eviction signal, NOT wall time); list/tuple ambiguity at `canonical_bytes` input (chain payloads use list shape only — tuple rejected at the canonical-form gate per the Sprint-2 doctrine).)
- `sandbox/backends/docker_sibling.py` (per ADR-004 + Sprint-8A spec §7 + `feedback_precise_security_terminology` — `DockerSiblingSandboxBackend`. The actual Wave-1 backend-specific enforcement surface; doctrinal-vs-implementation gap resolved at T10a: backend name is `DockerSiblingSandboxBackend` (NOT `DindBackend`); ADR-004 amendment ships the clarifying "Wave-1 DinD = sibling-pattern on host docker.sock" language alongside Sprint 8A. Dual-container internal-network topology: runtime container has `--network none` while the egress-proxy sidecar shares a sandbox-private bridge with the runtime container per `feedback_sandbox_network_isolation_precision` (egress proxy as a sidecar — NOT a "sidecar bound to sandbox loopback", which is impossible under `--network none`). Cgroup integration for CPU/memory caps via Docker `--cpus` + `--memory`; OOM-killer enforcement surfaces as `memory_cap_exceeded`; AgentOS-side timer surfaces as `walltime_cap_exceeded`. Production sandboxes use immutable runtime images only per `feedback_immutable_runtime_images_no_dynamic_install` — dynamic apt-get/pip at create-time refused in production profile with `sandbox_runtime_deps_unsupported_in_production`; dev-profile only with clear separation. Architecture-discipline test (URL-literal / port-int guards at `tests/unit/architecture/`) catches f-string URL drift — concrete loss case at T10c per `feedback_full_gate_pre_commit` (constant extracted as `_PROXY_SCHEME = "http"` so the f-string literal is `"://"`, not `"http://"`).)
- OFF the durable gate per spec §17 + Doctrine F: `sandbox/audit.py` (thin chain-row converter for the 8 sandbox lifecycle event taxonomies — the substantive audit-chain invariants are enforced upstream by the on-gate `core/audit.py` + `core/decision_history.py` + `core/canonical.py`; bugs in event-payload-rendering surface through the 8-event taxonomy unit test + the integration tests of `backends/docker_sibling.py`). **Note**: `sandbox/credentials.py` was an off-gate Sprint-8A carve-out (re-export shim covering the canonical home at `sandbox/admission.py`) but moved ON the gate at Sprint 10 Z1 (2026-05-24) alongside the real `VaultCredentialAdapter` implementation — executing the long-standing "Sprint 10's real adapter goes ON the gate when it lands" promise. The Sprint-10 on-gate entry lives in the Sprint-10 section below.

*Sandbox primitive (Sprint 8B):*

- `sandbox/backends/kubernetes_pod.py` (per ADR-004 amendment §29-30 + `project_openshift_deployment_target` + Sprint-8B T8B-d gate promotion — `KubernetesPodSandboxBackend`. The Wave-1 bank-production backend per the 2026-05-17 preflight decision that AgentOS production targets OpenShift / Kubernetes. Conforms to the SAME `SandboxBackend` Protocol at `protocol.py:254` as `DockerSiblingSandboxBackend`; reuses the backend-agnostic 9-step `admit_policy` Stage-2 pipeline at `admission.py:177` + the canonical image catalog at `catalog.py:206` + the 8-event audit taxonomy at `audit.py:65` + the `sandbox.rego` Wave-1 admission bundle — zero changes to any Sprint 8A primitive. K8s-specific surface: per-Pod NetworkPolicy (deny-all egress except the proxy sidecar via shared Pod localhost per `feedback_sandbox_network_isolation_precision` — sandbox + proxy share network namespace inside a single Pod, NOT a separate ClusterIP Service); OpenShift-compatible pod SecurityContext (no `--privileged`; omits `runAsUser` for namespace-allocated `MustRunAsRange` SCC compat; `capabilities.drop=[ALL]`; `readOnlyRootFilesystem`; `allowPrivilegeEscalation=False`); cgroup-via-exec cpu-budget monitor (NOT metrics-server — chosen at T8B-c Step 1 verification for sub-second granularity + no cluster prereq; tries cgroup v2 `/sys/fs/cgroup/cpu.stat usage_usec` first, falls back to v1 `/sys/fs/cgroup/cpuacct/cpuacct.usage`); OOMKilled detection via `ContainerStatus.last_state.terminated.reason == "OOMKilled"` (kubelet-authoritative; not exit 137 alone). Proxy-log fail-closed contract preserved (T10c R1 P1.2 wire-protocol-public per `SandboxPolicyViolationReason.egress_audit_unreadable`) — sidecar gone / cat exit nonzero / unexpected exception → `_ProxyLogReadFailure` → `egress_audit_unreadable` violation. `_classify_exec_failure` + `_ProxyLogReadFailure` live at the consumer-owned `sandbox/backends/_shared_exec.py` per `feedback_consumer_owned_protocol_for_unlanded_dep`; `docker_sibling.py` keeps its inline copies UNCHANGED per the sandbox isolation-boundary stop-rule; behavioural lockstep across both backends pinned by the test-only drift detector at `tests/unit/sandbox/backends/test_exec_classification_cross_backend_drift.py` per `feedback_drift_detector_test_only_no_runtime_import`. User-found P1 at orchestrator's trust-but-verify gate (T8B-c commit `7943491`): `backend_factory.get_backend` documented `kwargs["settings"] = settings` injection but did not deliver until the post-subagent P1 fix landed — refines the trust-but-verify subagent doctrine ("test fixture papers over production gap" is a distinct subagent failure mode beyond T8B-b's "re-gate-ladder after auto-fix" one; both reinforce `feedback_verify_promotion_meets_floor_at_promotion_time`'s "verify production claim end-to-end, not via test fixture" principle).)
- OFF the durable gate per Doctrine F: `sandbox/backend_factory.py` (pure selection seam — 130 LoC; wire-protocol-public contract is the `Settings.sandbox_backend` Literal arm set + `COGNIC_SANDBOX_BACKEND` env-var override per ADR-004 §32; drift detector at `tests/unit/sandbox/test_backend_factory.py::TestBackendFactoryEnumerateCoverage` pins the Literal-arm-set lockstep + the settings-injection contract is pinned by `TestBackendFactoryRoutesByLiteral` TM-revert verified; substantive enforcement lives in the chosen backend's methods which ARE both on the gate; same Sprint-7A T17 R4 P3 #5 doctrine that kept `cli/conformance.py` off-gate when the dispatched matrix is already CC). `sandbox/backends/_shared_exec.py` (pure-functional helper — 101 LoC, `_classify_exec_failure` + `_ProxyLogReadFailure`; consumer-owned by `kubernetes_pod` per `feedback_consumer_owned_protocol_for_unlanded_dep`; CC risk covered by the on-gate `kubernetes_pod.py` + `docker_sibling.py` consumers; promoting would double-count the same enforcement).

*Sandbox primitive (Sprint 8.5):*

The resumable-session API (`checkpoint()` / `suspend()` / `wake()` per ADR-004 §73-93) ships as Sprint 8.5. T12 promotes 2 modules to the durable per-file coverage gate (71 → 73) at the standard 95% line / 90% branch floor per spec §9. The same T12 commit ran `tools/check_critical_coverage.py` against fresh full-suite `coverage.json` — the user-locked tightening edit B per `feedback_verify_promotion_meets_floor_at_promotion_time` — and the run found BOTH modules below floor on fresh data (89.90%/85.48% + 92.58% line), landed the focused negative-path repair in the SAME commit, and surfaced + fixed a real symlink-loop taxonomy gap in the promoted adapter.

- `sandbox/checkpoint_store.py` (per ADR-004 §73-93 + Sprint-8.5 spec §3.4 + §4.1 — the substantive tenant-isolation + retention enforcement boundary of the resumable-session API. Owns the `CheckpointStore` orchestrator (`persist` / `load_latest` / `tombstone_session` / `load_tombstone` / `purge_expired` / `purge_by_id`), the `CheckpointMetadata` / `VaultLeaseRef` / `TombstoneRecord` frozen wire-public dataclasses, and the `TombstoneCorruptError` / `CheckpointMaxPerSessionRetentionLocked` typed exceptions. On the gate from T12 because: checkpoint bytes are keyed by `<tenant_id>/<session_id>/` — the per-tenant prefix IS the cross-tenant isolation boundary; `from_storage_payload` is the wake-time evidence parser whose every malformed-blob branch MUST route to `ValueError` so the wake() seam maps it to `sandbox_wake_checkpoint_corrupt` rather than crashing with a raw `TypeError`/`KeyError`; `load_tombstone` raising `TombstoneCorruptError` on a tampered sentinel (P1.r6 fail-closed) is what stops a destroyed session from looking restorable; retention-window enforcement at `purge_expired` is the regulator-erasure surface. The wake-time `SandboxRefusalReason` values it raises grew additively at Sprint-8.5 T1 per spec §3.3. Negative-path coverage pinned by `tests/unit/sandbox/test_checkpoint_store_coverage.py` — the T12 same-commit repair: 23 regressions covering the `_require_*` type-validation raise arms, `from_storage_payload` malformed-payload raises, `purge_expired` corrupt-blob/race skips, and `load_tombstone` fail-closed raises.)
- `db/adapters/local_object_store_adapter.py` (Sprint-4 `local_fs` `ObjectStoreAdapter` driver promoted to the gate at Sprint-8.5 T12 per spec §9. Sprint 8.5's `list_prefix()` Protocol extension (spec §3.5) makes the driver a RUNTIME checkpoint tenant-isolation enforcement surface — `CheckpointStore.load_latest()` + `purge_expired()` walk per-tenant `<tenant>/<session>/` prefixes via `list_prefix`, and the driver's dual root-safety check (every resolved dir + file must canonicalise under BOTH `self._root` AND the prefix subtree) is what stops a tenant-a symlink pointing at tenant-b's checkpoints from leaking into tenant-a's listing. Promoted alongside `checkpoint_store.py` because Sprint 8.5 adds the new substantive enforcement surface — NOT because 8.5 is the first runtime consumer (the driver already backs `protocol/supply_chain.py` Sigstore bundles per P3.r6). T12's coverage repair surfaced + fixed a real taxonomy gap: Python 3.12 `Path.resolve()` raises `RuntimeError` (not `OSError`) on a symlink loop, so the driver's four `except OSError` resolve-guards in `list_prefix` / `_walk_keys_root_safe` / `_validate_and_resolve` did not catch loops — a symlink loop escaped as a raw `RuntimeError` bypassing the `PathTraversalError` closed taxonomy. Fixed to `except (OSError, RuntimeError)` at all four sites; the `list_prefix` prefix-resolve site additionally changed from a silent empty-iterator `return` to a fail-closed `PathTraversalError` raise (a resolution failure is a genuine error — `resolve(strict=False)` never raises for a merely non-existent prefix, which is handled by the separate `exists()/is_dir()` guard). The `_walk_keys_root_safe` entry-resolve site DELIBERATELY keeps `continue` (skip the bad entry, do NOT raise) — raising mid-walk would be a DoS vector where one planted loop symlink kills every listing. Negative-path coverage pinned by `tests/unit/db/test_local_object_store_adapter_coverage.py` — the T12 same-commit repair: 12 path-traversal / symlink-escape / symlink-loop / scandir-race regressions (incl. the `get()` final-key symlink-loop taxonomy fix).)
- OFF the durable gate per spec §4.2 + Doctrine F: `sandbox/reaper.py` (`CheckpointReaper` — a thin asyncio loop `run_once()` / `run_forever()` wrapping the on-gate `CheckpointStore.purge_expired()`. NOT-CC because the substantive retention-floor enforcement lives in `checkpoint_store.py`; the reaper carries only schedule + loop + exception-survival glue. Started by the AgentOS app lifespan at T10, opt-in via the `create_app(checkpoint_store=...)` seam. Same Doctrine F precedent as Sprint 8A's `sandbox/audit.py` + Sprint 8B's `sandbox/backend_factory.py`. Related Sprint-8.5 extensions to already-on-gate modules: `core/config.py` gained 3 Settings at T10 (`sandbox_checkpoint_retention_s` / `sandbox_max_checkpoints_per_session` / `sandbox_reaper_interval_s`); `portal/api/app.py` lifespan gained the opt-in reaper wiring at T10; `portal/api/ui/action_routes.py` had its `resume` stub reason honesty-fixed at T11 — the planned `resume`→`wake()` lift was rejected for lack of a Wave-1 run→session resolver.)

*Sandbox primitive (Sprint 10):*

Vault credential leasing per ADR-004 §25 / §68 / §102. Sprint 10 (closed 2026-05-24) ships the real `VaultCredentialAdapter` replacing the Sprint-8A fail-loud `KernelDefaultCredentialAdapter` sentinel, the `CredentialAdapter` Protocol extension (`mint_lease` + `revoke_lease` per Q4 LOCK), and the per-backend `SandboxBackend.create()` extension threading `requires_credentials` post-admission. Wave-1 `transport.lease()` uses `client.read(path)` per Round-9 Gap Q (Vault's dominant dynamic-secret endpoints — database/aws/gcp — are GET-only; the original write-with-ttl recipe was wrong for 3 of the 4 spec §3.4 target engines + surfaced + closed by Z2's live proof against pre-running Vault + Postgres). Z1 promotes 4 modules to the durable per-file critical-controls coverage gate (81 → 85) at the standard 95% line / 90% branch floor per the user-locked tightening edit B + `[[feedback_verify_promotion_meets_floor_at_promotion_time]]` — all 4 promoted modules at or above floor on fresh `--cov-branch` data, 85/85 PASS verified end-to-end. Z2 real-Vault two-layer integration proof env-gated on `COGNIC_RUN_VAULT_INTEGRATION=1` (+ `COGNIC_VAULT_TEST_ADDR` + `COGNIC_VAULT_TEST_TOKEN` + `COGNIC_VAULT_TEST_SECRET_PATH` per Round-8 Q1/Q2/Q3 — pre-running server, no `vault` binary spin-up; Docker-only Layer 2; true dynamic backend, not kv-v2); the suite fails LOUD (`AssertionError`, NOT `pytest.skip`) on missing env vars / unreachable server / unconfigured dynamic secrets engine. Closed-enum growth: `SandboxRefusalReason` 22 → 26 → 27 (+4 at Sprint 10 T9: 3 mint-failure values with Stage-2 raise sites at T10 backend `create()` + 1 Rego TTL-cap value Literal-only per spec §7.3 amendment; +1 at Sprint 10.1 — `sandbox_credential_lease_ttl_grant_exceeds_request` for the new post-mint granted-vs-requested TTL refusal mapped from the new `VaultLeaseGrantExceedsRequest` exception per ADR-004 §25 amendment) + `SandboxLifecycleEvent` 12 → 15 (+3 at T9: `lease_minted` / `lease_revoked` / `lease_revoke_failed`; unchanged in Sprint 10.1). **Phase 3 still NOT closed** — Sprint 10 closes the sandbox-credentials sub-arc per ADR-004 §25/§68/§102; Sprint 10.5 scheduler primitive (ADR-022) remains for full Phase-3 closure.

**Sprint 10.1 hotfix (2026-05-25)**: post-merge review of PR #38 surfaced 2 gaps closed in the `fix/sprint-10.1-credential-leasing-gaps` branch: finding #2 (P1 security — granted-vs-requested TTL enforcement at `core/vault.py` + best-effort `transport.revoke(lease_id)` before raise per Finding A of plan-review round 1; new wire-public `VaultLeaseGrantExceedsRequest` exception carrying `lease_id` + `revoke_outcome ∈ {"revoked", "revoke_failed"}` attrs + new wire-public `SandboxRefusalReason("sandbox_credential_lease_ttl_grant_exceeds_request")` closed-enum value; lease_id token in formatted message string per Finding 3 of plan-review round 2 so backend `SandboxLifecycleRefused.detail=str(exc)` carries the dangling-lease correlator) + finding #3 (P2 test-honesty — Z2 importorskip → conditional fail-loud when opted in per spec §10 amendment; subprocess contract regression at `tests/unit/test_z2_import_fail_loud_contract.py`). Finding #1 (workload credential projection) DEFERRED to Sprint 10.5 with explicit shape-decision gate per ADR-022 (env / file / socket / projected secret). ADR-004 §25 amendment + Sprint-10 spec §3.5/§6.1/§7.1/§10 amendments land in the same hotfix branch. Per-backend `docker_sibling.py` + `kubernetes_pod.py` post-mint cleanup except-tuples extended 4 → 5 in the SAME commit as the closed-enum + cross-backend mapping extension per Finding B of plan-review round 1 — no intermediate state where the new exception escapes uncaught at the backend boundary.

- `core/vault.py` (per ADR-004 §25/§68/§102 + Sprint-10 spec §3.3 + Z1 promotion at commit `c10a888` — `core/` stop-rule per AGENTS.md "Anything in `core/`". Owns the dynamic credential leasing primitive: 5-field frozen `VaultLeaseRequest` (`secret_path + ttl_s + tenant_id + actor_ref + scope_label` per spec §3.1; `actor_ref` is a core-owned `VaultLeaseActorRef` projection per the `core → portal` architectural-arrow contract), 6-field frozen `CredentialLease` (`lease_id + request + token + minted_at + ttl_s_granted + expires_at`; `token` is `dict[str, str]` passthrough per spec §3.4 — kernel does NOT normalise across backends), 5-value exception taxonomy (`VaultUnavailable` / `VaultPathNotFound` / `VaultAuthDenied` / `VaultProtocolError` / `VaultLeaseGrantExceedsRequest` per spec §7.1 + Sprint-10.1 amendment per ADR-004 §25; the sandbox boundary at T6 collapses `VaultProtocolError` to `sandbox_credential_mint_failed_vault_unavailable` via the shared `_shared_credentials.py` mapping table for closed-enum stability; the new 5th value `VaultLeaseGrantExceedsRequest` (Sprint 10.1) maps to its own wire-public `sandbox_credential_lease_ttl_grant_exceeds_request` value via the same shared mapping helper, carries `lease_id` + `revoke_outcome ∈ {"revoked", "revoke_failed"}` attrs + includes the `lease_id` token in the formatted message string so backend `SandboxLifecycleRefused.detail=str(exc)` preserves the dangling-lease correlator), and the async `lease_credential(request, *, transport, settings) → CredentialLease` + `revoke_credential(lease_id, *, transport) → None` entry-points. `lease_credential` calls `transport.lease(secret_path, ttl_s)` — read-style HTTP path per Round-9 Gap Q; `ttl_s` is NOT passed to Vault on the wire (Vault's role-side `default_ttl` / `max_ttl` authoritative for what the wire returns; `CredentialLease.ttl_s_granted` reflects response's `lease_duration`) but is **enforced kernel-side post-mint per Sprint-10.1 amendment**: when `ttl_s_granted > request.ttl_s`, `lease_credential` best-effort-revokes the just-minted lease then raises `VaultLeaseGrantExceedsRequest` so an over-cap lease does not leak into Vault's role default_ttl window. Exception mapping covers the full hvac + OSError + protocol-error surface so no exception escapes the closed 5-value taxonomy; `asyncio.CancelledError` inherits `BaseException` so passes through uncaught on cancellation per the existing taxonomy contract. Architectural arrow: `core/vault.py` MUST NOT import from `cognic_agentos.portal.*`; pinned by `tests/unit/core/test_vault.py::TestArchitecturalArrow`.)
- `core/_vault_transport.py` (per Sprint-10 spec §2.1 + §3.5 + Z1 promotion at commit `c10a888` — `core/` stop-rule per AGENTS.md "Anything in `core/`". INTERNAL shared low-level Vault HTTP transport — domain-shaped methods (`read` / `write` / `lease` / `revoke` / `health_check`) each wrap the corresponding sync hvac call via `asyncio.to_thread` + bounded retry. Both `db/adapters/vault_adapter.py::VaultAdapter` AND `core/vault.py::lease_credential` consume this for ONE Vault transport discipline (one shared `hvac.Client` across both consumers; one static-token auth context; one retry policy). T3 user-locked carve-out: `VaultAdapter.lease()` funnels through `transport.read(path)` (wraps response in Sprint-1C `SecretLease` shape); `core/vault.lease_credential` funnels through `transport.lease(path, ttl_s)` (wraps in `CredentialLease` shape) — both methods delegate to `client.read(path)` at the hvac level post-Gap-Q; the two distinct method names exist so the two consumer-shape contracts can evolve independently (e.g. future PKI write-style support lands as a separate transport method `lease_with_body(path, body)`, NOT a runtime overload of `lease()` per spec §3.5). `lease()` body uses `client.read(path)` + explicit `del ttl_s` — `ttl_s` is NOT wire-forwarded at this transport layer but **remains load-bearing post-T1** because `core/vault.lease_credential` uses it as the post-mint cap (when `ttl_s_granted > request.ttl_s`, `lease_credential` best-effort-revokes + raises `VaultLeaseGrantExceedsRequest` per Sprint-10.1 amendment to ADR-004 §25); NO fallback-to-write-on-405 heuristic. Wave-1 static-token authentication only (operator-pre-provided `Settings.vault_addr` + `Settings.vault_token` + `Settings.vault_namespace`; no AppRole or K8s ServiceAccount auth flows — deferred per spec §10).)
- `sandbox/credentials.py` (per Sprint-10 spec §3.5 + Z1 promotion at commit `c10a888` — **executes the AGENTS.md off-gate → on-gate promotion promise** that was reserved at Sprint 8A. The real `VaultCredentialAdapter` per ADR-004 §102 Q4 LOCK: implements the post-T5 `CredentialAdapter` Protocol's 3 methods (`fetch_secret` + `mint_lease` + `revoke_lease`); thin delegation layer to `core/vault.py` (`mint_lease` → `lease_credential`; `revoke_lease` → `revoke_credential`; `fetch_secret` → `transport.read`). USER-LOCKED CORRECTION #1: `fetch_secret` does NOT swallow transport exceptions — Protocol contract at `sandbox/admission.py:119` says "Implementations MUST surface auth / network failures as exceptions (NOT silent None)"; the plan-of-record's `try / except Exception: return None` catch-all was rejected at Round-0. USER-LOCKED CORRECTION #2: `mint_lease` / `revoke_lease` PRESERVE the `core.vault` exception taxonomy unchanged — `mint_lease` can surface the full 5-value taxonomy post-Sprint-10.1 (`VaultUnavailable` / `VaultPathNotFound` / `VaultAuthDenied` / `VaultProtocolError` / `VaultLeaseGrantExceedsRequest` — the 5th value is raised by `core.vault.lease_credential` when the post-mint TTL gate fires per ADR-004 §25 amendment, with best-effort revoke before the raise); `revoke_lease` remains scoped to the hvac/transport-error 4-value subset (`VaultUnavailable` / `VaultPathNotFound` / `VaultAuthDenied` / `VaultProtocolError`) because revoke has no granted-vs-requested concept. The sandbox-closed-enum collapse to `sandbox_credential_*` belongs at the create / admission boundary that raises `SandboxLifecycleRefused` (the backend `create()` except-tuple at T10 + the Sprint-10.1 amendment that extended the tuple 4 → 5 to also catch `VaultLeaseGrantExceedsRequest`), NOT inside this thin adapter — pinned by AST-walk regression at `tests/unit/sandbox/test_credentials.py::TestNoClosedEnumMappingCreepInT6` (exempts docstring positions so this intent-documentation prose can name the closed-enum types without tripping the detector). The Sprint-8A re-exports (`CredentialAdapter` + `KernelDefaultCredentialAdapter`) are PRESERVED for consumer backward-compat.)
- `sandbox/backends/_shared_credentials.py` (per Sprint-10 T10 K8s round-2 Gap I + Round-7 Gap O Z1-pre-flight doctrinal lock + Z1 promotion at commit `c10a888` — **wire-protocol-public artifact owner; promoted as the NEW 4th Z1 module per the doctrinal-fit decision that distinguishes it from Doctrine-F carve-outs**. Owns the cross-backend Vault-exception → `SandboxRefusalReason` closed-enum mapping (`_mint_exception_to_refusal_reason` — `VaultUnavailable` / `VaultPathNotFound` / `VaultAuthDenied` → 3 distinct `sandbox_credential_mint_failed_*` values; `VaultProtocolError` collapses to `vault_unavailable` for closed-enum stability per spec §6.1 / §7.1 last row; Sprint-10.1 amendment adds the 5th arm `VaultLeaseGrantExceedsRequest` → `sandbox_credential_lease_ttl_grant_exceeds_request` per ADR-004 §25 amendment — 5-value `core.vault` exception taxonomy → 4-value `sandbox_credential_*` closed-enum mapping; both backends' `create()` post-mint cleanup except-tuples extend 4 → 5 in the SAME commit as the mapping extension per Finding B of plan-review round 1 so no intermediate state leaves the new exception escaping uncaught at the backend boundary). **The decisive Gap O contrast**: this module is the wire-public-artifact owner pattern (drift between Docker + K8s on the mapping = wire-protocol regression where the same Vault exception surfaces as different `SandboxRefusalReason` values to bank-overlay consumers) — same doctrinal fit as `core/canonical.py` (wire-format owner for evidence-pack export). This is DISTINCT from `sandbox/backends/_shared_exec.py`'s Doctrine F off-gate carve-out (consumer-owned helper; K8s primary consumer with Docker inlining the equivalent — substantive enforcement lives in the on-gate consumers; `_shared_exec.py` stays OFF the gate per AGENTS.md L48 "Anything in `sandbox/backends/`" being implicit not enumerated). The doctrinal arrow that justifies promoting `_shared_credentials.py` while keeping `_shared_exec.py` off-gate: wire-public-mapping-table evolution requires CC review; consumer-owned-helper evolution does not. Dependency-neutral module — imports ONLY from `cognic_agentos.core.vault` (for the 5 Vault exception classes post-Sprint-10.1: `VaultUnavailable` / `VaultPathNotFound` / `VaultAuthDenied` / `VaultProtocolError` / `VaultLeaseGrantExceedsRequest`) AND `cognic_agentos.sandbox.protocol` (for the `SandboxRefusalReason` Literal). Adding any backend-specific import (`aiodocker` / `kubernetes_asyncio` / any other) would re-introduce the coupling-bug class that motivated extracting the helper out of `docker_sibling.py` at T10 K8s round-2 — pinned by `tests/unit/sandbox/backends/test_shared_credentials.py`'s AST scan + subprocess blocked-import probe (the latter mirrors the reviewer's blocked-import verification method that surfaced Gap I).)
- OFF the durable gate per Doctrine F (Sprint-10 carve-out — explicit doctrinal-fit contrast with `_shared_credentials.py` per Round-7 Gap O above): `sandbox/backends/_shared_exec.py` (consumer-owned helper; K8s primary consumer with Docker inlining the equivalent; substantive enforcement lives in the on-gate consumers `docker_sibling.py` + `kubernetes_pod.py`; the test-only drift detector at `tests/unit/sandbox/backends/test_exec_classification_cross_backend_drift.py` pins behavioural lockstep without needing per-file CC promotion). The carve-out remains LOAD-BEARING because `_shared_exec.py` returns an internal exec-classification result (not a wire-protocol-public value) and is genuinely consumer-owned (one primary consumer pattern), whereas `_shared_credentials.py` is wire-public + symmetric cross-backend infrastructure. Future cross-backend pure helpers should be evaluated against this same wire-public-artifact vs consumer-owned-helper doctrinal split before deciding gate fit.

*Runtime scheduler primitive (Sprint 10.5a + 10.5b):*

ADR-022 runtime scheduler primitive — bounded-FIFO admission control + concurrency caps + hash-chained task lifecycle + Rego policy gate + sandbox-routing seam. core/ stop-rule per AGENTS.md L48. Sprint 10.5a (T1-T6 + Z1a) promotes 3 modules to the durable per-file critical-controls coverage gate (85 → 88) at the standard 95% line / 90% branch floor per the user-locked tightening edit B per [[feedback_verify_promotion_meets_floor_at_promotion_time]]. Sprint 10.5b (T7-T11 + Z1b) adds the `scheduler.rego` stop-rule policy bundle entry (above) + promotes `core/scheduler/policy.py` to the gate (88 → 89) — all 4 modules at or above floor on fresh `--cov-branch coverage.json` verified end-to-end (89/89 PASS). Closed-enum growth across 10.5a+10.5b: `SchedulerAdmissionOutcome` 7 values (2 accepted + 5 refused); `SchedulerRefusalReason` 5 values; `SchedulerTaskState` 7 values; `SchedulerPromotionRefusedReason` 2 values (`caps_saturated` / `not_at_queue_head`); `SchedulerSubmitInputInvalidField` 1 value (`parent_task_id`); `SchedulerTaskCancelledReason` Literal; `SchedulerTaskFailedReason` Literal; `SchedulerTaskPreemptedReason` Literal; `ActorType` 2 values. Sub-block 10.5c (workload credential projection per ADR-004 §25 amendment, closing the Sprint-10.1 deferred Finding #1) was **SPLIT INTO SPRINT 10.6** at the Z1b VALVE CHECK per plan §1318-1332 — cumulative wall-clock across T1-T11 + Z1a + Z1b crossed the 4.5 wu mitigation budget threshold. Sprint 10.5 closes at Z1b with 10.5a + 10.5b only; Sprint 10.6 owns the credential-projection sub-block (spec at `docs/superpowers/specs/2026-05-26-sprint-10.6-workload-credential-projection-design.md` + plan at `docs/superpowers/plans/2026-05-26-sprint-10.6-workload-credential-projection.md`). Phase 3 closes at the Sprint 10.6 commit since 10.6 is the final piece of Phase 3 after the split.

**Option A doctrine LOCKED at T9 (plan §1210 superseded)**: SchedulerPolicy owns Rego policy ONLY. SchedulerEngine owns operational gates (pack_state / kill_switch / quota / queue / caps / public SchedulerAdmissionOutcome taxonomy). Plan §1210's literal dual-consultation interpretation was REJECTED because it conflated ADR-018 (emergency controls) with ADR-015 (policy-as-code) — kill_switch is an operational real-time emergency surface, not a policy decision. QuotaInterrogator + KillSwitchInterrogator + ParentBudgetResolver + PackStateInterrogator stay consumer-owned Protocol seams in `core/scheduler/_seams.py`; NO `core/emergency/*` dependency. AST guards at `tests/unit/core/scheduler/test_architecture_no_emergency_import.py` + `test_architecture_no_sandbox_import.py` pin both substrate-independence boundaries.

**T11 SandboxAdapter ownership boundary**: scheduler→sandbox integration via the `SandboxAdapter` Protocol (atomic create+destroy pair) declared in `core/scheduler/_seams.py`. Scheduler NEVER imports from `cognic_agentos.sandbox/*` (substrate independence); AgentOS app's DI binder at startup wraps the real `sandbox.SandboxBackend` into a structurally-conforming adapter + translates upstream `SandboxLifecycleRefused` → scheduler-owned `SandboxCreateRefused` typed exception. The atomic create+destroy API makes the round-1 leak class (create without destroy → leak on storage-failure-after-create) unrepresentable at the type level; the round-2 P1 fix replaced two separate kwargs with the single adapter object.

**Sprint 13.7 (composition root) — production construction site**: `SchedulerEngine` is production-constructed in `harness/runtime.py::build_runtime` (off-gate composition root), CACHE-CONDITIONAL — built only inside the `if adapters.cache is not None:` block because its quota + kill-switch conformers need the Redis control plane; `Runtime.scheduler is None` on the gateway-only path (NEVER a silent `_Null`-quota scheduler that fails on first submit). Every seam slot is bound to a real conformer: `quota_interrogator` = the runtime `QuotaEngine` (13.6b), `kill_switch_interrogator` = `SchedulerKillSwitchConformer(engine=<runtime KillSwitchEngine>)` (13.6a), `pack_state_interrogator` = `PackStoreStateInterrogator(store=PackRecordStore(engine))` (11b), `policy_evaluator` = `SchedulerPolicy(...).evaluate` over a dedicated `OPAEngine` on `settings.scheduler_policy_bundle`, `approval_engine` = `runtime.approval_engine` (13.5b1 — the 13.5c2 scheduler approval seam is production-CONSTRUCTED; A4a added the delegated skip-consult path, and A4b live-exercises that delegated path from managed runs. The scheduler-owned grant-mint/verify consult remains not production-exercised by the managed-run path because high-risk work delegates to sandbox admission instead), `storage` = `SchedulerStorage(engine)`. `parent_budget_resolver` is OMITTED → the engine's `_NullParentBudgetResolver` fail-loud sentinel; a top-level submit never consults it, a sub-agent submit fails loud until Sprint 14A wires the real `LocalParentBudgetResolver` + a run→budget snapshot. The engine is exposed on `Runtime.scheduler` + threaded onto `app.state.scheduler` (lifespan introspection seam, pre-seeded `None`); 13.7 adds NO route/caller (Fork D — the managed-runtime submit→execute caller is 14A). No CC promotion (the scheduler stack is already on the gate; `build_runtime` + `core/config.py` + `portal/api/app.py` stay off-gate). MCP-host production construction is **Sprint 13.8**; sandbox-approval production wiring folds into **Sprint 14A**. See ADR-022 "Sprint 13.7 amendment".

- `core/scheduler/engine.py` (per ADR-022 + spec §4.1-§4.5 + Z1a promotion — `core/` stop-rule per AGENTS.md L48. The substantive admission-control + task-lifecycle orchestration boundary. Owns the public seam `SchedulerEngine` with 7 methods (submit / mark_running / complete / fail / cancel / preempt / reap_expired), the `PolicyDecision` + `PolicyEvaluator` callable seam (T8 SchedulerPolicy conforms), the `SchedulerPromotionRefused` typed exception with 2-value closed-enum `reason` field (`SchedulerPromotionRefusedReason` Literal: `caps_saturated` / `not_at_queue_head`), and the `_TaskAttribution` per-task counter-attribution snapshot (frozen dataclass) used by `_transition_terminal` + `reap_expired` for in-memory counter cleanup without storage round-trips. **Round-7 P1 contract — FIFO-within-class promotion**: `mark_running` consults `queue.peek()` BEFORE the cap re-check + storage transition; promoting any task that isn't the FIFO head raises `SchedulerPromotionRefused(reason="not_at_queue_head")` without mutating state. **T10 contract — parent_task_id narrowing wired (round-7 fail-loud replaced)**: when `submit_input.parent_task_id is not None`, `submit()` parses the str via `uuid.UUID(...)` inside a try/except that translates `ValueError` to the typed `SchedulerSubmitInputInvalid(field="parent_task_id", reason=...)` (closed-enum `SchedulerSubmitInputInvalidField` Literal: `parent_task_id`), then awaits `parent_budget.remaining_budget_for(parent_uuid)` and computes `effective_tokens = compute_child_budget(parent_remaining_budget=..., child_pack_quota=submit_input.requested_estimated_tokens)`. The narrowed `effective_submit_input` (via `dataclasses.replace`) is threaded through ALL 5 admission gates + ALL 5 `_emit_admission_refused` call sites so quota + storage + chain rows + audit all see the same value (closes the round-6 P1 #2 audit/quota-mismatch finding). Fail-loud-via-sentinel contract: pre-Sprint-11 deployments without a real `ParentBudgetResolver` injected propagate `NotImplementedError` from `_NullParentBudgetResolver.remaining_budget_for` per [[feedback_consumer_owned_protocol_for_unlanded_dep]] — the failure mode shifted from the round-7 engine-explicit raise to seam-Protocol propagation at T10. **Round-7 P2 contract — durable-first ordering**: `mark_running` queued path issues `storage.transition` FIRST, THEN commits in-memory bookkeeping (counter increment + attribution migration + queue removal). If storage fails, no in-memory state has mutated — engine state matches DB state without rollback code. **Round-5 P1 contracts**: (1) `refused_queue_full` releases the quota reservation before returning the refusal; (2) terminal transitions decrement the three concurrency counter dimensions via `_decrement_counts(attribution)` with defensive `max(0, ...)` clamping; (3) cancelled/failed queued tasks are removed from the FIFO queue via attribution lookup; (4) storage failure on the queued path rolls back the queue.enqueue (try/except around storage.submit inside `_do_admission_work`); (5) all 5 admission-refusal paths emit `scheduler.admission_refused` chain rows via `_emit_admission_refused`. The 4 consumer-owned seam Protocols are declared in `core/scheduler/_seams.py` (Sprint 11 + 13.5 supply real conformers): `QuotaInterrogator` (per ADR-018 emergency quotas), `KillSwitchInterrogator` (per ADR-018 kill switches), `ParentBudgetResolver` (per ADR-005 sub-agent budget inheritance), `PackStateInterrogator` (per ADR-012 pack lifecycle state probe). Wave-1 single-asyncio-loop assumption documented in the `mark_running` docstring: single-writer for promotion; multi-instance distributed counters are Wave-2 per ADR-022. **Sprint 14A-A4a** added the `approval_delegated_to` boundary validation (unknown value + mutual-exclusion-with-a-valid-`approval_request_id` → `SchedulerSubmitInputInvalid(field="approval_delegated_to")`, precedence-pinned behind the `approval_request_id` parse; `SchedulerSubmitInputInvalidField` 2 → 3) + the Step-3.5 skip-consult-when-delegated path (`if approval_delegated_to is not None: pass` ahead of the consult `elif` — no mint, `approval_verified` stays False, the rego admits high-risk via the delegated arm); **live at Sprint 14A-A4b — the managed-run executor sets the signal (the run-lane fail-closed manifest tier flows to both the scheduler submit + the sandbox `PackAdmissionContext`)**.)
- `core/scheduler/queue.py` (per ADR-022 + spec §4.3 + §4.5 + Z1a promotion — `core/` stop-rule per AGENTS.md L48. The bounded-FIFO + concurrency-cap primitives consumed by `SchedulerEngine` at construction time. Owns `BoundedQueue` (FIFO `deque` of `(item, enqueue_datetime)` tuples with bounded depth + 5 public methods: `enqueue` / `dequeue` / `peek` / `remove` / `compute_retry_after_s` + 2 properties `depth` + `max_depth`), the `ConcurrencyCaps` frozen dataclass (4 cap fields: `per_tenant_interactive` / `per_tenant_background` / `per_pack` / `per_actor`; each `gt=0` runtime-guarded at `__post_init__`), and the `QueueFull` exception class. **Bounded-invariant runtime guards are wire-protocol-public** — a caller passing zero/negative `max_depth` or `class_sla_s` would silently produce a queue that can never accept work OR an SLA that never reports usable retry-after, breaking the SchedulerEngine consumer contract; mirrored at the Settings-layer (T6) `gt=0` Pydantic Field constraints in `core/config.py`. **Round-6 P2 contract — dynamic wall-clock aging**: `BoundedQueue.compute_retry_after_s` ages dynamically against an injectable `clock` seam (`Callable[[], datetime]`) rather than the pre-round-6 static `oldest_age_s` parameter captured at enqueue. A queue waiting 4.9s on a 5s SLA correctly reports ~1s retry, not ~5s — pinned by `test_retry_after_dynamic_aging_shrinks_as_time_passes`. **Round-7 P1 contract — peek()**: returns the FIFO head WITHOUT removing it (idempotent); consumed by `SchedulerEngine.mark_running` to enforce FIFO-within-class promotion. **Spec §4.5 invariant**: `has_headroom_for(class_, tenant_count, pack_count, actor_count) → bool` is fail-closed on unknown `class_` (raises `ValueError`); interactive + background tenant caps are SEPARATE axes (pinned by `test_background_class_uses_background_tenant_cap`). Test coverage at Z1a: 100% line / 100% branch on fresh `coverage.json`.)
- `core/scheduler/storage.py` (per ADR-022 + ADR-009 + Z1a promotion — `core/` stop-rule per AGENTS.md L48. The Postgres-backed scheduler task store mirroring `packs/storage.py` + `models/storage.py` patterns. `SchedulerStorage` is the `DecisionHistoryStore.append_with_precondition` consumer driving `scheduler.admission_accepted` + `scheduler.task_*` chain rows via the Sprint-2.5 T2 atomic primitive. Two distinct write paths: `submit()` (genesis — INSERT the `scheduler_tasks` row in `pending` state + append `scheduler.admission_accepted` in one transaction) and `transition()` (state-machine — SELECT ... FOR UPDATE the row, validate via the pure-functional `validate_transition` from T2, UPDATE the state cache, append `scheduler.task_<terminal_or_running_state>`). **Doctrine Lock D mirror**: chain-head SELECT FOR UPDATE → task-row SELECT FOR UPDATE → `validate_transition` → state-cache UPDATE → chain row INSERT → chain-head UPDATE, all inside a single `engine.begin()` transaction owned by `append_with_precondition`. Failure at any step rolls back all three — fail-closed. **Round-3 P1 #2 contract — full evidence-snapshot threading**: `_LockedTaskSnapshot` frozen dataclass (11 fields) projects the row-locked `scheduler_tasks` projection from `_precondition` to `_build_record` so the chain payload is the canonical evidence snapshot per [[feedback_chain_payload_is_evidence_snapshot]]; an examiner reading the chain row alone can verify the lifecycle claim without joining back to the mutable `scheduler_tasks` row. **Round-4 P1 contract — reserved-key overlap guard**: `_RESERVED_TRANSITION_PAYLOAD_KEYS` 14-key frozenset is the wire-protocol-public closed set of keys `transition()` builds from the row-locked snapshot; caller-supplied `payload_extras` overlapping any reserved key raises `ValueError` at the function-entry preflight BEFORE any DB state is mutated, preventing silent corruption of hash-chained evidence. **Round-3 P1 #1 contract — preflight transition-name guard**: `to_state` not in `_STATE_TO_DECISION_TYPE` raises `SchedulerTransitionRefused("scheduler_transition_invalid_state_pair")` BEFORE the `_precondition` closure is defined (mirrors the `packs/storage.py` preflight pattern), so an out-of-vocabulary state name cannot leak past the closed-enum boundary as a `KeyError` past downstream `SchedulerTransitionRefused`-catching consumers (T5 `SchedulerEngine`). **Round-6 P2 contract — closed-enum admission-refused guard**: `record_admission_refused(reason: SchedulerRefusalReason)` runtime-rejects any value outside the 5-value `_VALID_REFUSAL_REASONS` frozenset (built via `typing.get_args(SchedulerRefusalReason)` so drift is impossible) BEFORE the chain row is persisted; refusal events have NO scheduler_tasks row insert (task was never admitted; the chain row is the only persisted evidence). **`record_admission_refused` ↔ `submit` separation contract**: only `submit()` inserts a row + emits `scheduler.admission_accepted`; `record_admission_refused()` emits a chain row WITHOUT row insert. The `SchedulerTaskNotFound` typed exception is caller-distinct from `SchedulerTransitionRefused` (not-found is a missing-row failure mode, not a state-machine refusal). Test coverage at Z1a: 100% line / 100% branch on fresh `coverage.json`. **Sprint 14A-A4a** added a conditional `approval_delegated_to` key to the `submit()` `scheduler.admission_accepted` payload — present ONLY when non-None, alongside `approval_verified=False` and NO scheduler `approval_request_id` (the grant-correlator key is gated on `approval_verified=True`, so a fake grant is unrepresentable on the delegated path); the `record_admission_refused` + `transition` paths are untouched.)
- `core/scheduler/policy.py` (per ADR-022 + ADR-015 + Sprint-10.5b T8 + Z1b promotion — `core/` stop-rule per AGENTS.md L48. The single Python boundary bridging the Wave-1 `policies/_default/scheduler.rego` bundle (above) to the `SchedulerEngine` admission pipeline via the existing `core/policy/engine.OPAEngine`. Owns the canonical `PolicyDecision` frozen dataclass (T8 re-homed from T5's stub at `engine.py`; engine remains the back-compat re-export site via `from ... import PolicyDecision as PolicyDecision`), the `SchedulerPolicy` class with single async `evaluate(submit_input) → PolicyDecision` public method, the pure-functional **10-key** `_build_rego_input` static helper projecting `SubmitInput` → spec §4.8 input shape (the 8-key spec set + `approval_verified` per Sprint-13.5c2 + `approval_delegated_to` per Sprint-14A-A4a), the `_fetch_refusal_reason` direct-subprocess helper for the string-returning decision point (Sprint-4 OPAEngine only handles boolean; T8 doctrine), and the `_MINIMAL_SUBPROCESS_ENV` constant lockstep-pinned with `OPAEngine`'s by the test-only drift detector at `test_policy.py::TestSchedulerPolicySubprocessEnvParity`. **Plan §1179 suppression contract**: when OPAEngine returns `allow=true`, returns `PolicyDecision(allow=True, policy_reason=None)` — suppresses the bundle's raw `scheduler_default_deny` document. **Plan §1181 fail-closed envelope**: any `OpaNotInstalledError` or `RegoEvaluationError` (including on the deny-path `_fetch_refusal_reason` second subprocess) → `PolicyDecision(allow=False, policy_reason="opa_unavailable")`. **Plan §1167 vocabulary-boundary contract**: `policy_reason` values (internal diagnostic) NEVER appear in the wire-public `SchedulerRefusalReason` Literal — engine's `submit()` maps every deny via `refused_policy_denied` outcome. Test coverage at Z1b: 100% line / 100% branch on fresh `coverage.json` (Z1b focused-coverage repair landed 10 subprocess-error-path regressions in the same commit per the user-locked tightening-edit B). Plan §1210 LOCKED-superseded — SchedulerPolicy owns Rego ONLY; kill-switch + quota + parent-budget + pack-state stay engine-side seams per the Option A doctrine above.)
- OFF the durable gate per Doctrine F (Sprint-10.5a + 10.5b carve-outs): `core/scheduler/_seams.py` (now 180+ LoC — 4 consumer-owned Protocols + 4 fail-loud sentinels + `compute_child_budget` pure-functional helper + T11 `SandboxAdapter` Protocol + `SandboxCreateRefused` typed exception per [[feedback_consumer_owned_protocol_for_unlanded_dep]]; substantive enforcement lives in the on-gate `engine.py` consumer; the sentinels' `NotImplementedError` raises are the contract surface, not threshold-based coverage). `core/scheduler/_types.py` (closed-enum Literals + `SubmitInput` / `AdmissionDecision` / `TaskActor` / `TaskFailedPayload` frozen dataclasses + `validate_transition` pure state-machine validator + `SchedulerTransitionRefused` typed exception; pure type module — the substantive enforcement lives in the on-gate `storage.py` + `engine.py` consumers; closed-enum vocabulary drift detectors at `test_closed_enums.py` + `test_state_machine.py` cover the surface independently of coverage threshold). `core/scheduler/__init__.py` (re-export only).

*Workload credential projection (Sprint 10.6):*

ADR-004 §25 amendment workload credential projection — closes the Sprint-10.1 deferred Finding #1 (the mint-then-project wire path from `VaultCredentialAdapter.mint_lease`'s `CredentialLease` into a running workload). Split out of Sprint 10.5 at the Z1b VALVE CHECK (10.5c → Sprint 10.6). `sandbox/` is a stop-rule isolation boundary (AGENTS.md "Stop rules" — the "Sandbox or sub-agent enforcement boundaries" rule). Sprint 10.6 Z1c promotes 1 module to the durable per-file critical-controls coverage gate (89 → 90) at the standard 95% line / 90% branch floor per the user-locked tightening edit B per [[feedback_verify_promotion_meets_floor_at_promotion_time]] — the gate runs against fresh `--cov-branch coverage.json` in the Z1c commit; the 2026-05-28 run found `sandbox/projection.py` at 100% line / 100% branch on fresh data (`tests/unit/sandbox/test_projection.py`, 39 tests), so no same-commit negative-path repair was required. Closed-enum growth: `SandboxRefusalReason` 36 (T16 +9 credential-projection values; the 4 planner-owned values listed below are the wire-equal `ProjectionRefusalReason` subset) + `SandboxLifecycleEvent` 19 (T17 +4: `credentials_projected` / `credentials_projection_failed` / `credentials_projection_cleaned_up` / `credentials_projection_cleanup_failed`).

- `sandbox/projection.py` (per ADR-004 §25 amendment + Sprint-10.6 spec §5.4 + Z1c promotion — `sandbox/` stop-rule isolation boundary. The per-credential pure-functional planner `compute_projection_plan(*, lease, manifest_decl)` at `sandbox/projection.py:323` (keyword-only; NO `resolved_workload_gid` input — the backend executors own chgrp (Docker) / fsGroup (K8s) per the spec §5.4 planner/executor split). Owns the `CredentialDecl` manifest-declaration dataclass at `:126` (manifest-side input), the `ProjectionPlanEntry` at `:160` + `ProjectionPlan` at `:178` success types, and the frozen `ProjectionRefused` at `:205` carrying the closed-enum `reason: ProjectionRefusalReason` at `:230` — a wire-equal subset of `SandboxRefusalReason` (no independent enum) with 4 values at `:113-116` (`sandbox_credential_projection_field_set_mismatch` + `…_field_value_non_string` + `…_field_value_empty_string` + `…_field_value_size_exceeded`) plus per-reason non-None diagnostics (field_set_mismatch → `expected_fields`/`actual_fields`; field_value_* → `field_name` + `actual_type`/`actual_length`/`actual_size`) surfaced on the `credentials_projection_failed` chain row per spec §5.7. The planner is the substantive credential-projection decision point: a bug here lets malformed credential material reach the backend executor (Docker bind-mount / K8s Secret), so it rides the durable gate, NOT thin wiring. Live end-to-end proof is DEFERRED — the Z3 (Docker) + Z4 (K8s) integration proofs at `tests/integration/sandbox/test_z{3,4}_*_credential_projection.py` are env-gated + run only in the operator's pre-merge audit; the Z1c promotion is a coverage-gate concern, NOT a live-proof claim.)

*Managed-run executor (Sprint 14A-A):*

ADR-022 + ADR-004 managed-run executor — the first production-grade EXERCISED managed-run path (`submit → scheduler admit → execute one sandbox-backed task → complete`), closing the 13.7 "constructed but no caller" gap for the scheduler's synchronous-run lane. `core/` stop-rule per AGENTS.md L48. Sprint 14A-A T1 promotes 1 module to the durable per-file critical-controls coverage gate (129 → 130) at the standard 95% line / 90% branch floor per [[feedback_verify_promotion_meets_floor_at_promotion_time]] — `core/run/executor.py` at 100% line / 100% branch on fresh `--cov-branch coverage.json` in the T1 commit, full 130-gate verified end-to-end. Closed vocabularies: `RunRefusalReason` 4-value Literal (`pack_record_not_found` / `pack_record_tenant_mismatch` / `pack_record_pack_id_mismatch` / `pack_record_not_installed`) + `RunTerminalState` 4-value (`completed`/`failed`/`refused`/`pending_approval` — the 4th added at 14A-A2) + `RunFailedReason` 2-value (`sandbox_create_refused`/`workload_runtime_error`) + the 14A-A2 `RunRBACScope` 1-value (`run.submit`).

- `core/run/executor.py` (per ADR-022 + ADR-004 + Sprint-14A-A T1 promotion — `core/` stop-rule per AGENTS.md L48. The first EXERCISED managed-run authority. `ManagedRunExecutor.run(RunRequest) -> RunResult` is the synchronous run loop: (0) load + validate the trusted pack record via the `PackRecordLoader` seam (four fail-closed pre-submit checks — exist / tenant-match / pack_id-match / `state=="installed"`, the last executor-side defence in depth duplicating the scheduler's `pack_state_interrogator`); (1) `submit` (projecting the core-owned `TaskActor`); (2) `mark_running` (NO `sandbox_adapter` — Fork A: the executor owns the session; the scheduler's `SandboxAdapter` seam returns no handle); (3) `backend.create(policy, actor=, tenant_id=, pack_context=, requires_credentials=())`; (4) `session.exec(list(argv))`; (5) `session.destroy()` (finally-guarded best-effort); (6) `complete`; (7) emit value-free `run.completed`. **Admission contract**: `accepted_immediate` runs; `accepted_queued` is cancelled via `scheduler.cancel(reason="actor_cancelled")` (pending → cancelled removes from the FIFO queue + releases the quota reservation + writes `scheduler.task_cancelled`) + refused `run_admission_queued_unsupported` (the synchronous executor has no worker to promote a queued task — running it would race `SchedulerPromotionRefused` + leak the task/reservation); `refused_*` surfaces as `run.refused`. **Failure semantics**: non-zero `exit_code` → `complete` (a run result, not a scheduler failure); `backend.create`/`session.exec` exception → `scheduler.fail` (`TaskFailedPayload` `sandbox_create_refused` / `workload_runtime_error`) + finally teardown. **Value-free evidence**: `run.completed`/`run.failed`/`run.refused` chain rows via `DecisionHistoryStore.append` — separate `stdout_sha256`/`stderr_sha256` + byte counts + `exit_code`, NEVER raw stdout/stderr (which return only on the caller's `RunResult`). **Layering**: owns the `PackRecordLoader` Protocol + the core-owned `LoadedPackRecord` projection — NO `cognic_agentos.packs` import (the conformer lives off-gate in `harness/sandbox.py`); NO runtime `cognic_agentos.portal` import (the `Actor` reference is TYPE_CHECKING-only — projected to `TaskActor` for the scheduler, passed through to `backend.create`); NO SDK import. AST-fenced at `tests/unit/architecture/test_run_no_sdk_import.py` (expected-sources + no-SDK + no-runtime-portal + no-packs). 12 orchestration tests over a REAL scheduler + REAL `DecisionHistoryStore` (in-memory sqlite) + stub backend/loader at `tests/unit/core/run/test_executor.py`; env-gated real-docker e2e at `tests/integration/run/test_managed_run_e2e.py`.)
- OFF the durable gate (off-gate composition, mirrors the 13.7/13.8 builders): `harness/sandbox.py` (the managed-run composition site — `is_sandbox_available()` DockerSibling-only + `build_sandbox_backend()` function-local SDK imports minting `VaultTransport` → `VaultCredentialAdapter` + the `PackRecordStoreLoader` conformer doing the direct UUID-keyed `PackRecordStore.load`; the substantive run-authority enforcement lives in the on-gate `core/run/executor.py` consumer + the on-gate sandbox backend). `core/config.py` (`sandbox_runtime_enabled` + `sandbox_policy_bundle` settings) + `portal/api/app.py` (SDK-gated lifespan construction of `app.state.sandbox_backend` + `app.state.managed_run_executor`, fail-soft, owned-client close on shutdown) stay off-gate. WIRED-DORMANT at 14A-A; **14A-A2 lands the `POST /api/v1/runs` route** — the first LIVE consumer of `app.state.managed_run_executor` (see the 14A-A2 extension below). Backend-level checkpoint→wake was deferred at 14A-A (wired into the managed-run lane at A3b/A3c).

*Managed-run portal route + sandbox approval threading (Sprint 14A-A2):*

Sprint 14A-A2 lands `POST /api/v1/runs` (the production caller — LIVE-exercises the executor + the scheduler approval seam) and WIRES the 13.5c1 sandbox approval seam for **cold-create only**. CC count stays **130** (no new gate module). On-gate edits: `core/run/executor.py` + `sandbox/protocol.py` + both concrete backends + `portal/rbac/{scopes,actor,enforcement}.py`. Off-gate: `portal/api/runs/{dto,routes}.py` + `harness/sandbox.py` + `portal/api/app.py`.

- `core/run/executor.py` (CC — pending-approval contract). `RunRequest` gains `approval_request_id: uuid.UUID | None`; `RunResult.terminal_state` gains `pending_approval` (+ `approval_request_id: str | None`). At `backend.create` the executor catches `SandboxLifecycleRefused` via a FUNCTION-LOCAL import (kernel-boot-clean — pinned by `test_core_run_no_module_level_sandbox_import` + the hvac subprocess probe) and maps per the F3 status map: `sandbox_approval_pending` → cancel + `run.pending_approval` + **202**; any OTHER `SandboxLifecycleRefused` (governance/admission refusal) → cancel + `run.refused` + **409**; generic `create()` OR any `exec()` exception → `scheduler.fail` + `run.failed` + **502**; `completed` (incl. non-zero exit) → **200**. It threads `request.approval_request_id` into `backend.create`.
- `portal/api/runs/{dto,routes}.py` (off-gate — the route). `build_run_routes()` mounted UNCONDITIONALLY (eval-router pattern) under `/api/v1/runs`; request-time `_require_managed_run_executor` dep → **503 `sandbox_runtime_unavailable`** when the lifespan did not populate the executor. `RequireScope("run.submit")`; NO `RequireHumanActor` (the sandbox approval seam owns the human checkpoint). `RunSubmitRequest` (`extra="forbid"`: `{pack_id, pack_uuid, pack_version, argv, approval_request_id?}`; tenant + actor from the bound `Actor` only; `argv` non-empty + bounded). `RunResponse` base64-encodes raw stdout/stderr + byte counts. `from __future__ import annotations` OMITTED (FastAPI `Annotated[..., Depends(...)]` invariant).
- `sandbox/protocol.py` + both backends (CC — approval threading, COLD-CREATE ONLY). `SandboxBackend.create` Protocol gains `approval_request_id: uuid.UUID | None = None` (matches `admit_policy`); `DockerSiblingSandboxBackend` + `KubernetesPodSandboxBackend` gain `__init__(approval_engine: ApprovalEngine | None = None)` + `self._approval_engine` + the cold-create `admit_policy` threading (cross-backend lockstep, pinned by `tests/unit/sandbox/backends/test_approval_threading.py`). 14A-A2 left the **wake-path** `admit_policy` calls (docker ~`:2559`, k8s ~`:2429`) UNCHANGED (checkpoint→wake deferred at 14A-A2); A3c T2 later threaded them (see the Sprint 14A-A3c extension below — `test_approval_threading.py` now pins the wake threading too). `harness/sandbox.py` (off-gate) threads `approval_engine=runtime.approval_engine` into `get_backend`.
- `portal/rbac/{scopes,actor,enforcement}.py` (CC — `run.submit` scope). `RunRBACScope = Literal["run.submit"]` + `RUN_SCOPES`; `Actor.scopes` + the `RequireScope` param union widened with `| RunRBACScope` (additive, `run.*` namespace-disjoint; pinned by `tests/unit/portal/rbac/test_run_scopes.py`).
- Posture honesty. The sandbox approval seam is LIVE **for cold-create only** and **only when** `sandbox_runtime_enabled` is enabled AND the backend is constructed through `build_sandbox_backend` (else the pre-13.5 Rego fallback stands). MCP `call_tool` and the real `LocalParentBudgetResolver` / sub-agent dispatch remain forward items (the wake approval correlator + run-level checkpoint→wake were superseded by A3b/A3c). The run route LIVE-exercises the executor + (via `scheduler.submit`) the scheduler approval seam; for the `read_only` run shape the scheduler seam is consulted on the auto-tier path (no high-risk pending).

*Run-persistence foundation (Sprint 14A-A3a):*

Sprint 14A-A3a ships the durable run-record substrate the deferred checkpoint→wake arc depends on (split A3a foundation → A3b resolver/resume → A3c wake approval correlator). STORE-ONLY / DORMANT — not wired into the executor or the route (A3b wires it); proven by tests. CC count **130 → 131** — `core/run/storage.py` is the ONLY newly on-gate module.

- `core/run/storage.py` (CC — per ADR-022 + ADR-004. The durable run-lifecycle tenant-isolation + chain-atomicity boundary; mirrors `core/scheduler/storage.py` + `packs/storage.py`. `RunRecordStore`: `create_run` (genesis: INSERT `pending` + append `run.lifecycle.pending`) + `transition` (SELECT … FOR UPDATE tenant-scoped row → `validate_transition` → UPDATE state + optional nullable columns → append `run.lifecycle.<to_state>`) drive `DecisionHistoryStore.append_with_precondition` (atomic chain row + state-cache UPDATE under one transaction, Doctrine Lock D; refusal rolls back). Two preflight guards (known transition target; reserved-payload-key overlap). Tenant isolation: `load`/`list_for_tenant`/the transition SELECT are tenant-scoped — a cross-tenant `run_id` reads as absent (`None` / `RunNotFound`); `list_for_tenant` paginates by `run_id` keyset cursor. The `runs` Table: `run_id` UUID PK, `tenant_id` (NOT NULL boundary), pack identity, nullable `task_id`/`session_id`/`checkpoint_id` (**`String(32)`** — the sandbox `CheckpointId` hex, NOT a Uuid)/`approval_request_id` (Uuid), `state`, timestamps; `ix_runs_tenant_state` index. Migration `0011`; drift pinned by `tests/unit/db/test_migration_20260615_0011.py`. The store emits `run.lifecycle.<state>` events — DISTINCT from the executor's existing direct `run.<terminal>` output-evidence rows (A3b decides whether to reconcile). Store-only/dormant; 100/100 at promotion.)
- OFF the durable gate (mirrors `core/scheduler/_types.py`): `core/run/_types.py` (`RunState` 9-value closed enum + `validate_transition` (the 6-pair A3a subset) + `RunRecord` + `RunTransitionRefused` — pure types + pure-functional validator; closed-enum + state-machine drift detectors at `tests/unit/core/run/test_run_types.py`). **LOCKED doctrine:** the 9-value `RunState` vocabulary is FIXED at A3a; A3b/A3c may only EXPAND the legal-transition matrix (add suspend/wake/cancel pairs), NEVER the stored vocabulary — pinned by `test_reserved_pairs_refuse_until_expanded`. The migration `20260615_0011_runs.py` stays off-gate (run-once DDL, Doctrine F).

**Sprint 14A-A3b extension (run→session resolver + resume route — no new on-gate module; CC count stays 131).** A3b wires the A3a substrate into the executor + adds the suspend/resume lane. No NEW gate module — the edits land in modules ALREADY on the gate (`core/run/executor.py`, `core/run/storage.py`, `portal/rbac/scopes.py`); `core/run/_types.py` + the dto/routes stay off-gate.
- `core/run/executor.py` (CC). The executor now DRIVES the run record (the A3a "store-only/dormant" posture is retired for the executor): every `run()` mints a `run_id` + a genesis `run.lifecycle.pending` and threads a record transition into EVERY terminal path (`RunResult.run_id` is now the first field). New `RunRequest.suspend_after_exec: bool` + `RunTerminalState +suspended`. **Suspend path:** `exec → session.suspend() → skip_destroy=True (set IMMEDIATELY, before any further await — once suspend() returns the session is gone) → checkpoint_store.load_latest(session_id, tenant_id) → transition running→suspended persisting session_id + checkpoint_id → scheduler.complete → run.suspended`. **Conditional teardown:** the `finally` destroys the session ONLY `if not skip_destroy` — a suspended session is NEVER torn down (destroy would tombstone it via `CheckpointStore.tombstone_session()`, making the checkpoint unwakable). New `resume(*, run_id, actor, argv)` — resolves `run_id → session_id` (tenant-scoped `load`; cross-tenant reads as absent → `RunNotFound`/404), `backend.wake(session_id, ...)` (the FIRST production caller of `backend.wake()`), runs a continuation `argv`, walks the record `suspended → woken → completed` (or `→ refused`/`→ failed`). **resume makes NO scheduler calls** (no re-admission, no quota reservation — the slot was freed at suspend; `task_id` is always None on resume). **Claim-gated teardown (the wake/tombstone race fix):** the woken session is destroyed in the `finally` ONLY after the atomic `suspended→woken` claim commits (`claimed_woken=True`); a concurrent loser (`RunTransitionRefused → RunResumeConflict`, 409) OR a non-stale DB error during the claim leaves `claimed_woken=False` and NEVER destroys the session (destroying would tombstone a session the winner is executing / a run still suspended+resumable). `RunNotResumable` (409 `run_not_suspended` + `current_state`) when the record isn't suspended.
- `core/run/storage.py` (CC). `_STATE_TO_DECISION_TYPE` gained `suspended`→`run.lifecycle.suspended` + `woken`→`run.lifecycle.woken` (`cancelled` stays reserved). `core/run/_types.py` (off-gate) expanded the transition matrix with `_A3B_VALID_TRANSITIONS` (+6 producible pairs: `running→suspended`, `suspended→{woken,refused,failed}`, `woken→{completed,failed}`); the 9-value `RunState` vocabulary is UNCHANGED (expand the matrix, never the vocab — the A3a locked doctrine).
- `portal/rbac/scopes.py` (CC). `RunRBACScope` gained `run.resume` (`Literal["run.submit", "run.resume"]`; `RUN_SCOPES` 1→2; additive, `run.*` namespace-disjoint).
- `portal/api/runs/{dto,routes}.py` (off-gate). `POST /api/v1/runs/{run_id}/resume` (the first production caller of `executor.resume()` AND — via the executor — of `backend.wake()`); `RequireScope("run.resume")`; the shared `_run_response_from_result` projector (`RunResponse.run_id`); the 3 pre-flight mappings (`RunNotFound→404`, `RunNotResumable→409 run_not_suspended`+`current_state`, `RunResumeConflict→409 run_resume_conflict`). `suspended → 202`. The resume route unblocks the deferred `resume` UI action (`portal/api/ui/action_routes.py`'s `action_backend_deferred_sandbox_unwired` stub — now has a run→session resolver behind it).
- `run.lifecycle.*` (store) stays DISTINCT from the executor's direct `run.*` (output evidence). **Forward items (still pending beyond A3c):** (1) **leaked orphaned backend resource** — on a resume claim-failure / concurrent-loser the woken backend container/pod is deliberately NOT destroyed (to avoid tombstoning the session) → it is a LIVE orphaned resource; the `CheckpointReaper` (`sandbox/reaper.py` → `CheckpointStore.purge_expired()`) purges only object-store checkpoint/tombstone artifacts, NOT live containers/pods, so this is NOT auto-reclaimed today (a resource leak, not data loss / not a tombstone) — cleanup/reconciliation is a forward item; (2) **quota/scheduler-on-resume** — resume makes NO scheduler calls and takes NO quota reservation (the slot was freed at suspend); the wake still reruns the sandbox `admit_policy` per Q3, but no scheduler/quota gate is re-entered — a deliberate forward item.

**Sprint 14A-A3c extension (wake-approval correlator — the A3c fence A3b held is now CROSSED; no new on-gate module; CC count stays 131).** A3c is the third + final WIRING slice of the checkpoint→wake arc (A3a substrate → A3b resolver/resume → **A3c wake approval correlator**). It threads the approval engine into the wake-path `admit_policy` (the cold-create mirror) + gives the executor's `resume()` a run-record wake-pending lifecycle. No NEW gate module — the edits land in modules ALREADY on the gate (`sandbox/protocol.py`, both backends, `core/run/executor.py`); `core/run/_types.py` + the dto/routes stay off-gate; `admission.py` / `checkpoint_store.py` / `storage.py` are consumed, not edited.
- `sandbox/protocol.py` + both backends (CC, byte-identical cross-backend). `wake()` Protocol gains the additive keyword-only `approval_request_id`; both backends thread `approval_engine=self._approval_engine` + `approval_request_id` into the wake-revalidation `admit_policy` call (mirroring the 14A-A2 cold-create wiring) AND the wake refusal-collapse wrapper now EXEMPTS the approval family: A3c adds `_APPROVAL_WAKE_PASSTHROUGH_REASONS` (the closed 5-reason set `sandbox_approval_{pending,denied,expired,binding_mismatch,request_not_found}`, single source of truth, no per-backend drift) and `if original.reason in _APPROVAL_WAKE_PASSTHROUGH_REASONS: raise` re-raises un-rewrapped, so `sandbox_approval_pending` + its `approval_request_id` reach the executor intact; only NON-approval revalidation refusals still collapse to `sandbox_wake_policy_revalidation_failed`. `admit_policy` is UNCHANGED — already approval-aware (`_consult_approval_engine` consults the engine only when `approval_engine is not None`); A3c just threads it on the wake path A3b left UNCHANGED. Pinned by `tests/unit/sandbox/backends/test_approval_threading.py`. The wake path is the 5th `admit_policy` approval consumer (after cold-create); NO new `SandboxRefusalReason` value (the 13.5c1 family is reused).
- `core/run/executor.py` (CC). `resume()` gains keyword-only `approval_request_id`; the resumable-state guard widens to `record.state ∈ {suspended, pending_approval}` + `from_state = record.state` (the 3 A3b hardcoded `"suspended"` transitions converted). **The pending arm:** on a `sandbox_approval_pending` wake, first-pending (from `suspended`) transitions `suspended → pending_approval` + stores the minted `approval_request_id` on the run row + emits `run.pending_approval` + returns terminal `pending_approval` (route → 202); still-pending (already `pending_approval`) emits NO transition (`pending_approval → pending_approval` is not a legal pair — a self-loop would be misleading evidence). **The no-re-mint guard (F2 pin):** a `pending_approval` re-resume MUST supply its stored `approval_request_id` — missing → `RunResumePendingApprovalRequired` / 409 `run_resume_approval_id_required`; mismatched → `RunResumeApprovalMismatch` / 409 `run_resume_approval_id_mismatch`; BOTH raised BEFORE `wake()` so admission Arm A (mint-fresh-request) is unreachable on a re-resume (no silent new-pending loop). resume STILL makes NO scheduler calls (quota/scheduler-on-resume = forward item); the claim-gated teardown is UNCHANGED — the pending + refused arms RETURN before the `suspended → woken` claim, so they own nothing and the `finally` never destroys.
- `core/run/_types.py` (off-gate). Folded `_A3C_VALID_TRANSITIONS` into the 3-way `_VALID_TRANSITIONS` union (+4 producible pairs: `suspended→pending_approval`, `pending_approval→{woken,refused,failed}`). The 9-value `RunState` vocabulary is UNCHANGED — A3c expands the matrix, never the vocab (the A3a locked doctrine, honored across all three slices).
- `portal/api/runs/{dto,routes}.py` (off-gate). `RunResumeRequest.approval_request_id`; `resume_run` threads it into `executor.resume(...)` + maps the 2 new refusals (409). The `pending_approval → 202` (`_STATUS_BY_TERMINAL`) + the `approval_request_id`-carrying `_run_response_from_result` retry contract is AUTOMATIC (pre-existed at A3a/A3b — 202 → grant → re-POST with `approval_request_id`).
- **NO `CheckpointMetadata` change (F1).** The wake grant binds `args_digest` over the already-persisted immutable `metadata.policy` / `metadata.pack_context`; `approval_request_id` rides the resume request (run row stores it); `approval_verified` is the transient per-admission attestation. `CheckpointMetadata`'s wire shape is UNTOUCHED (no schema change, no migration). **High-risk live exercise DEFERRED (F4):** the seam is WIRED + unit-proven (+ an env-gated stub-engine real-docker e2e at `tests/integration/run/test_managed_run_resume_approval_e2e.py`, `COGNIC_RUN_DOCKER_SANDBOX=1`), but the production run route submits the `read_only` shape, which auto-tiers under a real `ApprovalEngine` and never pends — so the wake-approval seam was wired-but-not-live-exercised-in-production for `read_only` (the 14A-A2 cold-create posture) until Sprint 14A-A4b; the manifest-driven high-risk run shape **lands at Sprint 14A-A4b (below)**, which live-exercises the COLD-CREATE approval seam. **The checkpoint→wake arc's WIRING is complete** (resolver + resume route in A3b; wake approval correlation in A3c); the remaining items (orphaned-backend-resource reconciliation, quota-on-resume) are independent forward tracks, NOT arc dependencies. **Sprint 14A-A4c then live-exercised the high-risk suspend→resume/WAKE path (the env-gated two-cycle e2e), closing F4 ENTIRELY — high-risk cold-create AND wake are now both live-exercised.**

**Sprint 14A-A4b extension (manifest-driven high-risk run — A4a ACTIVATED; no new on-gate module; CC count stays 131).** A4b is the **activator**: the executor sources a pack's validated manifest risk tier + sets the A4a `approval_delegated_to` signal, so the dormant A4a scheduler arm 3 + the 13.5c1 sandbox cold-create approval seam are now LIVE-exercised for high-risk runs (closing the A3c F4 caveat for cold-create). No NEW gate module — the edits land in `core/run/executor.py` (already on the gate); the manifest loader is off-gate (`harness/sandbox.py`); **no migration** (the tier is read off the existing `pack.lifecycle.submitted` chain row).
- `core/run/executor.py` (CC). **The vocab + fail-closed gate (I1):** local `RiskTier` Literal (the ADR-014 canonical 8, drift-pinned test-only against `cli._governance_vocab.RiskTier` since the `core/run → cli` arrow is now AST-fenced at `tests/unit/architecture/test_run_no_sdk_import.py::test_core_run_no_cli_import_at_all` — proven load-bearing) + `_CANONICAL_RISK_TIERS`; `RunRefusalReason` 4 → 6 (`pack_record_risk_tier_unresolved` + `pack_record_data_classes_malformed`); `LoadedPackRecord` +`risk_tier`/`data_classes`; `_validate_pack_record` +2 checks (tier FIRST, after `installed`) — an unresolved/non-canonical tier OR a present-but-malformed `data_classes` (the loader `None` sentinel) REFUSES, NEVER downgrades to `read_only` (the I1 invariant; `data_classes=()` absent/empty is legitimate + proceeds). **The flip (the activation):** the SubmitInput build threads `pack_risk_tier=record.risk_tier` + `data_classes=record.data_classes or ()` + `approval_delegated_to="sandbox_admission"` (activates A4a arm 3); `_build_pack_context` threads `risk_tier=cast(RiskTier, record.risk_tier)` + `data_classes` into the sandbox `PackAdmissionContext` — the SAME validated tier to BOTH gates (the A4a §3.8 setter obligation discharged by construction). A post-validation `assert record.risk_tier is not None` narrows `str | None → str` for the SubmitInput (coverage-safe — coverage.py does not count assert raise-arcs as branches). Pinned by the threading test (asserts BOTH the sandbox ctx + the `scheduler.admission_accepted` row's `pack_risk_tier`/`approval_delegated_to`) + the `@opa_required` real-`scheduler.rego` arm-3 admission guard.
- `harness/sandbox.py` (off-gate). `PackRecordStoreLoader.load_for_run` reads the manifest off `find_latest_submit_row(load_lifecycle_history(pack_uuid))` (`payload["manifest"]`) — shape-extract ONLY: `risk_tier` = `str | None` (None = unresolved/absent/non-string); `data_classes` = `()` (absent/empty) / `(str, ...)` (valid) / `None` (present-but-malformed sentinel the executor refuses on). The fail-closed GOVERNANCE decision is the on-gate executor's (the loader is a thin shape extractor; `core/run` cannot import `packs`, so the manifest read lives here).
- `tests/integration/run/test_managed_run_high_risk_e2e.py` (env-gated, `COGNIC_RUN_DOCKER_SANDBOX=1`). The F4-closing proof: a REAL `ApprovalEngine` (no conformer — `customer_data_read` → `require_single_approval` pends naturally) drives cold-create pend → real `grant()` (distinct human, `tool.approve.customer_data` scope) → re-POST → complete. Catalog cosign + sandbox-admission Rego stubbed allow; the approval engine's `tools.rego` classification is REAL.

**Sprint 14A-A4c extension (high-risk WAKE live exercise — e2e-only; no new on-gate module; CC count stays 131; no migration).** A4b live-exercised the high-risk COLD-CREATE approval seam; A4c live-exercises the high-risk **suspend→resume/WAKE** path, closing the A3c F4 caveat ENTIRELY (high-risk cold-create AND wake both live). **Recon-proven e2e-only — ZERO production code** (A3b/A3c wired the suspend/resume/wake-pending lifecycle + the no-re-mint guard; A4b persists the high-risk `pack_context` into the checkpoint at suspend, so the wake re-runs `admit_policy` against the persisted high-risk context and Arm A re-pends naturally).
- `tests/integration/run/test_managed_run_high_risk_wake_e2e.py` (env-gated, `COGNIC_RUN_DOCKER_SANDBOX=1`). A REAL `ApprovalEngine` (no conformer) walks the TWO-cycle high-risk path: cold-create pend → `grant(id1)` → re-POST (`suspend_after_exec=True`) → `suspended`; `resume` → wake pend → `grant(id2)` → re-resume → `completed`. Asserts the durable run's full 6-state lifecycle walk (`pending → running → suspended → pending_approval → woken → completed`) + the `run.suspended` / `run.pending_approval` / `run.completed` evidence rows. The two-`run_id` shape (the cold-create-pend run abandoned; the re-POST run suspends + resumes) carries from A4b. The wake is `admit_policy`'s 5th approval consumer (A3c) — now live-exercised for high-risk.

*Isolation boundaries:*
- `sandbox/` (isolation boundary, including `checkpoint/suspend/wake` audit-chain integrity per ADR-004)
- `subagent/` (privilege de-escalation boundary)

*Model + data governance:*
- `models/registry.py` (per ADR-013 — pure-functional lifecycle state machine; closed-enum 12-value `ModelLifecycleRefusalReason` per spec §2.1; 5-value `MODEL_LIFECYCLE_ISO_CONTROLS` stamped on every `model.lifecycle.*` chain row per spec §4.2). Sprint 9.5 Z1.
- `models/storage.py` (per ADR-013 — Postgres/Oracle `ModelRecordStore`; `DecisionHistoryStore.append_with_precondition` consumer driving register/promote/retire; A6.0 `_lifecycle_payload` carries the 17-field immutable evidence snapshot per the chain-payload-is-evidence-snapshot doctrine — `signature_digest` / `eval_results_ref` / lineage fields land ON the chain row, NOT just on the mutable table column; A5 read methods including `list_for_tenant(tenant_id, *, limit, cursor, state)` whose `WHERE tenant_id` IS the cross-tenant boundary). Sprint 9.5 Z1.
- `models/trust.py` (per ADR-013 — cosign artefact verification gate; same subprocess discipline as `protocol/trust_gate.py` — list-form argv, no shell, frozen 2-key env, asyncio timeout + SIGKILL + reap, exit-code-only verdict; bundle-only `cosign verify-blob` argv shape verified end-to-end at the target cosign version by the Sprint 9.5 Z2 real-cosign two-layer proof at `tests/integration/models/test_real_cosign_proof.py`; the `sigstore_bundle_digest` helper is consumed by the route's recompute-before-cosign evidence-integrity gate). Sprint 9.5 Z1.
- `portal/api/models/lifecycle_routes.py` (per ADR-013 — register/promote/retire route module; owns the 7-value closed-enum guard set on `_resolve_under_tenant_root` per the resolve-then-validate-path-containment doctrine + the body-aware promote-scope resolution + the state-aware human gates). Standing-offer §30: `from __future__ import annotations` INTENTIONALLY OMITTED so FastAPI can resolve `Annotated[..., Depends(<closure-local>)]` eagerly. Sprint 9.5 Z1.
- `packs/evidence/data_governance.py` + the manifest-driven DLP enforcement runtime (per ADR-017)
- `core/memory/` (per ADR-019 — what an agent may remember/forget/export/redact/reuse across sessions)

*LLM gateway:*
- `llm/gateway.py` (cloud-policy enforcer; provider-honesty ledger feed)

Use `core-controls-engineer` and `/critical-module-mode` when working on these.

## Production-grade implementation rule

AgentOS is built as a production-grade system. The product should be deployable largely as implemented, not rewritten later.

Rules:
- Do not implement mock, fake, placeholder, or synthetic behavior in the main runtime path.
- Do not replace real integrations with mock generators just because CI or local setup is harder.
- If an external dependency is difficult to use in CI, implement the real integration for runtime and use fixtures or recorded responses only in tests.
- Test-only mocks, fixtures, and demo-safe sample data are allowed only under clearly separated test/demo paths.
- Production code paths must remain real, swappable, and deployable.

Plugin stubs (e.g. `protocol/mcp_host.MCPHost.call_tool`) that raise `NotImplementedError` referencing an ADR are explicit scaffolding, not mocks — they fail loudly when called, document the contract, and protect against silent fallback.

**Deployment substrate (Sprint 14B-Z1a, ADR-024).** The kernel ships an OpenShift-compatible **Helm chart** at `infra/charts/agentos/` that packages the existing `default-adapters` prod image (`create_prod_app`) — validated by an always-on Helm-4 lint/template/kubeconform/byte-snapshot-drift CI gate + a pinned Helm-3 compatibility lane, plus an env-gated `kind` Ready-smoke against six real credential-free backends. **Helm is the only in-repo manifest source** — banks needing Kustomize render `helm template` and overlay in their own repos (the OS / bank-overlay boundary above). Z1a is pure additive infra-as-code: **CC count stays 131, no kernel change, no migration** (the only Python added is the rendered-YAML snapshot test); AKS/cloud bring-up, external-secrets depth, Ingress/Route + TLS, and observability wiring are deferred to Z1b. **Sprint 14B-Z1b-a** then added the three conditional, default-off external-access/scrape templates (`ingress.yaml` / `route.yaml` / `servicemonitor.yaml`) + a four-scenario byte-snapshot + kubeconform CI gate (Route's CRD schema is absent from the `datreeio/CRDs-catalog`, so the gate uses the scoped `-skip Route` fallback) — still **CC 131, no kernel change, no migration**; the live cloud/ingress exercise is Z1b-d. **Sprint 14B-Z1b-b** then added the conditional, default-off ESO `ExternalSecret` template (`externalsecret.yaml`, `external-secrets.io/v1`, populating the existing 2-key bootstrap Secret from an operator-owned `SecretStore`/`ClusterSecretStore` the chart never creates) + a 3-mode mutually-exclusive secret source (`secrets.create` XOR `secrets.existingSecret` XOR `externalSecrets.enabled`, enforced by the `agentos.validateSecretSource` Helm `fail` + a root-level schema `allOf`) + a fifth snapshot/kubeconform scenario (ExternalSecret is schema-validated; the gate keeps the narrow `-skip Route`) — still **CC 131, no kernel change, no migration**; the live cloud/ESO exercise is Z1b-d. **Sprint 14B-Z1b-c** then added the generic OTLP exporter primitive (`observability/otel.py` `_build_otlp_exporter` branches `grpc`/`http` + threads `headers` into both; http reuses the mTLS triple as file-path kwargs, `insecure` is gRPC-only) behind two new `core/config.py` Settings (`otel_exporter_protocol` + `otel_exporter_headers`) + the endpoint-gated chart wiring (`values.otel.exporter.{endpoint,protocol,insecure,headersSecretKey}`; the Basic-auth header rides a `secretKeyRef` passthrough, `existingSecret`-only) + a sixth `otel-http` snapshot scenario (core kinds — no CRD change) — **CC 131, no migration, no new dependency**; `core/config.py` is a `core/` stop-rule edit (the off-coverage-gate kernel change carried halt-before-commit scrutiny). The standalone env-gated Langfuse ingestion proof (`COGNIC_RUN_LANGFUSE_OTEL=1`) ships in Z1b-c; the live cloud/cluster exercise (AKS + the chart path in-cluster, incl. ServiceMonitor → Prometheus) is Z1b-d. **Sprint 14B-Z1b-d-1** then added the two **generic, cloud-agnostic** chart workload-identity hooks — `serviceAccount.annotations` (→ the SA's `metadata.annotations`) + `podLabels` (→ the Deployment's pod **template** labels `.spec.template.metadata.labels` ONLY, **never** `.spec.selector.matchLabels`: the selector-stability invariant that keeps `helm upgrade` working) — so the chart SA can federate to any cloud IAM identity (Azure WI / GKE WI / AWS IRSA, named only in the runbook examples + one Azure-WI test fixture; the chart values/schema stay plain `type: object` maps), default-empty → render nothing (the default + 6 existing snapshots byte-UNCHANGED), validated by a 7th `workload-identity` snapshot (core kinds — ServiceAccount + Deployment — no CRD change) — still **CC 131, no kernel change, no migration**; the live AKS exercise is Z1b-d-2. **Sprint 14B-Z1b-d-2** then shipped the live-cloud capstone — an always-on 8th `all-surfaces` byte-snapshot scenario (every Z1b conditional surface in one dedicated ESO-mode render; `-skip Route` unchanged) + operator-run reference Bicep (`infra/azure/aks-smoke/main.bicep`: AKS OIDC+WI, UAMI, empty Key Vault, federated credential, KV roles) + a self-contained env-gated AKS smoke (`run-aks-smoke.sh`: ESO-from-Key-Vault + workload identity + Ready; namespace-pinned to the Bicep `agentosNamespace`; migrations-off + a post-gate non-hook migration Job to avoid the pre-install-hook-vs-ESO-secret deadlock; an auxiliary `Merge` ExternalSecret for the OTLP header preserving the chart's fixed 2-key contract, behind a fail-loud 3-key gate with a first-class `ENABLE_OTLP=0` 2-key fallback). **No AKS CI job** (a deliberate security posture); still **CC 131, no kernel change, no migration**. With Z1b-d-2 the whole 14B Deployment Substrate (Z1a + Z1b-a/b/c/d-1/d-2) is **complete**.

## Code layers

The three-pool rule (tools / skills / agents) governs **agent internals** outside this repo. Inside this repo:

- **Platform primitive.** Deterministic system module — peers of `cognic_agentos.core.*`. Includes governance, persistence, observability, channels, RBAC, plugin registry, sandbox, subagent.
- **Persistence adapter.** Database / external-store implementation of a platform contract.
- **Portal surface.** HTTP endpoints + DTOs.
- **Protocol layer.** MCP host, A2A endpoint, plugin registry.
- **Compliance evidence.** ISO 42001 control mapping + audit emission.

All Layer A/B/C (tools/skills/agents) live in plugin pack repos, not here.

## Human-only decisions

Do not finalise:
- Threshold changes
- Production deployments
- Model promotions / rollbacks
- Compliance sign-off
- Release gates
- Incident severity
- Bank communications
- Certification commitments
- Plugin-pack trust-root rotation
- Per-tenant allow-list changes

## Compaction

When compacting or stopping, preserve:
- Current task / subsystem
- Files changed
- Tests run + results
- Open risks / blockers
- ADR status
- Whether governance, sandbox, sub-agent, plugin trust, RBAC, or wire protocol were touched
- Next concrete step
