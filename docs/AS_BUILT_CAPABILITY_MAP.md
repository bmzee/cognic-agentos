# AgentOS v1 — As-Built Capability Map

**Date:** 2026-06-12 · **Baseline:** `main @ 1af0e62` (post Sprint 13.5c3, PR #64) · **Status:** AUTHORITATIVE "what v1 is now" map
**Rule:** every claim below is grounded in repo evidence (source modules, ADR sections, tests, e2es). Nothing aspirational. Where a capability is named but unbuilt, its state says so plainly.
**Posture vocabulary:** `production-wired` (a deployed runtime reaches it through the composition root) · `seam-only` (built + test/DI-proven; no production caller constructs it yet) · `env-gated live proof` (real-infrastructure proof exists but is opt-in, run pre-merge by the operator) · `partial` · `missing` (named, sequenced, deliberately unscoped).

This is NOT a proof of concept. AgentOS v1 is a production-hardened, bank-deployable governance kernel + runtime: 128 modules ride the durable critical-controls coverage gate (95% line / 90% branch, re-verified on fresh `--cov-branch` data at every promotion), the full suite stands at ~10.4k tests / 0 failed, and CI runs live Postgres + Oracle integration lanes plus image-size budgets on every PR.

## Capability pillars

| # | Capability | Current state | Evidence | Remaining gap | Proposed sprint owner |
|---|---|---|---|---|---|
| 1 | **Governed Agent Memory** (Hermes-agent-like memory management, governed) | **production-wired** | `core/memory/` (api, gate, storage, tiers, consent, episodes, export, forget, redact, vector, reaper, `_digest`); ADR-019 + its 13.5c3 amendment; 10 ops (7 core + 3 block) over 3 tiers (`scratch`/`task`/`long_term`, default-deny long-term); write-time DLP (`core/dlp/scanner.py`), consent tokens, regulator-erasure pathway, vector recall (Qdrant), ADR-023 per-tenant overlay; high-tier `long_term` writes are approval-gated LIVE (`core/memory/gate.py` Step 7; `harness/runtime.py:225` threads `runtime.approval_engine` — identity-pinned in `tests/unit/harness/test_runtime.py`); portal surfaces list/forget/redact/export (`portal/api/memory/routes.py`); tests `tests/unit/core/memory/` (incl. 24-test approval seam) + e2e `tests/integration/approval/test_memory_seam_e2e.py` | Write ops deliberately not HTTP-exposed (Layer-C surface); Hermes-style *self-improvement promotion* stays gated behind `[tool.cognic.learning_surface]` + pack promotion (by design, ADR-019) | maintained |
| 2 | **Managed Agent Runtime** (Claude managed-agents-like long-running / asynchronous work) | **partial** (primitives built; runtime surface missing) | Scheduler primitive `core/scheduler/` (ADR-022: 2-class FIFO, caps, chain-row lifecycle, Rego admission, approval seam); sub-agent primitive `subagent/` (ADR-005: scheduler-mediated spawn, budget narrowing, privilege de-escalation — test/DI-proven per the 11b D1 memo, NO production dispatch path); sandbox resumable sessions `sandbox/checkpoint_store.py` (`checkpoint()/suspend()/wake()`, ADR-004); composition root `harness/runtime.py` (`build_runtime`: gateway + memory + approval trio LIVE; **scheduler app-wired (Sprint 13.7 — cache-conditional, all seam slots bound to real conformers; parent-budget `_Null` until 14A; exposed on `Runtime.scheduler` + `app.state.scheduler`, no caller)** composition-root-bound, dormant until 14A caller) | The managed-runtime surface itself: the scheduler submit→execute **caller** (the engine is app-wired at 13.7 but no production caller invokes it yet), live sub-agent dispatch, long-running/async task orchestration + resumption UX | **Sprint 14A — Managed Agent Runtime** |
| 3 | **Dynamic Workflow Orchestration substrate** | **missing** (named, deliberately unscoped) | Nearest substrate already built: scheduler admission + sub-agent spawn + A2A task lifecycle (`protocol/a2a_endpoint.py`) + UI event stream (ADR-020) | Everything — scoping is future brainstorming, not this reconciliation | **Sprint 15A — Workflow Orchestration** |
| 4 | **AgentOS ADK / SDK / CLI / local developer runtime** (Claude Code / Cowork-like local agent execution integrated with AgentOS) | **partial** (authoring loop built; local agent execution missing) | `agentos` CLI (ADR-008): `validate` (6 per-concern validators + shape gate), `sign` (cosign + SBOM + grype + license + JWS + SLSA + in-toto), `verify` (11-step offline trust gate + isolated-subprocess load probe), `test-harness`, `conformance`, `eval-bulk`; SDK `sdk/` (hook authoring); authoring docs `docs/HOW-TO-WRITE-A-PACK.md`, `docs/PACK-MANIFEST-SPEC.md`, `docs/SDK-REFERENCE.md` | The ADK proper: local dev runtime executing agents against a local AgentOS (Claude-Code-like loop), pack scaffolding/dev-cycle ergonomics | **Sprint 15B — AgentOS ADK / local runtime** |
| 5 | **Production Deployment Substrate** (OpenShift-compatible Kubernetes packaging; AKS first environment; operational runbooks) | **partial** | Dockerfile + CI boot-smoke proving the prod image refuses dev defaults (Wave-1 deploy-safety G1–G8, `2026-06-05` closeout); image-size budget CI lane (kernel ≤120 MiB); docker-compose stacks + Oracle/vLLM overlays (Sprints 1C/1D); `KubernetesPodSandboxBackend` with OpenShift-compatible SecurityContext + NetworkPolicy (ADR-004, Sprint 8B); `docs/operator-runbooks/` (e.g. hook-pack failure policy); env-gated live proofs for Vault, Docker/K8s credential projection (Z2/Z3/Z4) | Helm/Kustomize packaging of AgentOS itself; AKS first-environment bring-up + smoke; complete operator runbook set; release/evidence checklist | **Sprint 14B — Deployment Substrate Z1** (parallelizable after 13.7) + **Sprint 16 — runbooks/checklist** |
| 6 | **Runtime approval / policy-as-code / emergency controls** | approval: **engine production-grade, 1 of 4 seams LIVE**; policy: **production-wired**; emergency: **ADR-018 arc CLOSED — kill switches (13.6a) + quotas (13.6b)** | Approval: `core/approval/` (ADR-014 13.5a — non-blocking engine, value-free envelope, 4-eyes, lazy authoritative expiry, replay binding, 7 `tool.approve.*` scopes) + portal API (13.5b1: list/detail/grant/grant-second/deny; NO public create endpoint, by design) + four consumer seams — MCP host (13.5b2, seam-only), sandbox admission (13.5c1, seam-only), scheduler submit (13.5c2, seam-only), **memory write gate (13.5c3, production-wired)**; both `*_pre_13_5` Rego refusals CONVERTed (engine-absent fallbacks kept) and the build-time credentials-validator pre-13.5 refusal REMOVED (13.5c4 — approval arc CLOSED); e2es `tests/integration/approval/` (5 files: the four seam e2es `test_{mcp,sandbox,scheduler,memory}_seam_e2e.py` + `test_approval_api_e2e.py`). Policy: `core/policy/engine.py` (OPA) + 8 kernel bundles (`supply_chain`, `sampling`, `elicitation`, `sandbox`, `scheduler`, `memory`, `memory_purpose_matrix`, `tools`) — all stop-rule, drift-pinned. Emergency (ADR-018 — arc CLOSED): **kill switches 13.6a** — `core/emergency/kill_switches.py` `KillSwitchEngine` 8-class matrix, brake-before-evidence flip/revert (`emergency.kill_switch_*` chain + `kill_switch.*` UI), gateway F4 gate + memory `tenant_full` freeze production-wired, operator portal + `EmergencyRBACScope` 1→9; `enforcement_status` honesty (model/cloud/tenant_full/memory LIVE; pack/tenant_packs/tool/feature armed). **Quotas 13.6b** — `core/emergency/quotas.py` `QuotaEngine` (decomposed atomic Redis counters, NOT Lua; consumer-owned `_AsyncRedisQuotaLike`; deny-safe TTL-healing crash window), gateway tenant-aggregate actuals gate + `record_actuals` (production-wired), scheduler `QuotaInterrogator` conformance (DI-ready), `gateway_call_ledger` token/cost evidence cols + migration `0010`, read-only quota portal + `QuotaRBACScope`, env-gated real-Redis atomicity proof; CC 128→129 | Single-use grant `consume` (Sprint-14+, explicitly deferred); operator quota override / `PUT /quotas` + spend-class enforcement (no pricing source) — deferred follow-ups; composition-root wiring for the MCP-host approval seam (**Sprint 13.8**) + the sandbox-admission approval seam (**Sprint 14A**, blocked on a Runtime-owned sandbox backend); the scheduler approval + kill-switch + quota conformers are now production-bound at the composition root (**13.7** — engine constructed with all seam slots; the approval consult fires once the 14A submit→execute caller exercises it) | **Sprint 13.8 (MCP seam) + 14A (sandbox seam)** |
| 7 | **Pack lifecycle, trust gate, MCP/A2A protocol surfaces** | **production-grade** (host construction seam-only) | Plugin registry + trust gate + supply chain (`protocol/plugin_registry.py`, `trust_gate.py`, `supply_chain.py`; ADR-016: cosign + SLSA + in-toto + SBOM + vuln + license + 7-yr Sigstore retention); pack lifecycle state machine + portal (ADR-012: author/review/operator/inspection/evidence routes, 5-gate approval composer incl. the 13c adversarial gate, OWASP agentic conformance matrix); MCP host (ADR-002: Streamable HTTP, OAuth/PRM, STDIO 4-gate refusal, `docs/MCP-CONFORMANCE.md`); A2A endpoint (ADR-003: Agent Cards 3-pass validation, task lifecycle, version negotiation, `docs/A2A-CONFORMANCE.md`) | Production MCP-host construction (registry-walk → `MCPServerEntry` → `app.state.mcp_host`) — relocated from the scheduler-only 13.7 to its own sprint (~500-800 LOC: manifest→`MCPServerEntry` mapper + `registry.mcp_servers()` + long-lived `MCPAuthzClient` + transports + mount) | **Sprint 13.8 — MCP Host Construction** |

## Cross-cutting as-built facts

- **Critical-controls gate:** 129 modules at 95/90 floors (`tools/check_critical_coverage.py`), verified at promotion on fresh data every time (`core/emergency/quotas.py` promoted at Sprint 13.6b).
- **Evidence substrate:** hash-chained `decision_history` + `audit_event` (Doctrine Lock D atomicity; canonical form per `core/canonical.py` is the evidence-pack wire format, ADR-006); ISO 42001 evidence-pack export (Sprint 9).
- **Observability:** value-free `llm.gateway.completion` OTel span on every completion; Langfuse OTLP ingestion is a parked follow-up (exporter is gRPC-only today).
- **Eval/quality gates:** bulk eval + LLM-judge + live replay + adversarial gate feeding the pack 5-gate composer (ADR-010/011; Sprints 12–13c).
- **Per-tenant config overlay:** tighten-only, fail-closed (ADR-023, Wave-2 scope shipped).

## Reconciled forward sequence

Named and sequenced. The DONE entries are landed; the forward sprints carry a one-line **scope intent**, NOT a spec — each still gets its own brainstorm → spec → plan → execution cycle when started. The forward part was **resequenced 2026-06-13 for delivery speed** (the substantive change: Deployment Substrate moves earlier — 14B, parallelizable after 13.7 — to prove deployability early) with **NO quality/feature cuts and NO gate lowering**.

**Standing operating rule (2026-06-13):** if any forward sprint expands beyond reviewable size, split it into `A/B` at a VALVE checkpoint (the 13.6 precedent) — but never remove features or lower gates to fit.

**Landed:**

1. **13.5c4** — credentials-validator REMOVE lift + final approval/ADR reconciliation. **DONE 2026-06-12** — refusal verified live then REMOVED (build-time de-blocking only, no runtime change); the ADR-014 13.5c4 amendment closes the approval arc.
2. **13.6 — ADR-018 emergency controls: DONE 2026-06-13 (arc CLOSED).** Split at the half-1 VALVE into:
   - **13.6a — kill switches: DONE.** The `KillSwitchEngine` 8-class matrix + brake-before-evidence flip/revert + `kill_switch.*` UI events + gateway F4 gate & memory `tenant_full` freeze (production-wired via `build_runtime`) + operator portal surface + `EmergencyRBACScope` 1→9 + e2e. See the ADR-018 Sprint-13.6a amendment.
   - **13.6b — quotas: DONE.** `QuotaEngine` (decomposed atomic Redis counters — NOT Lua — with the deny-safe TTL-healing crash window; consumer-owned `_AsyncRedisQuotaLike`, no shared-adapter change) + `gateway_call_ledger` token/cost evidence cols + migration `0010` + gateway tenant-aggregate quota gate & actual-usage metering + scheduler `QuotaInterrogator` conformance + read-only quota portal + `QuotaRBACScope` + env-gated real-Redis atomicity proof; CC 128→129. The Lua approach was REJECTED at the design checkpoint (the adapter exposes only get/set/scan_iter). See the ADR-018 Sprint-13.6b amendment. Deferred follow-ups: operator override / `PUT /quotas`, spend-class enforcement (no pricing source).
3. **13.7 — Composition-Root Wiring (scheduler): DONE 2026-06-13.** `SchedulerEngine` production-constructed at `build_runtime`, CACHE-CONDITIONAL (`None` on the gateway-only path — never a silent `_Null`-quota scheduler that fails on first submit). Every seam slot bound to a real conformer: quota (13.6b) / kill-switch (13.6a `SchedulerKillSwitchConformer`) / pack-state (`PackStoreStateInterrogator`, 11b) / policy (`scheduler.rego` via a dedicated `OPAEngine`) / approval (`runtime.approval_engine`, 13.5b1 — bound into the live engine but DORMANT until a 14A submit→execute caller exercises it; memory 13.5c3 stays the only live-exercised approval seam) / storage; parent-budget is the `_NullParentBudgetResolver` fail-loud sentinel until 14A (top-level submit never consults it; sub-agent submit fails loud). Exposed on `Runtime.scheduler` + `app.state.scheduler`; NO route/caller (Fork D). 3 new Settings (policy bundle + 2 class SLAs); no CC promotion (count stays 129). **Scope split** from the original "all seams" framing — reconnaissance found only the scheduler's prerequisites ready, so MCP-host construction moved to 13.8 and sandbox-approval wiring to 14A (no feature cut; each piece moves to the sprint where its prerequisites exist). See the ADR-022 Sprint-13.7 amendment.

**Forward (resequenced for speed):**

4. **Sprint 13.8 — MCP Host Construction.** Production MCP-host: manifest→`MCPServerEntry` mapper + `registry.mcp_servers()` + long-lived `MCPAuthzClient` + transports + `app.state.mcp_host` mount (~500-800 LOC, relocated from 13.7's scheduler-only scope). Activates the 13.5b2 MCP-host approval seam in production.
5. **Sprint 14A — Managed Agent Runtime Vertical Slice.** The first production-grade long-running / asynchronous run path: submit → scheduler persist → execute via the sandbox / sub-agent primitive → suspend/wake → audit + UI events + evidence. Also lands the scheduler submit→execute caller, the real `LocalParentBudgetResolver` + run→budget snapshot, and the sandbox-admission approval seam (it needs a Runtime-owned sandbox backend, which this slice constructs).
6. **Sprint 14B — Deployment Substrate Z1 Base.** Parallelizable after 13.7. OpenShift-compatible Helm/Kustomize base + AKS smoke environment + externalized secrets + migrations + health checks + observability. Proves deployability early.
7. **Sprint 15A — Dynamic Workflow Orchestration Kernel.** Headless workflow substrate first: workflow definition model + DAG/state machine + scheduler integration + policy/approval/emergency hooks + durable audit/evidence. No visual designer yet.
8. **Sprint 15B — AgentOS ADK / Local Runtime.** Claude Code / Cowork-like local developer loop: scaffold pack/agent + run locally against AgentOS + validate/sign/test + simulate governance + install/publish path.
9. **Sprint 16 — Production-Readiness Validation + Runbooks.** End-to-end bank-deployable proof: extracted packs + a real governed query + full audit chain + evidence export + backup/restore + migration + secret rotation + incident response + the release checklist.

## What this map supersedes

- `docs/ROADMAP.md` (frozen pre-code, 2026-04-26) — retired to a pointer here.
- `docs/BUILD_PLAN.md` Sprint 14/15 sequencing ("per-tenant deployment kit" → folded into Sprint 14B Deployment Substrate; "End-to-end POC" → production-readiness validation reframed as Sprint 16) and the pre-execution Sprint 13.5 section text — see the in-place reconciliation blocks there. The 2026-06-13 forward-plan resequencing (Deployment Substrate moved earlier to 14B; concrete sprint numbers 13.7→16, with **13.8** inserted for MCP-host construction once 13.7 was scoped to scheduler-only; the A/B-valve operating rule) supersedes the original item 3–8 ordering.
- Stale "POC / proof of concept" framing in `README.md` / `docs/PROJECT_PLAN.md` / `docs/source-of-truth/ARCHITECTURE.md` — AgentOS v1 is production-hardened, bank-deployable software validated by live-infrastructure CI lanes and env-gated operator proofs, not a POC.
