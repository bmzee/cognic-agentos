# Sprint 7B.1 — Bank pack lifecycle state machine + storage + harness 4-kind expansion (per ADR-012 + ADR-006 + ADR-009) — Closeout Note

**Date:** 2026-05-11
**Sprints closed:** 7B.1 (pure-functional lifecycle state machine at `packs/lifecycle.py` with 11-state taxonomy + 10 transitions + closed-enum 13-value `LifecycleRefusalReason` + canonical ISO 42001 control mapping helper `iso_controls_for(transition)` + Postgres+Oracle-portable record store at `packs/storage.py` with single-transaction atomic chain-insert + state-cache UPDATE + chain-head UPDATE via `DecisionHistoryStore.append_with_precondition` + Alembic migration `20260510_0003_packs_lifecycle.py` with dialect-portable SQLAlchemy types + 4 PG/Oracle integration canaries + `pack.lifecycle.<target_state>` audit-chain namespace + `cli/test_harness.py` 4-kind vocabulary expansion via public SDK seams + 3-way drift detector + critical-controls floor 41 → 43 with `packs/lifecycle.py` + `packs/storage.py` promoted + AGENTS.md "Authoring — Bank pack lifecycle (Sprint 7B.1)" subsection).
**State:** **READY-FOR-GATE** on `feat/sprint-7b1-lifecycle-state-machine`. No push, no PR, no merge until the human authorises per the AGENTS.md per-action rule.
**Pre-T8 tip:** `8de1dc5 feat(sprint-7b1): T7 — AGENTS.md "Authoring — Bank pack lifecycle (Sprint 7B.1)" subsection + critical-controls floor 41→43 (CRITICAL CONTROLS)`.
**Branch base:** `fcfdbc2` on `main` — the merged Sprint-7A2 PR #21.
**9 commits total after T8 lands** atop the merged Sprint-7A2 baseline (`fcfdbc2` on `main`): T1 (chore plan-file), T2 (`packs/lifecycle.py` pure-functional state machine + drift detectors), T3 (`packs/storage.py` `PackRecord` + `PackRecordStore` + 4 PG/Oracle integration canaries), T4 (Alembic migration 0003 `packs_lifecycle` CC-ADJACENT), T5 (ISO 42001 control mapping + fail-closed semantics tests), T6a (harness 4-kind vocabulary + `hook → cognic.hooks` + 3-way drift detector), T6b (per-kind dry-run dispatch via public SDK seams + R1/R2 reviewer rounds), T7 (AGENTS.md subsection + critical-controls floor 41→43), T8 closeout (this commit).

**Sub-sprint allocation context.** Sprint 7B was pre-split per BUILD_PLAN.md §1142 schedule-risk fallback into 7B.1 + 7B.2 + 7B.3 + 7B.4 before T1. **Sprint 7B.1 is the lifecycle-state-machine + storage + harness 4-kind expansion sub-sprint only.** Portal API endpoints, RBAC scopes, OWASP conformance integration, reviewer evidence panels, 5-gate approval composition, and UI event-stream endpoints all defer to 7B.2 / 7B.3 / 7B.4 per the hand-off checklist below.

## What ships in `feat/sprint-7b1-lifecycle-state-machine` after Sprint 7B.1

### Lifecycle state machine (Sprint-7B.1 T2)

- **`src/cognic_agentos/packs/__init__.py`** — empty package marker.
- **`src/cognic_agentos/packs/lifecycle.py`** (T2, CRITICAL CONTROLS) — pure-functional pack-lifecycle state machine. Closed-enum **13-value** `LifecycleRefusalReason` at `lifecycle.py:165-179` (`lifecycle_transition_invalid_state_pair` / `…_state_unknown` / `…_kind_unknown` / `…_name_unknown` / `…_terminal_state` / `…_kind_state_combination_forbidden` / `…_double_install` / `…_revoke_already_revoked` / `…_uninstall_not_revoked_or_disabled` / `…_withdraw_post_review` / `…_approve_without_review_claim` / `…_disable_not_installed` / `…_allow_list_not_approved`) — finalised at T2 from the plan-of-record's provisional ±1 count as the transition table was enumerated. `_VALID_TRANSITIONS` legal-pair table at `lifecycle.py:196` (**10 transitions / 13 legal pairs:** 7 single-from + 3 multi-from × 2). The 11-state taxonomy (`draft` / `submitted` / `under_review` / `approved` / `rejected` / `withdrawn` / `allow_listed` / `installed` / `disabled` / `revoked` / `uninstalled`) is sourced from ADR-012 §"Lifecycle states". 4-kind support (`tool | skill | agent | hook`) is wired from day one so the Sprint-7A2 carry-forward constraint is satisfied without a kind-schema migration. `validate_transition(*, from_state, to_state, kind, transition)` pure validator at `lifecycle.py:398` — four keyword-only args (positional misuse is a bug-class for state machines; the keyword-only contract eliminates it at the call site). `iso_controls_for(transition)` at `lifecycle.py:343` returns the canonical ISO 42001 control tuple from the 3-value `_KNOWN_ISO_CONTROL_CODES` set (`A.5.31` / `A.5.32` / `A.6.2.4` at `lifecycle.py:249`); helper is the single write source for the chain row's `iso_controls` tag (storage calls it internally; callers cannot inject or override). `LifecycleTransitionRefused(reason)` at `lifecycle.py:309` is a thin wrapper carrying ONLY the closed-enum reason — no `transition_name` field on the exception. Pure-functional: no I/O, no DB access; the only in-tree consumer today is `packs/storage.py:108`. Asymmetric-runtime-guard at `lifecycle.py:393-394` mirrors the same fail-loud pattern at `storage.py:437-438` (preflight transition-name guard that refuses out-of-vocabulary transitions BEFORE any helper invocation).
- **`tests/unit/packs/test_lifecycle.py`** — pure-functional state-machine coverage: closed-enum vocabularies (`PackKind` / `PackState` / `TransitionName` / `LifecycleRefusalReason`) in `TestSprint7B1ClosedEnumVocabulary`; every entry of the `_VALID_TRANSITIONS` table in `TestSprint7B1ValidTransitionsTable` + `TestSprint7B1ValidTransitionsReturnNone`; per-refusal-reason invalid-transition matrix in `TestSprint7B1InvalidTransitionsEmitClosedEnum`; refusal precedence ordering in `TestSprint7B1RefusalPrecedence`; KeyError-leak invariant in `TestSprint7B1NoKeyErrorLeakInvariant`; keyword-only-signature lock in `TestSprint7B1KeywordOnlyArgumentSignature`. The ISO 42001 control mapping + `_KNOWN_ISO_CONTROL_CODES` coverage lives separately in `tests/unit/packs/test_lifecycle_audit.py` per T5.

### Storage layer (Sprint-7B.1 T3)

- **`src/cognic_agentos/packs/storage.py`** (T3, CRITICAL CONTROLS) — Postgres+Oracle-portable `PackRecordStore` at `packs/storage.py:296`; the `DecisionHistoryStore.append_with_precondition` consumer that drives every lifecycle transition through the Sprint-2.5 T2 atomic primitive. `transition()` at `storage.py:360` first runs the preflight transition-name guard at `:437-438` — `if transition not in _TRANSITION_TO_TARGET_STATE: raise LifecycleTransitionRefused("lifecycle_transition_name_unknown")` — which fail-loud refuses out-of-vocabulary transitions BEFORE any helper invocation or DB work (mirrors the asymmetric-runtime-guard at `lifecycle.py:393-394`). On the green path: resolves `target_state = _TRANSITION_TO_TARGET_STATE[transition]` at `:440`, then derives the canonical ISO 42001 control tuple via `iso_controls_for(transition)` at `:456` BEFORE the closure is defined (pure-functional helper call; no I/O); then opens a chain-head `FOR UPDATE` lock (via `append_with_precondition`'s outer envelope). INSIDE the `_precondition` closure at `:458`: `SELECT ... FOR UPDATE` on the `packs` row at `:467-473` → `validate_transition(from_state=…, to_state=…, kind=…, transition=…)` at `:480-485` under the row-locked view → on success `UPDATE packs SET state, last_actor, updated_at` at `:494-502` under the same lock (three columns; no `version_counter` field — atomicity comes from the chain-head + row lock pair, not a counter) → closure returns the captured `(from_state, kind)` tuple. OUTSIDE the closure: `_build_record` at `:505` mints a `DecisionRecord` with `decision_type=f"pack.lifecycle.{target_state}"` at `:508` and the pre-derived `canonical_iso_controls` value (no fresh `iso_controls_for` call from `_build_record`). `append_with_precondition` at `:524` commits chain row + state-cache UPDATE + chain-head UPDATE atomically (single transaction, single commit point).
- **Two-class refusal taxonomy.** Deliberately split — callers dispatch on class to distinguish state-machine refusals from API-contract refusals:
  - `PackNotFound` at `storage.py:219` — missing-pack lookups.
  - `PackRecordRefusalReason` at `storage.py:235` — 1-value Literal (`pack_record_save_draft_initial_state_not_draft`) carried by `PackRecordRefused` at `storage.py:238-265`; refusal class for the `save_draft` API-contract guard only (genesis-state must be `"draft"`, never lifecycle-table refusals).
  - `LifecycleTransitionRefused(reason)` (re-exported from `packs/lifecycle.py:309`) — raised from inside the precondition closure at `storage.py:487` so the engine's transactional rollback fires (no chain row, no state-cache mutation, no orphan INSERT). Exception PROPAGATES up through `append_with_precondition` and `transition()` to the caller — neither storage layer catches it.
- **`tests/unit/packs/test_storage.py`** — unit-scope tests for every refusal class + the `save_draft` initial-state guard + chain-emit shape; 100% line / 100% branch under unit+integration.
- **`tests/integration/packs/test_storage_lock_serialisation.py`** — env-gated (`COGNIC_RUN_POSTGRES_INTEGRATION=1` / `COGNIC_RUN_ORACLE_INTEGRATION=1`) `SELECT ... FOR UPDATE` serialisation proof against live Postgres + Oracle. Two row-lock scenarios mirrored per-dialect (PG + Oracle each): `test_*_row_lock_serialises_competing_transitions` and `test_*_pack_row_lock_blocks_competing_transition` — 4 canaries total covering chain-head FOR UPDATE single-writer enforcement and the pack-row lock that blocks a concurrent state transition while a precondition closure holds the row. Same file also carries the 4 CHECK-constraint enforcement tests (PG + Oracle × `kind` + `state`) covering the unknown-value rejection guaranteed by the migration's `ck_packs_{kind,state}` constraints.

### Alembic migration (Sprint-7B.1 T4)

- **`src/cognic_agentos/db/migrations/versions/20260510_0003_packs_lifecycle.py`** (T4, CC-ADJACENT — doctrine-critical DDL but off the 95/90 floor per Doctrine F gate-counting rule) — `packs` table + supporting indexes. Dialect-portable SQLAlchemy types: `sa.Uuid()` for UUID columns, `chain_hash_column_type()` (Sprint-2 helper) for fixed 32-byte SHA-256 digest material, `sa.TIMESTAMP(timezone=True)` for timestamps to preserve offsets on Oracle, `sa.String(_KIND_ENUM_MAX_LEN)` for `kind` with CHECK constraint enforcing the 4-kind closed enum (`tool | skill | agent | hook`), `sa.String(_STATE_ENUM_MAX_LEN)` for `state` with CHECK constraint enforcing the 11-state closed enum. Two indexes — `ix_packs_kind_state` on `(kind, state)` and `ix_packs_tenant_state` on `(tenant_id, state)` — both created by `op.create_index` in `upgrade()` and dropped in reverse order by `downgrade()`. No `updated_at` index. CHECK constraints `ck_packs_kind` and `ck_packs_state` are inline on the `packs` table and named explicitly so live-DB downgrades drop them without dialect quirks. Downgrade fully reversible.
- **`tests/unit/db/test_migration_20260510_0003.py`** — applies the migration against a SQLite-aiosqlite `tmp_path` DB via the Sprint-2 Alembic seam and verifies: (a) upgrade creates the `packs` table; (b) column inventory matches the `packs/storage.py` SQLAlchemy table object exactly; (c) the two indexes `ix_packs_kind_state` + `ix_packs_tenant_state` are present; (d) downgrade-to-`0002` cleanly drops the table; (e) downgrade-then-upgrade restores it; (f) the `PACKS_TS_TYPE` timestamp type compiles to `TIMESTAMP WITH TIME ZONE` on the Oracle dialect (Oracle-safe TZ test); (g) the chain-hash column type matches Sprint-2 `chain_hash_column_type()` shape.
- **`tests/integration/db/test_alembic_migration_20260510_0003.py`** — env-gated live PG+Oracle round-trip suite mirroring the Sprint-2 integration suite at `test_alembic_migrations.py`. Per-dialect: upgrade-downgrade-upgrade roundtrip + CHECK constraint enforcement for both `kind` and `state` (insert with unknown value → IntegrityError) + `information_schema.check_constraints` (Postgres) / `user_constraints` (Oracle) inventory verification proving both `ck_packs_kind` + `ck_packs_state` constraints landed with their expected definitions.

### ISO 42001 control mapping (Sprint-7B.1 T5)

- **`packs/lifecycle.py`** — two separate ISO surfaces. (1) Canonical 3-value `_KNOWN_ISO_CONTROL_CODES` frozenset at `lifecycle.py:249` (`A.5.31` access control / `A.5.32` configuration management / `A.6.2.4` system change management). (2) Per-transition `_TRANSITION_TO_ISO_CONTROLS` mapping at `lifecycle.py:295-306` (10 transitions × canonical ISO 42001 control tuples, each value strictly drawn from the `_KNOWN_ISO_CONTROL_CODES` set per the module docstring). `iso_controls_for(transition)` at `lifecycle.py:343` is the single write source — storage calls it once per transition BEFORE the precondition closure is defined and threads the result to `_build_record`. Forbidden re-derivation paths: `_build_record` never re-calls `iso_controls_for`; storage callers cannot inject or override the value.
- **Chain-emission contract.** Every state transition emits a hash-chained `pack.lifecycle.<target_state>` event keyed by target state (NOT by transition name and NOT with a `.refused` suffix — refusals roll back BEFORE any chain row is written, so there is no `pack.lifecycle.refused` event). Audit row carries `iso_controls` field populated from `iso_controls_for(transition)`.
- **Fail-closed semantics tests.** Each cited unit test asserts: (a) the closed-enum `LifecycleTransitionRefused.reason` raised, (b) no new chain row, (c) no state-cache mutation. Pinned across two files: `tests/unit/packs/test_lifecycle_audit.py` carries `TestSprint7B1FailClosedRefusalPaths` (split into a PREFLIGHT layer at `storage.py:437-438` that refuses out-of-vocab transition names BEFORE any DB connection is acquired + an IN-PRECONDITION layer where `validate_transition` raises inside the closure and the engine's transaction rolls back) + `TestSprint7B1LifecycleChainIntegrity` for the chain-emission contract + `TestSprint7B1IsoControlsForHelper` / `TestSprint7B1IsoControlsRecordedForEveryTransition` for the ISO control mapping invariants; `tests/unit/packs/test_storage.py` carries `TestSprint7B1PackRecordStoreTransitionRefused` for the storage layer's refusal-causes-rollback proof against a SQLite substrate. Row-lock release semantics + live PG/Oracle serialisation are NOT asserted by these unit tests — they live in the env-gated integration canaries at `tests/integration/packs/test_storage_lock_serialisation.py` for selected paths (the `*_pack_row_lock_blocks_competing_transition` pair proves the pack-row lock holds while a precondition closure runs and releases on rollback; the `*_row_lock_serialises_competing_transitions` pair proves chain-head FOR UPDATE single-writer enforcement under concurrent transactions).

### Harness 4-kind vocabulary expansion (Sprint-7B.1 T6a + T6b)

- **`src/cognic_agentos/cli/test_harness.py`** (T6a + T6b, NOT-CC — boundary doctrine evolution of an existing harness) — Sprint-7A2 T13 narrowed `_HARNESS_SUPPORTED_KINDS = frozenset({"tool"})` to refuse skill / agent / hook dispatch at the harness boundary; **Sprint-7B.1 T6a + T6b grow the dispatch table to `{"tool", "skill", "agent", "hook"}` and wire per-kind dry-run dispatch via the public SDK seams** (no harness-internal monkey-patching of Tool / Skill / Agent / Hook ABC internals). T6a adds the vocabulary + `hook → cognic.hooks` entry-point group mapping + 3-way drift detector pinning the kind enum against `[pack].kind` taxonomy. T6b adds the per-kind dispatch impls calling the actual public SDK seams (`Tool.invoke` at `sdk/tool.py:144` / `Skill.execute` at `sdk/skill.py:110` / `Agent.handle` at `sdk/agent.py:52` / `Hook.invoke` at `sdk/hook.py:347` — four methods, four different names, one method per kind) — pinning SDK-seam-correctness via R1+R2 reviewer rounds.
- **`tests/unit/cli/test_harness_vocabulary.py`** (T6a) — pins `_HARNESS_SUPPORTED_KINDS` against the `PackKind` Literal + the `_KIND_TO_ENTRY_POINT_GROUP` table including the `hook → cognic.hooks` row + the harness's synthetic-unknown-kind refusal path emitting `harness_unsupported_pack_kind`.
- **`tests/unit/cli/test_harness_dispatch.py`** (T6b) — pins per-kind green-path dispatch (`test_run_harness_against_{tool,skill,agent,hook}_reference_pack_dispatches_green`) + the hook-SDK contract regressions that fire BEFORE / AFTER `Hook.invoke` (context error before invoke, payload error before invoke, malformed result shape after invoke) + the payload-never-logged invariant carried into the dispatch report + the per-name identity contract for `Skill.execute`'s multi-tool registry + the `sha256(payload) ↔ TaskRecord.payload_digest` lockstep on `Agent.handle`'s dispatch path.
- **Reference pack `examples/cognic-hook-example-minimal/`** — Sprint-7A2 T11's hook reference pack now exercises the green-path harness dispatch via the central `tests/unit/cli/test_reference_packs_full_lifecycle_green.py` 4th lifecycle arm (lifted from the Wave-1 `harness_unsupported_pack_kind` refusal that was pinned at Sprint-7A2 T13).

### Critical-controls coverage gate extension (Sprint-7B.1 T7)

- **`tools/check_critical_coverage.py`** — extended in T7 with the Sprint-7B.1 lifecycle pair at the single strict 95% line / 90% branch floor: `packs/lifecycle.py`, `packs/storage.py`. Gate now enforces **43 modules**; all PASS at current coverage. Off-gate per Doctrine F gate-counting rule: `src/cognic_agentos/db/migrations/versions/20260510_0003_packs_lifecycle.py` (Alembic version file is doctrine-critical DDL but not coverage-tracked by convention; T4 marked Halt: YES + Gate counted?: NO). Off-gate per boundary-evolution rule: `cli/test_harness.py` (T6a + T6b extend an existing CC-adjacent boundary, not a fresh CC promotion).

### AGENTS.md doctrine surface (Sprint-7B.1 T7)

- **`AGENTS.md`** — new "Authoring — Bank pack lifecycle (Sprint 7B.1)" critical-controls subsection inserted between Sprint-7A2 hook quartet and Isolation boundaries. Two bullets pinning `packs/lifecycle.py` + `packs/storage.py` to file:line — every load-bearing surface (LifecycleRefusalReason 13 values; `_VALID_TRANSITIONS` 10-transition table; `iso_controls_for` write source + threading contract; preflight transition-name guard at `storage.py:437-438`; precondition closure at `storage.py:458`; `validate_transition` call-site inside closure at `:480-485`; UPDATE packs 3-column shape at `:494-502`; chain namespace `pack.lifecycle.<target_state>` at `:508`; exception propagation contract `engine.begin() at core/decision_history.py:482` rolls back, neither storage layer catches). Doctrine Lock C (lifecycle pure-functional + closed-enum consumer-API wire-protocol) + Doctrine Lock D (atomic chain-insert + state-cache UPDATE + chain-head UPDATE single transaction) named explicitly.

### Closeout doc + BUILD_PLAN §602 status flip (Sprint-7B.1 T8 — this commit)

- **`docs/closeouts/2026-05-11-sprint-7b1-lifecycle-state-machine.md`** *(NEW; this file)* — Sprint 7B.1 closeout note; mirrors Sprint-7A2 closeout style.
- **`docs/BUILD_PLAN.md` §602** — section heading suffixed with "(pre-split per BUILD_PLAN §1142 schedule-risk fallback into 7B.1 + 7B.2 + 7B.3 + 7B.4)"; 7B.1 status row added marking sub-sprint CLOSED with gate count 41→43 + 2 CC modules promoted (`packs/lifecycle.py`, `packs/storage.py`).

## CI / production-grade gates

| Gate | Workflow | Trigger | Behaviour |
|---|---|---|---|
| Lint + types + tests | `python.yml` → `lint + test` | push / PR | unchanged — `ruff` + `ruff format --check` + `mypy` strict + `pytest -v` |
| Per-file critical-controls coverage gate | `python.yml` → `lint + test` | push / PR | `tools/check_critical_coverage.py` against `coverage.json` — fails CI if any of the **43** critical-controls modules drops below 95% line OR 90% branch (extended in Sprint-7B.1 T7 to add the lifecycle pair) |
| MCP STDIO architecture test | `python.yml` → `lint + test` | push / PR | unchanged — Sprint-5 floor `≥ 5` stays |
| A2A no-subprocess architecture test | `python.yml` → `lint + test` | push / PR | unchanged — Sprint-6 closeout's `≥ 9` floor stays |
| A2A caller-URL architecture test | `python.yml` → `lint + test` | push / PR | unchanged — Sprint-6 |
| A2A spec-drift CI gate | `python.yml` → `a2a-spec-drift` (env-gated `COGNIC_RUN_A2A_UPSTREAM=1`) | push / PR | unchanged — Sprint-6 |
| Image-size budget + boot smoke (kernel) | `python.yml` → `image size budget` | push / PR | unchanged — kernel still ≤120 MiB; Sprint-7B.1 ships no new runtime dependencies |
| Image-size budget + boot smoke (default-adapters) | `python.yml` → `image size budget` | push / PR | unchanged |
| Live Postgres / Oracle integration | `python.yml` → `postgres integration` / `oracle integration` | push / PR | extended — Sprint-7B.1 T3 + T4 add integration canaries in two files. **`tests/integration/packs/test_storage_lock_serialisation.py`** covers 4 PG+Oracle scenarios: (1) row-lock serialisation of competing transitions (`*_row_lock_serialises_competing_transitions`); (2) pack-row `FOR UPDATE` blocking a concurrent transition (`*_pack_row_lock_blocks_competing_transition`); (3) CHECK constraint rejection of unknown `kind` (`*_check_constraint_rejects_unknown_kind`); (4) CHECK constraint rejection of unknown `state` (`*_check_constraint_rejects_unknown_state`). **`tests/integration/db/test_alembic_migration_20260510_0003.py`** separately covers: upgrade-downgrade-upgrade roundtrip + `information_schema.check_constraints` (Postgres) / `user_constraints` (Oracle) inventory verification + CHECK constraint enforcement for both columns on both dialects. All env-gated. |

## Doctrine adherence

- **AGENTS.md per-edit halt-before-commit on critical-controls modules.** Every commit that touched a critical-controls module paused for explicit user authorization. T2 (`packs/lifecycle.py` new CC module) halt-reviewed; T3 (`packs/storage.py` new CC module) halt-reviewed across R1+R2 (atomic-primitive choice + two-class refusal taxonomy); T4 (Alembic migration 0003 CC-ADJACENT — doctrine-critical DDL) halt-reviewed; T5 (chain-emission contract via `iso_controls_for` helper) halt-reviewed; T6a (harness boundary doctrine evolution) halt-reviewed; T6b (per-kind dispatch via public SDK seams) halt-reviewed across R1+R2 (SDK-seam-correctness load-bearing); T7 (AGENTS.md + coverage gate config) halt-reviewed across R1-R7 (**7 reviewer rounds closed pre-commit** on a doctrine-only commit; see "Cite-from-memory anti-pattern" doctrine entry below). T8 (this closeout + BUILD_PLAN §602) halts on AGENTS.md + BUILD_PLAN.md (both doctrine documents). **Behavioral findings produced regression tests pinning the fix; doc-only findings were closed by text edits + R2-extended grep audits per `feedback_strict_review_off_gate.md` §8.**
- **AGENTS.md `core/canonical.py` per-edit stop rule.** Not touched in Sprint 7B.1.
- **Closed-enum vocabulary doctrine.** Sprint-7B.1 added `LifecycleRefusalReason` (13 values at `packs/lifecycle.py:165-179`); `PackRecordRefusalReason` (1 value at `packs/storage.py:235`). Taxonomy surfaces: 11-state `PackState` Literal at `lifecycle.py:116` + runtime frozenset `_KNOWN_STATES` at `lifecycle.py:232`; 4-kind `PackKind` Literal at `lifecycle.py:111` + runtime frozenset `_KNOWN_KINDS` at `lifecycle.py:228`; 10-transition `_VALID_TRANSITIONS` mapping at `lifecycle.py:196`; 3-control `_KNOWN_ISO_CONTROL_CODES` frozenset at `lifecycle.py:249`. Drift detectors live in `tests/unit/packs/test_lifecycle.py::TestSprint7B1ClosedEnumVocabulary`.
- **Doctrine Lock C (lifecycle pure-functional + closed-enum consumer-API wire-protocol).** `packs/lifecycle.py` has no I/O and no DB access. The closed-enum `LifecycleRefusalReason` vocabulary IS the consumer-API wire-protocol contract carried by `LifecycleTransitionRefused.reason`. Chain audit events themselves use the `pack.lifecycle.<target_state>` namespace at `storage.py:508` keyed by target state, not transition name, and not with a `.refused` suffix — refusals roll back BEFORE any chain row is written.
- **Doctrine Lock D (atomic chain-insert + state-cache UPDATE + chain-head UPDATE single transaction).** Lifecycle transitions ride `DecisionHistoryStore.append_with_precondition`'s Sprint-2.5 T2 atomic envelope — chain row + `packs` UPDATE + chain-head UPDATE commit as one transaction, one commit point. Row lock + chain-head FOR UPDATE pair is the atomicity primitive (no version counter). Integration canaries pin serialisation on live Postgres + Oracle.
- **Doctrine F gate-counting rule.** Critical-controls floor extension at T7 adds `packs/lifecycle.py` + `packs/storage.py` to the 95/90 floor; the Alembic migration version file at `src/cognic_agentos/db/migrations/versions/20260510_0003_packs_lifecycle.py` is doctrine-critical DDL but excluded from coverage-gate by convention. `cli/test_harness.py` stays off the floor because T6a + T6b are boundary-evolution of an existing CC-adjacent surface, not fresh promotions.
- **Asymmetric-runtime-guard pattern (T2 + T3).** Preflight `if transition not in _TRANSITION_TO_TARGET_STATE: raise LifecycleTransitionRefused("lifecycle_transition_name_unknown")` at `storage.py:437-438` mirrors the fail-loud pattern at `lifecycle.py:393-394`. Refuses out-of-vocabulary transitions BEFORE any helper invocation or DB work; eliminates a class of silent-no-op bugs by treating out-of-vocab keys as refusal-class rather than lookup-fallback.
- **Cite-from-memory verification depth doctrine (T7 R1-R7; 9 sub-shapes catalogued).** 7 reviewer rounds on a doctrine-only commit because cite-from-plan paraphrase masked 9 distinct sub-shapes of drift from R1's initial grep audit. Sub-shapes catalogued at compact-summary level — cite-from-plan-vocabulary (wrong semantic content like 7-value vs 13-value enum); wrong validator signatures; refusal-class conflation across two-class taxonomies; wrong UPDATE columns + wrong target table; wrong audit-event namespace; wrong filesystem path; cite-the-wrong-runtime-consumer (T6b harness vs actual T3 storage); identifier word-wrap splits defeating grep; source-modules repeat the same drift across forward-referenced comments; path word-wrap splits; line-number drift after CC source patch; call-site-scope conflation (inside vs outside closure); sequence-step omission (skipped preflight guard); exception-flow precision (catch vs propagate). New doctrine: **verify EXISTENCE + CONTENT + ACTUAL-CONSUMERS + identifier/path ATOMICITY + LINE NUMBERS post-source-edit + CALL-SITE SCOPE + SEQUENCE-STEP COMPLETENESS + EXCEPTION-FLOW PRECISION + cross-update doc citations after every CC source patch.**

## Test + coverage state

- **Suite size:** **4370 passed / 46 skipped / 605.80s** (full suite at user-run baseline immediately pre-T8-R3 Option B; T2-T6b each landed with focused targeted suites green; T7 + T8 R1-R5 are doctrine + comment-only, no executable code touched — `pytest --cov=cognic_agentos --cov-branch --cov-report=json` so the same `coverage.json` feeds the per-file critical-controls gate).
- **Per-file critical-controls coverage gate (43 modules at 95/90):** all 41 Sprint-7A + Sprint-7A2 modules unchanged + Sprint-7B.1 pair:

| Module | Sprint | Line% | Branch% | Status |
|---|---|---|---|---|
| (Sprint-7A 37 + Sprint-7A2 hook quartet — 41 modules unchanged from `2026-05-10-sprint-7a2-hook-packs-runtime.md`) | 2-7A2 | ≥95 | ≥90 | PASS |
| `packs/lifecycle.py` | 7B.1 | 100 | 100 | PASS |
| `packs/storage.py` | 7B.1 | 100 | 100 | PASS |

## ADR validation

| ADR | Title | Sprint-7B.1 relevance | Status |
|---|---|---|---|
| ADR-006 | ISO 42001 control mapping | Lifecycle transitions emit hash-chained `pack.lifecycle.<target_state>` audit events tagged with canonical ISO 42001 control tuples via `iso_controls_for(transition)` write source; 3-value `_KNOWN_ISO_CONTROL_CODES` (`A.5.31` / `A.5.32` / `A.6.2.4`) | ✅ |
| ADR-009 | Pluggable infrastructure adapters | `PackRecordStore` is dialect-portable (Postgres + Oracle live integration canaries pin row-lock serialisation + Alembic upgrade/downgrade + CHECK-constraint enforcement); SQLAlchemy types chosen for dialect portability | ✅ |
| ADR-012 | Bank pack lifecycle | 11-state taxonomy + 10-transition table + 4-kind support + chain-emission contract; Portal API endpoints + RBAC scopes + 5-gate approval composition + OWASP conformance integration are deferred to 7B.2 / 7B.3 / 7B.4 per the sub-sprint split | ✅ (lifecycle state-machine + storage scope only) |

## Doctrine amendments accepted (Sprint-7B.1)

- **Doctrine Lock C (lifecycle pure-functional + closed-enum consumer-API wire-protocol).** Locked at T2; consumed by T3 / T5 / T7.
- **Doctrine Lock D (atomic chain-insert + state-cache UPDATE + chain-head UPDATE under a single transaction).** Locked at T3; consumed by T5 (chain namespace) + T7 (AGENTS.md doctrine surface).
- **Doctrine F gate-counting rule (Alembic migration version files are doctrine-critical DDL but excluded from coverage-gate by convention).** Locked at T4; consumed by T7.
- **Asymmetric-runtime-guard pattern.** **3 instances** in the Sprint-7B.1 lifecycle subsystem, each refusing out-of-vocab transition names with the closed-enum `LifecycleTransitionRefused("lifecycle_transition_name_unknown")` rather than letting them through as a silent-no-op `KeyError` at the downstream mapping lookup: (1) `lifecycle.py:472-473` — `validate_transition` step 3 guard against `_KNOWN_TRANSITIONS` (added at T2 R1 P2); (2) `lifecycle.py:393-394` — `iso_controls_for` guard against `_TRANSITION_TO_ISO_CONTROLS` (added at T5 R1 P2); (3) `storage.py:437-438` — `PackRecordStore.transition()` preflight guard against `_TRANSITION_TO_TARGET_STATE` (added at T3 R1 P2 #2). T7 R7 only patched the prose comments around these guards (false T6-harness-consumer claims removed from the `LifecycleTransitionRefused` class docstring at `lifecycle.py:312` + `storage.py:211-213` + `:428-430`); T7 added no runtime behavior. Sprint-7B.1 T8 R3 Option B added explicit propagation/rollback wording inside `lifecycle.py` at the (1) + (2) guard comments, shifting every other lifecycle.py coordinate downward by 5 lines (and the validate_transition step 3 guard at (1) by an additional +4 lines from the second comment expansion). The three sites mirror each other on the pattern level: out-of-vocab lookup key → closed-enum refusal, not silent fallback.
- **Cite-from-memory verification depth doctrine (new from T7 R1-R7).** Strongest concrete loss case to date — 7 reviewer rounds on doctrine-only files. Saved at `feedback_verify_code_citations_at_doc_write.md` (Sprint-7A T13 precedent) but extended at T7 with 9 sub-shapes. See "Cite-from-memory anti-pattern" doctrine entry above.

## Sprint 7B.2 hand-off checklist

Sprint 7B.2 picks up the **Portal API endpoints + RBAC scopes + OWASP conformance integration** layer of ADR-012 + BUILD_PLAN.md §602.

- [ ] **Portal API endpoints (BUILD_PLAN §613-617).** 4 surfaces × ~24 endpoints: author (`POST /api/v1/packs/drafts`, `PUT /…/{id}`, `POST /…/submit`, `DELETE /…/{id}`); review (`GET /api/v1/packs?status=submitted`, `POST /…/{id}/claim`, `POST /…/{id}/approve`, `POST /…/{id}/reject`, `GET /…/{id}/evidence`); operator (`POST /…/{id}/allow-list`, `POST /…/{id}/install`, `POST /…/{id}/disable`, `POST /…/{id}/revoke`, `DELETE /…/{id}/install`); inspection (`GET /api/v1/packs`, `GET /…/{id}`, `GET /…/{id}/audit`, `GET /…/{id}/invocations`).
- [ ] **14 RBAC scopes per ADR-012 (BUILD_PLAN §619-623).** Author: `pack.submit`, `pack.withdraw`. Reviewer: `pack.review.claim`, `pack.review.approve`, `pack.review.reject`. Operator: `pack.allow_list`, `pack.install`, `pack.disable`, `pack.revoke`, `pack.uninstall`. Examiner: `pack.audit.read`, `pack.invocation.read`.
- [ ] **OWASP Agentic Top 10 + Agentic Skills Top 10 conformance suite (BUILD_PLAN §625-628).** `packs/conformance/__init__.py` + `packs/conformance/owasp_agentic.py` — tool misuse / goal hijacking / identity abuse / prompt-injected skills / dependency poisoning / secret exfiltration / unsafe filesystem-network access checks.
- [ ] **`agentos conformance` CLI extension.** `packs/conformance/cli.py` — local-run command for the OWASP suite; mirrors Sprint-7A `agentos verify` flow.
- [ ] **`agentos test-harness` CLI extension (per ADR-012 §"Local governance test harness").** Local-run command exercising the lifecycle state machine + Postgres+Oracle row-lock serialisation against a developer-local DB; Sprint-7B.1 T6a + T6b's harness 4-kind expansion is the seed.
- [ ] **Auto-run conformance on `submit` transition; attach failure to evidence.** Every `submit` transition runs the OWASP suite; any failure attaches to the pack record's evidence panel + gates downstream approval until cleared. Per BUILD_PLAN §627.

## Sprint 7B.3 + 7B.4 hand-off (carry-forward; reserved for owning sub-sprints)

- **7B.3** — Reviewer evidence panels (BUILD_PLAN §630-634): `packs/evidence/data_governance.py`, `packs/evidence/risk_tier.py`, `packs/evidence/supply_chain.py`, `packs/evidence/conformance_matrix.py`. 5-gate approval composition + reviewer-acknowledgement field enforcement.
- **7B.4** — UI event-stream endpoints per ADR-020 (BUILD_PLAN §640-645): SSE endpoints + frontend-action POST + portable JSON schema published at `/.well-known/cognic-ui-events.json`. Carry-forward from Sprint-7A2: realtime auto-attestation API, compliance helper emit path, `fail_open_exception` build-time manifest shape per ADR-017 amendment A4.

## Carryover

None. Every plan §406-430 deliverable for T8 landed.

## Out of scope (Sprint-7B.1 intentionally did NOT ship)

- **Portal API endpoints + RBAC scopes + OWASP conformance integration** — Sprint-7B.2 per the hand-off checklist above.
- **Reviewer evidence panels + 5-gate approval composition** — Sprint-7B.3.
- **UI event-stream endpoints (SSE + frontend-action POST + portable JSON schema)** — Sprint-7B.4 per ADR-020.
- **`fail_open_exception` build-time manifest shape per ADR-017 amendment A4** — Sprint-7A2 carry-forward; reserved for Sprint-7B.4 or a Sprint-7C2 mirror.
- **Realtime auto-attestation API + compliance helper emit path** — Sprint-7A carry-forward; reserved for Sprint-7B.4.
- **Studio UI authoring** — ADR-008 Phase B; explicitly deferred per ADR-008 + ADR-021 reservation.

## Next sprint

**Sprint 7B.2 — Portal API endpoints + RBAC scopes + OWASP conformance integration**. See BUILD_PLAN.md §602 status row + the hand-off checklist above. Sprint-7B.1's lifecycle state machine + storage + 4-kind harness expansion is the foundation Sprint-7B.2 builds the portal surface on top of; the chain-emission contract + RBAC scope vocabulary + OWASP suite all wire through to the same `pack.lifecycle.<target_state>` audit-chain namespace and the same `PackRecordStore.transition()` atomic primitive established here.
