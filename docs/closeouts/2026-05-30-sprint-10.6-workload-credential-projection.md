# Sprint 10.6 - Workload Credential Projection - Pre-Merge Closeout Report

**Date:** 2026-05-30
**Branch:** `feat/sprint-10.6-workload-credential-projection`
**Head at verification:** `f2dc126`
**Branch distance:** 43 commits ahead of `main`
**State:** READY-FOR-PR / HOLD. Code, full gate, and the canonical Z3/Z4
operator audit are green. The Sprint 10.6 and Phase 3 `CLOSED` markers remain
merge-time only.

## Scope

Sprint 10.6 closes the workload credential projection split from Sprint 10.5c.
The implementation covers the ADR-004 section 25 credential projection runtime
contract and the ADR-017 data-governance credential declaration contract.

The closeout gate follows the Sprint 10.6 plan Task 26 path and the design
anchors at section 5.7 (audit event payloads) and section 5.8
(`SandboxBackend.create()` lifecycle integration). Earlier plan revisions cited
a legacy section reference; the actual closeout evidence is tied to sections
5.7 and 5.8 in:

- `docs/superpowers/specs/2026-05-26-sprint-10.6-workload-credential-projection-design.md`
- `docs/superpowers/plans/2026-05-26-sprint-10.6-workload-credential-projection.md`

## Canonical Artifacts

| Artifact | Signed reference |
|---|---|
| Runtime image | `ghcr.io/bmzee/cognic-agentos/sandbox-runtime-python@sha256:b9ed3440ebf8535ba779f574b3c12a45095720ce78c292d8cc5cd338990e8eac` |
| Egress proxy image | `ghcr.io/bmzee/cognic-agentos/sandbox-egress-proxy@sha256:eb4ea75b427d0bc42039c68039eec51d6b0d0789400ba5bfdbf470ebec9139aa` |
| Trust root | `/Users/bmz/.cognic/canonical-signing/cognic-canonical-cosign.pub` |
| Expected workload GID | `65534` |

Digest guard at T15 confirmed `Settings`, Docker backend constants, and K8s
backend constants all point at these canonical refs.

## Audit Evidence

| Gate | Environment | Result |
|---|---|---|
| Z4 K8s happy path | CRC / OpenShift namespace `cognic-sandbox`, GHCR pull-secret, canonical runtime and proxy, Vault dynamic credential path | `2 passed in 1.96s` |
| Z4 K8s negative path | Same run, two-credential LIFO unwind | Included in `2 passed in 1.96s` |
| Z3 Docker happy path | GitHub Actions `ubuntu-24.04`, Docker, ephemeral Vault + Postgres, canonical runtime and proxy | `2 passed in 12.15s` |
| Z3 Docker negative path | Same workflow run, field-set mismatch refusal before container create | Included in `2 passed in 12.15s` |

Z3 GitHub Actions audit:

- Workflow: `Z3 canonical Docker audit`
- Run id: `26669996941`
- Commit: `f2dc126`
- Created: `2026-05-30T00:59:32Z`
- Runner: `ubuntu-24.04`
- Final target step used `sudo -E uv run pytest` so the Docker credential
  executor could perform the production `chown` to workload GID `65534` on
  Linux `/dev/shm`.

Z4 CRC audit:

- Command targets:
  - `tests/integration/sandbox/test_z4_k8s_credential_projection.py::test_z4_happy_path_real_vault_real_k8s_one_credential`
  - `tests/integration/sandbox/test_z4_k8s_credential_projection.py::test_z4_negative_path_two_credential_lifo_unwind`
- Result: `2 passed in 1.96s`
- The pre-rerun `proxy-diag` pod confirmed the canonical proxy sidecar starts
  on CRC with `SESSION_ID`, `ALLOW_LIST`, writable `/etc/cognic-proxy`,
  writable `/var/log/cognic-proxy`, UID `10002(cognicproxy)`, supplementary
  group `65534`, and a readable `access.jsonl` contract.

## T30 Tail Evidence

The post-digest canonical arc landed on top of the original Sprint 10.6
credential-projection implementation:

| Commit | Evidence |
|---|---|
| `de0cb0e` | T10 Settings canonical image refs |
| `dc69475` | T11 backend factory canonical catalog injection |
| `8034384` | T12 backend egress-proxy constants swapped to real signed ref |
| `51b4862` | T13 live egress-enforcement proof, including denied HTTPS CONNECT |
| `37cf45c` | T14 Step 2b real-catalog admission proof against signed artifacts |
| `1c09fbf` | T14.1 K8s/Docker proxy env, writable config, Docker proxy UID 10002 |
| `e3f1f7d` | T14.2 K8s proxy-log readiness gate |
| `f2dc126` | Z3 GitHub audit workflow runs the canonical Docker proof with chown-capable sudo |

## T15 Verification

All verification below ran at `f2dc126` after the Z3/Z4 audit was green:

| Verification | Result |
|---|---|
| Digest-match guard | PASS |
| `uv run ruff check .` | PASS |
| `uv run ruff format --check .` | PASS, `619 files already formatted` |
| `uv run mypy src tests` | PASS, `603 source files` |
| `uv run pytest -q --cov=cognic_agentos --cov-branch --cov-report=json` | PASS, `8602 passed / 94 skipped / 2 warnings` in `653.57s` |
| `uv run python tools/check_critical_coverage.py` | PASS |

Critical-control coverage highlights from the fresh `coverage.json`:

| Module | Line | Branch |
|---|---:|---:|
| `src/cognic_agentos/sandbox/backends/docker_sibling.py` | 97.15% | 95.00% |
| `src/cognic_agentos/sandbox/backends/kubernetes_pod.py` | 96.76% | 94.68% |
| `src/cognic_agentos/sandbox/projection.py` | 100.00% | 100.00% |

## Remaining Human-Only Steps

- Decide whether to keep the branch-local
  `.github/workflows/z3-canonical-docker-audit.yml` as a durable manual audit
  workflow, or remove/squash it before final PR merge.
- Open the PR.
- Merge after review.
- Add the Sprint 10.6 and Phase 3 `CLOSED` markers only at the PR merge, with
  the merge commit hash and final branch-commit count.

The feature branch is already on `origin` from the Z3 CI audit workflow runs.
No merge-to-main push, PR, merge, or CLOSED marker is performed by this
closeout report.
