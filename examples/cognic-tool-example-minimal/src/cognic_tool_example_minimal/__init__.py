"""Sprint-7A T15 reference tool pack — inert by design.

The package exists to host the entry-point target for
``[project.entry-points."cognic.tools"]`` in pyproject.toml. The
``ExampleMinimalTool`` class is the integration surface the load probe
exercises during ``agentos verify``.
"""
