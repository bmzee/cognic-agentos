apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "agentos.fullname" . }}-config
  labels:
    {{- include "agentos.labels" . | nindent 4 }}
data:
  {{- include "agentos.configData" . | nindent 2 }}
