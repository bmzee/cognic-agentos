"""{{ module_name }} — Sprint-7A2 hook pack scaffold.

The hook entry-point class lives in ``hook.py`` next to this
``__init__``; the SDK's runtime registry (Sprint-7A2 T7) discovers
it via ``importlib.metadata.entry_points()`` against the
``cognic.hooks`` group.
"""
