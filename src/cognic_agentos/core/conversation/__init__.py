"""ADR-028 conversational sessions — the kernel-owned conversation primitive.

M8.5-A ships the substrate: the ``conversations`` / ``conversation_turns`` store
(:mod:`cognic_agentos.core.conversation.storage`) and the closed-enum lifecycle
vocabulary (:mod:`cognic_agentos.core.conversation._types`).
"""
