"""Portal surface — HTTP endpoints + (Wave 2+) UI components.

Layer classification: **portal surface**.

Per ``AGENTS.md`` "Code layers", portal surfaces depend on platform
primitives, not the other way round. The portal never imports a plugin pack.
"""
